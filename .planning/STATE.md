---
gsd_state_version: 1.0
milestone: v1.5
milestone_name: Hybrid Chat Search
current_plan: "Phase 66 COMPLETE (66-01..04). Next milestone work: Phase 67 (Cmd+K palette) or Phase 68 (backfill) — both depend on 65, not on 66."
status: completed
stopped_at: Completed 66-04-PLAN.md
last_updated: "2026-05-29T22:19:40.457Z"
last_activity: "2026-05-29 — 66-04 landed: full-phase e2e (UI-01/02/03/04) on chromium + mobile-chromium; Rule-1 fix to ChatThread so the sidebar click-journey deep-link actually pulses + strips ?m="
progress:
  total_phases: 6
  completed_phases: 4
  total_plans: 11
  completed_plans: 11
  percent: 67
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-20) · .planning/ROADMAP.md (v1.5 Phases 63-68, 2026-05-29) · .planning/REQUIREMENTS.md

**Core value:** Anyone can sit down, chat with persistent memory, find agent apps others built, and create their own — the full loop from user to builder in one product, with full visibility and control over what every extension can do.
**Current focus:** v1.5 Hybrid Chat Search — roadmap created 2026-05-29. Six phases (63-68), 35 requirements mapped 100%. Build order: 63 Indexing Primitives (schema + outbox + chunker + embedder truncation fix + test-harness widening) → 64 Embed-on-Write Worker → 65 Hybrid Search SQL + API → then 66 Sidebar / 67 Cmd+K Palette / 68 Backfill in parallel. Index type is HNSW (NOT ivfflat). Reuses the existing `all-MiniLM-L6-v2` Transformers.js embedder + pgvector already wired for memories + knowledge_base_chunks. Next: `/gsd:plan-phase 63`.

## Current Position

Milestone: v1.5 Hybrid Chat Search
Phase: 66 — Sidebar Search (COMPLETE — 66-01 infra + 66-02 sidebar + 66-03 deep-link + 66-04 e2e all landed)
Current Plan: Phase 66 COMPLETE (66-01..04). Next milestone work: Phase 67 (Cmd+K palette) or Phase 68 (backfill) — both depend on 65, not on 66.
Status: 66-04 complete — full-phase end-to-end coverage (UI-01/02/03/04) across chromium + mobile-chromium. Extended `web/e2e/conversation-search.spec.ts` (viewport-aware helpers + toggle/default, reload-persist, two-section results, <2-char guard, degraded-no-mutate, generic empty) and created `web/e2e/sidebar-search-deeplink.spec.ts` (recent / strip-on-reload / paginated-out window-grow / off-branch branch-switch / unknown-id no-op / group-header-not-deep-link). 42/42 e2e green on both projects (21 cases × 2). Reuses the 66-01 /api/search/messages mock + makeSearchHit; clicks real 66-02 message-hit rows. One Rule-1 SUT fix in `ChatThread.svelte`: the `?m=` deep-link only fired on COLD MOUNT, so the sidebar click-journey (client `goto(...?m=)` on the persistent, non-remounting component) never pulsed and never stripped `?m=`. Moved the `?m=` consume into a reactive `$effect` (fires on cold load AND client nav) and made `resolveDeepLink found:false` non-terminal until `initialLoadDone` (so it retries once the target conv's tree loads instead of giving up against the previous conv's messages). 23/23 ChatThread.component + 84 adjacent ChatThread/deep-link vitest tests stay green; svelte-check clean on all owned files. Sacred-12-stash held (12→12); explicit-path adds only.
Last activity: 2026-05-29 — 66-04 landed: full-phase e2e (UI-01/02/03/04) on chromium + mobile-chromium; Rule-1 fix to ChatThread so the sidebar click-journey deep-link actually pulses + strips ?m=

Progress: [███████   ] v1.5 67% — 4/6 phases complete (63 + 64 + 65 + 66); 67/68 unblocked (depend on 65)

**v1.5 phase map (35/35 requirements, 100% coverage):**
| Phase | Goal | Requirements | Depends on |
|-------|------|--------------|-----------|
| 63 — Indexing Primitives | Durable chunk+outbox foundation on HNSW; embedder truncation fix; test-harness widening | IDX-01..07 | — |
| 64 — Embed-on-Write Worker | Background outbox drainer; degraded gate, retry/backoff, boot recovery, kill-switch | ING-01..05 | 63 |
| 65 — Hybrid Search SQL + API | Single-CTE RRF query + `/api/search/messages` (hybrid/keyword/semantic), in-CTE scoping, degraded fallback | SRCH-01..08 | 64 |
| 66 — Sidebar Search | Mode toggle + localStorage + deep-link on `ConversationList.svelte` | UI-01..04 | 65 |
| 67 — Command Palette Search | Cmd+K palette extending `CommandPalette.svelte`; Cmd+Shift+P rebind; a11y; mobile BottomSheet | PAL-01..07 | 65 |
| 68 — Backfill + Operations | Resumable idempotent backfill; throttle; post-batch ANALYZE; progress visibility | OPS-01..04 | 64 |

**Research flags:** Phase 65 (RRF k-value measurement + `hnsw.iterative_scan` cross-backend availability) and Phase 67 (palette extension point + mobile adapter + shortcut collision) recommended for `/gsd:research-phase` during planning.

<details>
<summary>Previous milestone closeout — v1.4 Phase 62 final state (archived 2026-05-20)</summary>

Plan 62-06 complete — Phase 62 fully complete (9 of 9 plans landed). 62-06: 180-line vitest component-test added at `web/src/__tests__/MetaAgentChat.component.test.ts` + 33-line malformed-JSON arm appended to `web/src/__tests__/api-agent-configs-generate.server.test.ts`, covering Phase 6 sub-plan 06-04 MetaAgentChat deliverable. 10/10 cases pass on `cd web && bunx vitest run` (1.44s): server-side third arm asserts the regex+JSON.parse extractor at `+server.ts:39-50` returns `config=null` (not 500) on malformed JSON; client-side 2 cases assert `onconfig` fires when `data.config` is non-null and does NOT fire when `data.config === null` (covers `MetaAgentChat.svelte:76-78`). Pre-flight grep confirmed handler already wraps `JSON.parse` in try/catch — plan's escape-hatch (test.fixme + UN-BLOCKER for handler hardening) was NOT triggered, primary path landed clean. 1 atomic commit `a3b6ad2` (Disposition: TEST-ADD (coverage)). 3 Rule-1 deviations auto-fixed all within plan scope (component-test driver iteration): (a) ChatInput textarea has `role="combobox"` not `"textbox"` — switched to `container.querySelector(".chat-textarea")`; (b) missing `/api/models/capabilities` stub crashed ChatInput's `$derived(... && capabilities.kinds.length > 1)` — added full quartet stub (`/api/models` + `/api/models/capabilities` + `/api/modes` + `/api/agent-configs/generate`) + `__resetCapabilityCacheForTests` import from `$lib/chat/attachment-client` in beforeEach to flush module-scope promise cache; (c) substring-prefix swallow (`/api/models` matched `/api/models/capabilities` first in if-ladder) — re-ordered if-ladder most-specific-first with explicit invariant comment. Zero SUT touches (`git diff main -- web/src/lib/components/MetaAgentChat.svelte web/src/routes/api/agent-configs/generate/+server.ts` both empty). Sacred-12-stash invariant preserved (12 → 12 → 12). Explicit-path `git add web/src/__tests__/api-agent-configs-generate.server.test.ts web/src/__tests__/MetaAgentChat.component.test.ts` discipline — zero touches to parallel-session dirty files (`.planning/ROADMAP.md`, `.planning/STATE.md`, `web/src/lib/hljs-theme.css`). Phase 6 sub-plan 06-04 deliverable coverage closed for the MetaAgentChat surface (4 of 5 Phase 6 coverage gaps now addressed across Phase 62: 62-05 ConversationSettings + 62-07 layout-agents-nav 06-01 + 62-09 migrate-phase6-schema + 62-06 MetaAgentChat). Decisions: (1) full-quartet fetch stub (3 routes won't do — ChatInput's capability $effect needs the 4th); (2) `aria-label="Send message"` button-selector for ChatInput's Send button + wait-for-not-disabled gate before `fireEvent.keyDown({key:"Enter"})` because ChatInput.svelte:592 returns early when `selectedModel` is null; (3) wait on fetch-spy seeing `/api/agent-configs/generate` (not a DOM signal) for the negative case because `data.config=null` produces no observable post-fetch DOM change. Pattern established: component-test driver for any ChatInput-based component test (reusable for future MetaAgentChat / ChatInput-derived coverage plans). Plan 62-09 prior status: 182-line bun:test added at `src/__tests__/migrate-phase6-schema.test.ts` covering Phase 6 sub-plan 06-04 deliverable — INFORMATION_SCHEMA + pg_catalog FK introspection on `agent_configs.category` (text, NULLABLE, no default) + `conversations.agent_config_id` (text, NULLABLE, FK to agent_configs(id) `ON DELETE SET NULL`) + behavioral DELETE-parent probe (insert project → agent_config → conversation; DELETE agent_config; assert child row's agent_config_id is NULL not cascaded). 3 cases pass / 0 fail / 15 expect() calls on `bun test src/__tests__/migrate-phase6-schema.test.ts` (2.52s initial, 5.05s re-run, zero flake). Pattern mirrors precedent `src/__tests__/db-migrate-idempotent.test.ts` (PGlite + drizzle + sql) — that precedent file still 3/3 passing post-add (Layer 2 clean, zero adjacent-test regression). Existing migrate-idempotent suite only fingerprints schema equality across repeated calls — wouldn't catch a flipped FK delete_rule. New suite pins both column shapes + FK action via TWO probe modes: schema-shape introspection AND live behavioral DELETE. 1 atomic commit `7ed5a41` (Disposition: TEST-ADD (coverage)). Zero true deviations; 1 plan-prescribed adjustment within plan's explicit caveat (added `projects` row INSERT in test 3 to satisfy conversations.project_id NOT NULL FK — plan's example INSERT omitted it; plan's Task 1 § Caveat explicitly anticipated this with "if it fails with 'null value in column X violates not-null constraint', add column X to the INSERT" — followed preemptively via schema.ts:7-15 read, so first run passed without iteration). Zero SUT touches (`git diff main -- src/db/schema.ts src/db/migrate.ts` empty). Sacred-12-stash invariant preserved (12 → 12 → 12). Explicit-path `git add src/__tests__/migrate-phase6-schema.test.ts` discipline — zero touches to parallel-session dirty files (`web/src/lib/hljs-theme.css`, 3 untracked web test files from parallel agents). Phase 6 sub-plan 06-04 deliverable coverage closed (3 of 5 Phase 6 coverage gaps now addressed: 62-05 ConversationSettings 06-04 branch + 62-07 layout-agents-nav 06-01 + 62-09 migrate-phase6-schema 06-04 columns). Decisions: 3-case split (schema-shape × 2 + behavioral × 1) chosen over single fingerprint because behavioral probe catches FK-action flips that pure introspection might miss if pg_catalog serialization is ambiguous; pre-delete sanity assertion in test 3 makes regression mode unambiguous (pre/post comparison isolates DELETE-action correctness from INSERT-success); Disposition trailer `TEST-ADD (coverage)` — distinct from REPAIR (test-layer) and FIX (product) used elsewhere in Phase 62. Pattern established: behavioral FK probe alongside schema-shape probe — both needed because pg_catalog text serialization could match while runtime DELETE action differs. Plan 62-05 prior status: 119-line vitest component-test added at `web/src/__tests__/ConversationSettings.component.test.ts` covering Phase 6 sub-plan 06-04 deliverable — the `{#if conversation.agentConfigId}` branch on `ConversationSettings.svelte:103-131`. 4 cases pass on `cd web && bunx vitest run` (0.95s): (1) agent conversation renders read-only panel + 'Managed by agent persona' notice + system prompt content; (2) agent conversation with null systemPrompt renders '(none)' fallback; (3) regular conversation (agentConfigId=null) renders editable textarea + Save button (regression guard); (4) regular conversation does NOT show 'Managed by agent persona' notice (regression guard against branch leak). Uses @testing-library/svelte ^5.3.1 (verified pre-flight; primary render-based path; plan's source-read fallback unused) + `vi.mock("$lib/api.js")` with importOriginal-spread to neutralize `loadPromptPreview` $effect without coupling to its internals. 1 atomic commit `e6e3647` (Disposition: TEST-ADD (coverage)). Zero deviations. Zero SUT touches (`git diff main -- web/src/lib/components/ConversationSettings.svelte` empty). Sacred-12-stash invariant preserved (12 → 12 → 12). Explicit-path `git add web/src/__tests__/ConversationSettings.component.test.ts` discipline — zero touches to parallel-session dirty files (`web/src/lib/hljs-theme.css`, `web/src/__tests__/api-agent-configs-generate.server.test.ts`, `web/src/__tests__/MetaAgentChat.component.test.ts` untracked from parallel 62-06). 1 issue logged but out-of-scope: 2 failures in `MetaAgentChat.component.test.ts` during Layer 2 full-suite run — file is untracked (`?? `) parallel-session artifact, NOT my modification, NOT caused by my changes. Phase 6 sub-plan 06-04 deliverable coverage closed (2 of 5 Phase 6 coverage gaps now addressed by Phase 62, after 62-07's 06-01 closure). Decision: container.querySelector('#conv-prompt') instead of getByLabelText (id is dual-purpose), getByRole('button', { name: /save/i }) regex accommodates Saving... state. Plan 62-07 prior status: 53-line source-read bun:test added at `web/src/__tests__/app-layout-agents-nav.test.ts` pinning the Agents nav link in BOTH navLinks branches of `(app)/+layout.svelte` (global-project branch: `group="Build"` at L190; per-project branch: `group="Platform"` at L202) + defensive exactly-2-occurrence regex guard. 3/3 cases pass on `cd web && bun test src/__tests__/app-layout-agents-nav.test.ts` (15ms). Pattern mirrors Phase 49.1 precedent `layout-mobile-breakpoint.test.ts` exactly (same file, same `readFileSync` + `import.meta.url` + literal/regex assertions); Phase 49.1 precedent file still 9/9 passing (zero regression in adjacent source-read test). 1 atomic commit `5048b32` (Disposition: TEST-ADD (coverage)) — first net-new TEST-ADD trailer in Phase 62, establishing a third trailer disposition distinct from REPAIR (test-layer) and FIX (product). Zero deviations. Zero SUT touches (`git diff main -- '(app)/+layout.svelte'` empty). Sacred-12-stash invariant preserved (12 → 12 → 12). Explicit-path `git add web/src/__tests__/app-layout-agents-nav.test.ts` discipline — zero touches to parallel-session dirty file `web/src/lib/hljs-theme.css`. Phase 6 sub-plan 06-01 deliverable coverage closed (1 of 5 Phase 6 coverage gaps addressed by Phase 62). Plan 62-04 prior status: agent-detail.spec.ts:23 (Cluster B :23 not-found) disposed via 1 atomic commit `798daa8` (Disposition: FIX (product); Debug: .planning/debug/agent-detail-breadcrumb-strict-mode.md) — 4-line `{:else}` branch added to `/agents/[name]/+page.svelte:436` (rounded panel with `Agent "{agentName}" not found.` message). Phase 62's ONLY product-code change; all other plans are test-side. Layer 1: 22/24 passing on agent-detail.spec.ts (was 20/24 after 62-02; net +2 from :23 closing on both viewports). 2 remaining failures = :178 Chat/Test buttons strict-mode collision (pre-existing out-of-scope, NOT 62-04 regressions). :23 stability re-run zero flake. Cross-spec safety: 34/34 PASS on agents-list + agent-chat. Layer 5 clean: zero touches to playwright.config, api-mocks, agent-detail.spec.ts (this plan must NOT touch the spec — 62-02 already did). 1 deviation auto-fixed (Rule-3 blocking): long-running `bun run preview` server (PID 1563110, started 11:23) was serving baked production bundle from BEFORE the edit; `webServer.reuseExistingServer: !process.env.CI` in playwright.config.ts:36 reused stale server. Killed PIDs, ran `PI_SKIP_INIT=1 bun run build`, restarted preview → :23 now PASSES on both viewports (214ms chromium + 265ms mobile-chromium). Plan correctly anticipated this in Task 2 § Pre-flight; deviation is plan's own escape-hatch firing. Pattern confirmed: Phase 61-02 § Rule 3 (Preview server rebuild + restart for SUT testid changes) applies to ALL Svelte template-body edits, not just testid additions. Sacred-12-stash invariant preserved (12 → 12 → 12). Zero `git stash` operations. Zero touches to parallel-session dirty file `web/src/lib/hljs-theme.css` via explicit-path `git add 'web/src/routes/(app)/agents/[name]/+page.svelte'` discipline. TEST-02 Cluster B fully closed (:23 + 5 selector failures from 62-02 = 6/6 agent-detail regressions disposed). Plan 62-02 prior status: agent-detail.spec.ts (Cluster B spec) disposed via 1 atomic commit `45dba79` (Disposition: REPAIR (test-layer); Debug: .planning/debug/agent-detail-breadcrumb-strict-mode.md). 5 selector edits + 1 fixture projectId addition closed 5 of 6 target failures; :23 not-found held for 62-04 product fix. Layer 1: 12 → 20 passing on agent-detail.spec.ts (chromium + mobile-chromium); 4 remaining failures = 2 intentional (:23 both viewports) + 2 pre-existing out-of-scope (:178 Chat/Test buttons strict-mode collision, mobile flaky). Layer 2-5 clean. 3 deviations auto-fixed (2 Rule-1 bug, 1 Rule-3 blocking): (a) getByLabel('JSON Input') returns 0 inside scope due to duplicate id=json-input across mobile+desktop snippet copies — substituted getByRole('textbox') + getByText label-visibility check; (b) plan's `.md\\:block` scope is desktop-only — widened to `.md\\:block:visible, details.md\\:hidden:visible` so both viewports pass with the same locator; (c) makeRun fixtures lacked projectId='global' — runs were silently filtered out by SUT's r.projectId === store.activeProjectId check, so Run History block never rendered. Sacred-12-stash invariant preserved (12 → 12). Zero touches to parallel-session dirty file `web/src/lib/hljs-theme.css` via explicit-path `git add web/e2e/agent-detail.spec.ts` discipline. Pattern established: viewport-agnostic visible-scope locator for Tailwind responsive dual-render specs (`page.locator('SCOPE:visible, ALT_SCOPE:visible').filter({hasText: ANCHOR})`). TEST-02 cluster-B closed; cluster-A still pending (62-04 product fix for :23). Plan 62-03 prior status: agent-chat.spec.ts:56 (Cluster C) disposed via 1 atomic commit `6aa729f` (Disposition: REPAIR (test-layer); Debug: .planning/debug/agent-chat-mobile-subtitle.md). 11-line viewport-aware drawer-open helper + 2 assertion-line scope refinement: `const isMobile = (page.viewportSize()?.width ?? 0) < 768`; on mobile, tap "Open conversations" hamburger AND scope `getByText("Agent conversation"/"Regular Chat")` to `page.getByTestId("swipe-drawer")` (to dodge strict-mode collision when BOTH desktop CSS-hidden + mobile drawer-mounted ConversationLists are in DOM). Edit 2 (preventive :85 guard) REVERTED per plan's explicit revert-on-regression clause — SwipeDrawer backdrop intercepts pointer events on ChatHeader settings button. Layer 1: 12/12 cases pass on chromium + mobile-chromium, 2 consecutive runs (8.6s + 8.4s) zero flake. Layer 2: 5 baseline-passing entries preserved + 1 new (the previously-failing mobile :56 case). Layer 3: Disposition trailer + debug-doc citation. Layer 4: zero test.fixme adds (pure REPAIR). Layer 5: zero SUT touches (playwright.config, api-mocks, SwipeDrawer.svelte, ConversationList.svelte all clean vs main); no `.first()`/`.last()`; no timeout widening; no class-based locators. 2 Rule-1 deviations auto-fixed (scope-refine to avoid strict-mode collision + Edit-2 revert per plan clause). Sacred-12-stash invariant preserved (12 → 12 → 12). Zero `git stash` operations. Zero touches to parallel-session dirty files (`web/e2e/agent-detail.spec.ts` from 62-02, `web/src/lib/hljs-theme.css`) — explicit-path `git add web/e2e/agent-chat.spec.ts` discipline. Phase 62 Wave 1 progress: 2 of 3 plans landed (62-01 + 62-03; 62-02 pending).
Last activity: 2026-05-13

Progress: [██████████] v1.4 99% Phase 62 (per-plan; phases 54 + 55 + 56 complete + 57 in flight + 58 complete + 59-01/02/03/04/05/06 landed + 60 COMPLETE + 61 COMPLETE + 62 COMPLETE)

</details>

## Accumulated Context

<!-- v1.4 plan-outcome record preserved below as carry-forward knowledge for future phases. -->

### Plan 62-01 outcome
- 3 surgical selector edits in `web/e2e/agents-list.spec.ts` aligning Phase 6 tests with Phase 49.2 UX (always-on ownership-filter chip row + `<EmptyState>` empty-state copy)
- L17: `getByText("No agents available.")` → `getByText("No agents configured")` (EmptyState title prop at `+page.svelte:328`)
- L30: `getByRole("button", { name: "All" }).not.toBeVisible()` → `getByRole("button", { name: "All categories" }).not.toBeVisible()` (re-target no-chip assertion to category clear-button since ownership "All" chip is unconditional)
- L83: `const allBtn = page.getByRole("button", { name: "All" })` → `getByRole("button", { name: "All categories" })` (category clear-button — needed for L107's `allBtn.click()` to-reset-filter usage; plan's `{ exact: true }` prescription was wrong, re-targeted per debug-doc §57 alternative)
- 1 atomic commit: `e30b79b` (Disposition: REPAIR (test-layer); Debug: .planning/debug/agents-list-chips-and-empty-state.md)
- Layer 1: 8 → 11 cases pass per project, both runs identical (22/22 across chromium + mobile-chromium, zero flake)
- Layer 2: 16/16 baseline-passing entries preserved, +6 new entries (3 cases × 2 projects)
- Layer 3 + 4 + 5 clean (disposition + debug citation, zero FIXME adds, zero widening)
- Sacred-12-stash invariant preserved throughout (12 → 12 → 12; zero `git stash` operations)
- 1 Rule-1 deviation auto-fixed: plan's Edit-3 semantic misread (ownership-chip click was prescribed, but L107 requires category clear-button click to reset Engineering filter)

### Plan 62-02 outcome
- 5 surgical selector edits + 1 fixture `projectId` addition in `web/e2e/agent-detail.spec.ts` aligning tests with Phase 6 sub-plan 06-04's responsive rewrite of `/agents/[name]` (Breadcrumb `md:hidden`, dual mobile/desktop `runAgentContent()` snippet render at +page.svelte:397-410, dual mobile <summary> + desktop <h3> Run History at :412-434, back link retargeted `/` → `/agents`)
- Edits 1+2 (L36-39, L47-52): scope JSON Input + Run to viewport-visible run-agent wrapper via `page.locator(".md\\:block:visible, details.md\\:hidden:visible").filter({hasText:"Run Agent"})`; assertions use `getByText("JSON Input") + getByRole("textbox") + getByRole("button", {name:"Run"})`. Edit 3 (L65-72): Run History via same visible-scope pattern + added `projectId:"global"` to both `makeRun()` fixtures (SUT filter at +page.svelte:94 excludes runs with mismatched projectId). Edit 4 (L81-83): toHaveURL `/` → `/agents` + viewport-agnostic visible-scope `a[href='/agents']` locator (desktop "Back to Agents" / mobile breadcrumb "Agents" — both target /agents). Edit 5 (L152, L173): `getByText(name)` → `getByRole("heading", {name})` for file-agent + shared-agent (breadcrumb `<span>` collides on mobile; heading role excludes <span>)
- 1 atomic commit: `45dba79` (Disposition: REPAIR (test-layer); Debug: .planning/debug/agent-detail-breadcrumb-strict-mode.md)
- Layer 1: 12 → 20 cases pass on agent-detail.spec.ts across chromium + mobile-chromium (3 stability runs: 20/4, 21/3 (flake), 20/4 — stable steady-state 20 passing). 4 remaining failures: 2 intentional (:23 not-found both viewports — held for 62-04 product `{:else}` fix) + 2 pre-existing out-of-scope (:178 Chat/Test buttons strict-mode collision on `getByRole("button", {name:"Chat"})` matching both `agent-chat-cta` AND aria-label="Remove Current Chat Model"; mobile flaky on this case)
- Layer 2: all 12 baseline-passing entries preserved post-fix (verified against `.planning/phases/61-test-debt-followup-feature-rework-specs/baseline-passing.txt` lines 130-141)
- Layer 3 + 5 clean: Disposition trailer + debug citation present; `git diff main -- web/playwright.config.ts web/e2e/fixtures/api-mocks.ts web/src/` all empty (zero SUT changes, zero playwright config touched, zero api-mocks edits)
- Sacred-12-stash invariant preserved (12 → 12 → 12); zero `git stash` operations
- Explicit-path `git add web/e2e/agent-detail.spec.ts` discipline — zero touches to parallel-session dirty file `web/src/lib/hljs-theme.css`
- 3 deviations auto-fixed (2 Rule-1 bug, 1 Rule-3 blocking): (1) `getByLabel("JSON Input")` returns 0 inside `.md\:block` scope because duplicate `id="json-input"` (both mobile <details> + desktop <div> render same `<label for="json-input"> + <textarea id="json-input">`) defeats Playwright accessibility-tree scoping — substituted `getByRole("textbox")` + `getByText("JSON Input")` for label-text visibility; (2) plan's `.md\:block` scope is desktop-only — widened to `.md\:block:visible, details.md\:hidden:visible` so both viewports pass with the same locator; (3) added `projectId:"global"` to `makeRun()` fixtures so runs pass SUT's `agentRuns` filter (default `store.activeProjectId === "global"`, fixture default `projectId: undefined`) — pre-fix runs were silently excluded so Run History block never rendered regardless of selector
- Pattern established: viewport-agnostic visible-scope locator (`page.locator("SCOPE_CLASS:visible, ALT_SCOPE:visible").filter({hasText: ANCHOR})`) for Tailwind responsive dual-render specs — picks the visible wrapper at any viewport, avoids `.first()` and class-only primary selectors
- Anti-pattern caution documented in SUMMARY: `getByLabel(NAME)` is NOT scope-honoring for `<label for=ID>` references when duplicate ID exists in DOM — use `getByRole("textbox")` or `locator("textarea")` for input access inside a scoped wrapper
- TEST-02 cluster-B closed (5 of 6 agent-detail failures repaired); cluster-A still pending (62-04 product `{:else}` fix for :23)

### Plan 62-03 outcome
- 1 file modified — `web/e2e/agent-chat.spec.ts` (13 insertions / 2 deletions): 11-line viewport-aware drawer-open helper block before :56 assertion + 2-line assertion scope refinement (bare `page.getByText(...)` → `sidebar.getByText(...)` where `sidebar = isMobile ? page.getByTestId("swipe-drawer") : page`)
- 1 atomic commit: `6aa729f` (Disposition: REPAIR (test-layer); Debug: .planning/debug/agent-chat-mobile-subtitle.md)
- Layer 1: 12/12 cases pass on chromium + mobile-chromium across 2 consecutive runs (8.6s + 8.4s; zero flake)
- Layer 2: 5 baseline-passing entries preserved + 1 new (was-failing mobile :56 case now green)
- Layer 5 invariants clean: `git diff main -- web/playwright.config.ts web/e2e/fixtures/api-mocks.ts web/src/lib/components/SwipeDrawer.svelte web/src/lib/components/ConversationList.svelte` all empty
- 2 Rule-1 deviations auto-fixed: (1) refined plan's Edit 1 with visible-container locator-scope to dodge strict-mode collision (BOTH desktop CSS-hidden + mobile drawer-mounted ConversationLists in DOM after drawer-open); (2) reverted plan's Edit 2 (:85 preventive drawer-open guard) per plan's explicit revert-on-regression clause — SwipeDrawer backdrop intercepts pointer events on ChatHeader 'Conversation settings' button
- Sacred-12-stash invariant preserved (12 → 12 → 12); zero `git stash` operations
- Explicit-path `git add web/e2e/agent-chat.spec.ts` discipline — zero touches to parallel-session dirty files (`web/e2e/agent-detail.spec.ts` from 62-02; `web/src/lib/hljs-theme.css`)
- Pattern established: responsive-spec locator-scope (`page` on desktop, `page.getByTestId("swipe-drawer")` on mobile) for cluster-C-style repairs where both responsive branches mount simultaneously
- TEST-02 partial closure (cluster-C closeout); 62-02 still pending for full TEST-02

### Plan 62-04 outcome
- 1 file modified — `web/src/routes/(app)/agents/[name]/+page.svelte` (+4 lines): `{:else}` branch added to `{#if agent}` at line 436, renders rounded panel with `Agent "{agentName}" not found.` message
- 1 atomic commit: `798daa8` (Disposition: FIX (product); Debug: .planning/debug/agent-detail-breadcrumb-strict-mode.md)
- Phase 62's ONLY product-code change — all other plans (62-01/02/03/05–09) are test-side
- Pre-existing product gap predating Phase 6 (verified against initial commit `1e30079` in the debug doc) and NOT a Phase 6 regression — test at agent-detail.spec.ts:23 was always asserting against a UI that never existed
- Layer 1: 22/24 passing on agent-detail.spec.ts across chromium + mobile-chromium (was 20/24 after 62-02; :23 NOW PASSES on both viewports — net +2). 2 remaining failures = pre-existing out-of-scope :178 Chat/Test buttons strict-mode collision (NOT 62-04 regressions; deferred for future test-layer plan)
- Layer 1 stability: `bunx playwright test -g "not found" --project=chromium --project=mobile-chromium` → 2/2 PASS in 2.1s zero flake
- Layer 2 baseline preservation: all 11 baseline-passing entries still pass + 2 new :23 passes; net +2 cases
- Cross-spec safety: `bunx playwright test agents-list.spec.ts agent-chat.spec.ts --project=chromium --project=mobile-chromium` → 34/34 PASS
- Layer 3: Disposition trailer `FIX (product)` (distinct from REPAIR (test-layer) trailers on the 8 test-side commits in Phase 62) + debug-doc citation
- Layer 5 invariants clean: `git diff main -- web/playwright.config.ts web/e2e/fixtures/api-mocks.ts web/e2e/agent-detail.spec.ts` all empty (this plan must NOT touch the spec — 62-02 already did); `+page.svelte` diff shows ONLY the 4-line `{:else}` block addition
- 1 deviation auto-fixed (Rule-3 blocking): long-running `bun run preview` server (PID 1563110, started 11:23) was serving baked production bundle from BEFORE the edit; `webServer.reuseExistingServer: !process.env.CI` in playwright.config.ts:36 reused stale server. Killed PIDs 1563110/1563113/1563114, ran `PI_SKIP_INIT=1 bun run build`, started fresh preview via `PI_SKIP_INIT=1 nohup bun run preview` (PID 2487584) → :23 now PASSES on both viewports (214ms chromium + 265ms mobile-chromium). Plan correctly anticipated this in Task 2 § Pre-flight; deviation is plan's own escape-hatch firing
- Sacred-12-stash invariant preserved (12 → 12 → 12); zero `git stash` operations
- Explicit-path `git add 'web/src/routes/(app)/agents/[name]/+page.svelte'` discipline — zero touches to parallel-session dirty file `web/src/lib/hljs-theme.css`
- Decision-1: Used `agentName` (route param `$derived(page.params.name)`) instead of `agent.name` — `agent` is `undefined` in the `{:else}` branch by definition; route param is always defined and matches user expectation ("the URL you typed")
- Decision-2: Card panel wrapper using `rounded-lg border border-[var(--color-border)] bg-[var(--color-surface-secondary)] p-6` — matches existing Run Agent / Test Conversations card style for visual consistency
- Decision-3: NO `data-testid` added — existing test regex `/not found/i` matches copy directly; data-testid would be over-engineering
- Decision-4: NO touch to `Breadcrumb` or back-link — breadcrumb still renders `Agents > nonexistent` outside the conditional, matching UX expectation
- Pattern established: Preview-server-restart-on-SUT-edit — Phase 61-02 § Rule 3 ("Preview server rebuild + restart for SUT testid changes") applies to ALL Svelte template-body edits, not just testid additions, because Vite preview serves a baked production bundle from `build/`, not live source
- Pattern established: Dual-trailer convention for Phase 62 — `Disposition: FIX (product)` for the lone product change, `Disposition: REPAIR (test-layer)` for the 8 test-side commits; both cite debug docs
- TEST-02 Cluster B fully closed (:23 + 5 selector failures from 62-02 = 6/6 agent-detail regressions disposed)
- Phase 62 Wave 2 progress: 1 of 1 product-fix plans landed (62-04 standalone; depends on 62-02 for files-modified-overlap protection on agent-detail.spec.ts which 62-04 does NOT touch)

### Plan 62-05 outcome
- 1 file created — `web/src/__tests__/ConversationSettings.component.test.ts` (119 lines; within plan's <120-line `done` budget after one trim pass): vitest component-test with `@testing-library/svelte` ^5.3.1 + `vi.mock("$lib/api.js")` shim
- 4 cases all PASS on `cd web && bunx vitest run src/__tests__/ConversationSettings.component.test.ts` (944ms): (1) agent conversation renders read-only panel + 'Managed by agent persona' notice + system prompt content; (2) agent conversation with null systemPrompt renders '(none)' fallback; (3) regular conversation (agentConfigId=null) renders editable textarea (#conv-prompt) + Save button (regression guard); (4) regular conversation does NOT show 'Managed by agent persona' notice (regression guard)
- Covers Phase 6 sub-plan 06-04 deliverable: ConversationSettings.svelte's `{#if conversation.agentConfigId}` branch at lines 103-131 (previously uncovered; `conversation-settings-logic.test.ts` only covers prompt-level prioritization, NOT the agentConfigId-conditional render branch)
- 1 atomic commit: `e6e3647` (Disposition: TEST-ADD (coverage))
- Layer 5 invariants clean: `git diff main -- web/src/lib/components/ConversationSettings.svelte` empty (zero SUT touches)
- Layer 2 full vitest suite: 1988 passing of 1990 (2 failures in `MetaAgentChat.component.test.ts`); investigation confirmed file is **untracked** working-tree artifact (`?? `) from parallel-session 62-06, NOT my modification, NOT caused by my changes — out of scope per scope-boundary rules
- 0 deviations (Rules 1-4) — plan executed exactly as written; one minor self-consistency resolution between plan's example-code line count (133) and `done` criterion (<120) resolved by following the stricter `done` criterion (trim doc-comment header from 17 lines to 12, inline `Record<string, unknown>` parameter type instead of explicit `ConvOverrides` alias)
- Sacred-12-stash invariant preserved (12 to 12 to 12); zero `git stash` operations
- Explicit-path `git add web/src/__tests__/ConversationSettings.component.test.ts` discipline — zero touches to parallel-session dirty files (`web/src/lib/hljs-theme.css`, `web/src/__tests__/api-agent-configs-generate.server.test.ts`, `web/src/__tests__/MetaAgentChat.component.test.ts` untracked from parallel 62-06)
- Decision-1: render-based @testing-library/svelte path (verified ^5.3.1 in web/package.json; plan's source-read fallback unused)
- Decision-2: `vi.mock("$lib/api.js")` with importOriginal-spread to preserve `Conversation` type export while neutralizing `fetchSettings`/`upsertSetting` so `loadPromptPreview` $effect resolves deterministically on mount
- Decision-3: `container.querySelector("#conv-prompt")` for textarea presence/absence (textarea id is dual-purpose: label's `for=` references it + plan's spec)
- Decision-4: `getByRole("button", { name: /save/i })` regex accommodates dual-text state (`Save` vs `Saving...`)
- Decision-5: NO SwipeDrawer mock per plan's explicit DO-NOT — SwipeDrawer mounts children inside `{#if visible}` which falls through to `open=true` prop; jsdom mounts naturally
- Pattern established: Component-test stub-via-vi.mock for unrelated $effect side-effects — when an $effect fires on mount but is orthogonal to the SUT branch under test, mock its imports rather than asserting on its behavior
- Pattern established: Branch-coverage pair — positive case (agent-scoped rendered) + regression-guard negative case (non-agent rendered) on the same component (Svelte 5's `{#if}/{:else}` blocks easy to leak across branches during refactor)
- TEST-02 Phase 6 deliverable coverage: 62-05 closes the ConversationSettings agent-scoped read-only mode gap (1 of 5 Phase 6 coverage gaps closed by this plan alone)

### Plan 62-07 outcome
- 1 file created — `web/src/__tests__/app-layout-agents-nav.test.ts` (53 lines): 21-line block-comment header (Phase-6-deliverable rationale + Phase 49.1 source-read pattern justification + regression intent), followed by `describe`/3-`test` block asserting on the layout-source string
- 3 cases pin the Agents nav link policy: (1) global-project branch regex matches `{ href: "/agents", label: "Agents", group: "Build" }` (line 190 in `+layout.svelte`); (2) per-project branch regex matches `{ href: "/agents", label: "Agents", group: "Platform" }` (line 202); (3) defensive occurrence-count exactly-2 match of `href: "/agents", label: "Agents"` — catches accidental triple-include regressions
- 1 atomic commit `5048b32` (Disposition: TEST-ADD (coverage)) — first net-new TEST-ADD trailer in Phase 62
- Layer 1: `cd web && bun test src/__tests__/app-layout-agents-nav.test.ts` → 3 pass, 0 fail, 3 expect() calls, 15ms
- Layer 2: `cd web && bun test src/__tests__/layout-mobile-breakpoint.test.ts` (precedent sanity check) → 9 pass, 0 fail, 13 expect() calls, 14ms (zero regression in adjacent source-read test)
- Layer 3: `Disposition: TEST-ADD (coverage)` trailer present in `git log -1 --pretty=%B`
- Layer 5 invariants clean: `git diff main -- 'web/src/routes/(app)/+layout.svelte'` → empty (zero SUT touches confirmed)
- Must-have artifact line count: `wc -l web/src/__tests__/app-layout-agents-nav.test.ts` → 53 lines (meets plan's min_lines: 40 contract)
- Zero deviations from plan — executed exactly as written; pre-flight stash check (12) held throughout
- Sacred-12-stash invariant preserved (12 → 12 → 12); zero `git stash` operations
- Explicit-path `git add web/src/__tests__/app-layout-agents-nav.test.ts` discipline — zero touches to parallel-session dirty file `web/src/lib/hljs-theme.css`
- Decision-1: Used `bun:test` (not vitest) — mirrors Phase 49.1 precedent file `layout-mobile-breakpoint.test.ts` in the same `web/src/__tests__/` directory; bun:test is appropriate for pure source-string assertions where no Svelte compiler/jsdom/reactive-runtime is needed
- Decision-2: Used regex `.toMatch()` (not literal `.toContain()`) — allows whitespace flexibility between object-literal fields, robust to future indentation changes inside `(app)/+layout.svelte` without weakening the three-key (href + label + group) policy
- Decision-3: Added defensive occurrence-count case (third test) — catches regression class the per-branch matchers can't: an accidental triple-include where someone copies the Agents link into the trailing spread or admin branch
- Decision-4: Zero `data-testid` adds; zero SUT modifications — pure coverage add; Layer 5 invariant trivially holds
- Pattern established: TEST-ADD disposition trailer convention — Phase 62 establishes a third trailer disposition alongside REPAIR (test-layer) and FIX (product); TEST-ADD signals net-new coverage with no behavior change, distinguishing coverage-additions from spec-fixes
- Pattern reinforced: `web/src/__tests__/*.test.ts` can use either `bun:test` (source-read / pure-logic) or `vitest` (component / server / jsdom-required) — choice driven by what the test actually exercises
- TEST-02 incremental coverage closure: Phase 6 sub-plan 06-01 deliverable (Agents nav link) now has bun:test source-read coverage; 1 of 5 Phase 6 coverage gaps closed
- Phase 62 Wave 3 progress: 1 of 5 TEST-ADD plans landed (62-07 standalone; depends_on: [])

### Plan 62-08 outcome
- 2 files modified — `web/src/__tests__/api-agents.server.test.ts` (+43 lines: 1 new vitest case `multi-agent listing preserves per-row category including null` appended inside existing `describe("GET /api/agents")` block) + `web/src/__tests__/api-conversations.server.test.ts` (+39 lines: 1 new vitest case `GET listing echoes agentConfigId field for agent-conversations` appended inside existing `describe("GET /api/conversations")` block)
- 1 atomic commit: `d00dba1` (Disposition: TEST-ADD (coverage))
- Layer 1: 17 → 19 cases pass across both server-test files (`cd web && bunx vitest run src/__tests__/api-agents.server.test.ts src/__tests__/api-conversations.server.test.ts` → `Test Files 2 passed (2) / Tests 19 passed (19)` in 1.28s; net +2 cases)
- Layer 5 invariants clean: `git diff main -- web/src/routes/api/agents/+server.ts web/src/routes/api/conversations/+server.ts web/src/lib/api.ts | wc -l` → 0 (zero SUT lines touched)
- Closes 1 of 5 Phase 6 sub-plan 06-04 api.ts type-extension coverage gaps: `category` (Agent type, api.ts:47) and `agentConfigId` (Conversation type, api.ts:329) now have explicit round-trip assertions — guards against silent field-dropping in either GET serializer when an agent persona owns the conversation
- Multi-row category mock asserts 3 rows with categories `"Productivity" / null / "Engineering"` via by-name lookup map (`Object.fromEntries(body.map((a) => [a.name, a.category]))`) — order-stable against future listing-sort changes in the handler
- GET conversations mock asserts both `agentConfigId: "cfg-1"` (present-value) and `agentConfigId: null` (null-branch) in one test case — mirrors the api-agents multi-row pattern; covers both branches of the nullable in a single test
- Decision-1: By-name lookup map for multi-row assertions instead of `body[0/1/2]` index-based — order-stable against future handler sort changes
- Decision-2: Both present-value AND null branches asserted in one conversations test — single test covers both branches of `agentConfigId` nullable, avoiding two separate near-duplicate tests
- Decision-3: Reused existing `makeEvent` + `PROJECT_ID` fixtures + `now = new Date()` shared between mock rows — DRY with the 13 existing cases; new case slots cleanly into existing GET describe block without new helpers
- Decision-4: Indentation matched per-file (api-agents uses 2-space, api-conversations uses tabs) — each new case adopts the file's existing convention
- Zero deviations — plan provided two literal test-case templates, both compiled and passed on first run. No auto-fixes needed.
- Sacred-12-stash invariant preserved (12 → 12 → 12); zero `git stash` operations
- Explicit-path `git add web/src/__tests__/api-agents.server.test.ts web/src/__tests__/api-conversations.server.test.ts` discipline — zero touches to parallel-session dirty files (`web/src/lib/hljs-theme.css`, `web/src/__tests__/api-agent-configs-generate.server.test.ts` from 62-06 in-flight, plus untracked `ConversationSettings.component.test.ts` + `MetaAgentChat.component.test.ts` + `migrate-phase6-schema.test.ts` from 62-05/06/09 in-flight)
- Pattern established: Round-trip coverage for nullable serializer fields — assert BOTH present-value branch AND null-branch in one test case (multi-row mock + per-row find). Prevents the common bug where a nullable field gets coalesced or stripped only on null inputs.
- Pattern established: Disposition: TEST-ADD (coverage) trailer convention for Phase 62 pure-coverage commits (no SUT change, no spec repair, only new test cases). Third trailer disposition in Phase 62 alongside REPAIR (test-layer) and FIX (product).
- Phase 6 sub-plan 06-04 api.ts type-extension coverage gap closed (1 of 5 Phase 6 coverage gaps addressed by Phase 62; pairs with 62-07's 06-01 nav-link coverage + 62-09's 06-04 schema-migration coverage)

### Plan 62-09 outcome
- 1 file created — `src/__tests__/migrate-phase6-schema.test.ts` (182 lines, 3 test cases, 15 expect() calls)
- 1 atomic commit: `7ed5a41` (Disposition: TEST-ADD (coverage))
- Covers Phase 6 sub-plan 06-04 column adds: (a) `agent_configs.category` text NULLABLE no default, (b) `conversations.agent_config_id` text NULLABLE FK to agent_configs(id) `ON DELETE SET NULL`
- 3-case structure: (1) schema-shape introspection on `category` (INFORMATION_SCHEMA.columns); (2) schema-shape + FK introspection on `agent_config_id` (INFORMATION_SCHEMA.columns + table_constraints + key_column_usage + constraint_column_usage + referential_constraints join for delete_rule extraction); (3) behavioral DELETE-parent probe (insert projects → agent_configs → conversations; DELETE agent_configs; assert child agent_config_id is NULL not cascade-deleted)
- Layer 1: 3 pass / 0 fail / 15 expect() calls on initial 2.52s run + 5.05s re-run (zero flake)
- Layer 2: precedent `src/__tests__/db-migrate-idempotent.test.ts` still 3 pass / 0 fail / 5 expect() calls (verified before AND after the new file landed; zero adjacent-test regression)
- Layer 3: Disposition trailer `TEST-ADD (coverage)` present; commit body documents the 3-case structure + why the behavioral probe complements the schema-shape introspection
- Layer 5: `git diff main -- src/db/schema.ts src/db/migrate.ts` empty (zero SUT changes)
- Sacred-12-stash invariant preserved (12 → 12 → 12); zero `git stash` operations
- Explicit-path `git add src/__tests__/migrate-phase6-schema.test.ts` discipline — zero touches to parallel-session dirty file `web/src/lib/hljs-theme.css` or 3 untracked web test files from parallel 62-05/06/08 agents
- Zero true deviations; 1 plan-prescribed adjustment (within plan's explicit caveat): added `projects` row INSERT in test 3 because `conversations.project_id` is NOT NULL with FK to `projects.id` (schema.ts:45) — plan's example INSERT omitted this and would have failed. Followed plan's Task 1 § Caveat preemptively (read schema.ts:7-15 first) rather than reactively, so first run passed without iteration cycle
- Decision-1: Three test cases (schema × 2 + behavioral × 1) instead of one combined fingerprint — behavioral probe (test 3) is the highest-value because it catches FK-action regressions (set-null → cascade) that pure schema-shape introspection might miss if pg_catalog serialization is ambiguous; plan explicitly highlighted test 3 as "the highest-value of the three"
- Decision-2: Pre-delete sanity assertion in test 3 (`expect(preRows[0].agent_config_id).toBe("cfg-x")`) makes regression mode unambiguous — pre/post comparison isolates DELETE-action correctness from INSERT-success
- Decision-3: Omitted `created_at`/`updated_at` from all INSERTs in test 3 — all timestamp columns use `defaultNow()` so they're auto-populated. INSERT-only-required-columns minimizes test brittleness if schema column-ordering changes
- Decision-4: Disposition trailer `TEST-ADD (coverage)` — distinct from `REPAIR (test-layer)` (62-01/02/03 selector repairs) and `FIX (product)` (62-04 svelte template change); reflects net-new coverage, not a repair of existing failing tests. Mirrors 62-07/62-08's TEST-ADD adoption.
- Pattern established: Behavioral FK probe alongside schema-shape probe — schema-shape pins the FK metadata, behavioral pins the runtime DELETE action; both needed because pg_catalog text serialization could theoretically match while the runtime action differs
- Pattern established: Multi-row INSERT precedence in PGlite schema tests — insert parents first (projects → agent_configs → conversations), respect every NOT NULL FK, omit defaultNow() columns from INSERT
- TEST-02 residual closure: 3 of 5 Phase 6 coverage gaps now addressed by Phase 62 (62-05 ConversationSettings 06-04 branch + 62-07 layout-agents-nav 06-01 + 62-09 migrate-phase6-schema 06-04 columns)
- Phase 62 progress: 8 of 9 plans landed; only 62-06 remains

### Plan 62-06 outcome
- 2 files touched — `web/src/__tests__/api-agent-configs-generate.server.test.ts` (+33 lines: 1 new vitest case `malformed JSON inside <agent_config>: returns config=null, no 500` appended inside existing `describe("POST /api/agent-configs/generate")` block) + `web/src/__tests__/MetaAgentChat.component.test.ts` (NEW; ~180 lines: 2 vitest cases on the `onconfig` prop wiring at MetaAgentChat.svelte:76-78)
- 1 atomic commit: `a3b6ad2` (Disposition: TEST-ADD (coverage))
- Layer 1: 7 → 8 cases pass on server-test file + 0 → 2 cases on new component-test file (`cd web && bunx vitest run src/__tests__/api-agent-configs-generate.server.test.ts src/__tests__/MetaAgentChat.component.test.ts` → `Test Files 2 passed (2) / Tests 10 passed (10)` in 1.44s; net +3 cases)
- Layer 5 invariants clean: `git diff main -- web/src/lib/components/MetaAgentChat.svelte web/src/routes/api/agent-configs/generate/+server.ts | wc -l` → 0 (zero SUT lines touched on either surface)
- Server-side: third arm of the `<agent_config>{...}</agent_config>` extractor at `+server.ts:39-50` — happy-path-with-match (line 146), no-match (line 181), match-but-parse-fails (this plan); regex hits, JSON.parse throws inside try/catch, handler returns `{text, config: null}` not 500. Plan's escape-hatch (test.fixme + UN-BLOCKER for handler hardening if try/catch missing) was NOT triggered — pre-flight grep confirmed `+server.ts:42-49` already wraps JSON.parse in try/catch.
- Client-side: 2 cases on MetaAgentChat.svelte's `if (data.config) onconfig(data.config)` branch at L76-78. Positive case asserts `onconfig` called exactly once with `{name, prompt}`; negative case asserts `onconfig` NOT called when `data.config === null` (verified by waiting on fetch-spy seeing the generate call, then asserting `mock.calls.length === 0`).
- 3 Rule-1 deviations auto-fixed during Task 2 component-test driver iteration: (a) ChatInput textarea has `role="combobox"` (mention-listbox owner) not `"textbox"` — switched to `container.querySelector(".chat-textarea")`; (b) missing `/api/models/capabilities` stub crashed `ChatInput.svelte:298`'s `$derived(... && capabilities.kinds.length > 1)` with `Cannot read properties of undefined (reading 'length')` — added full quartet stub (`/api/models` + `/api/models/capabilities` + `/api/modes` + `/api/agent-configs/generate`) + `__resetCapabilityCacheForTests` import from `$lib/chat/attachment-client` in beforeEach to flush module-scope promise cache; (c) substring-prefix swallow (`/api/models` is a strict prefix of `/api/models/capabilities`) — re-ordered if-ladder most-specific-first with explicit invariant comment locking the ordering.
- Decision-1: Full-quartet fetch stub (3 routes won't do — ChatInput's capability $effect needs the 4th, otherwise `caps.kinds.length` blows up downstream); each branch has documented purpose.
- Decision-2: `aria-label="Send message"` button-selector + wait-for-not-disabled gate before `fireEvent.keyDown({key:"Enter"})` because ChatInput.svelte:592 short-circuits `submit()` when `selectedModel` is null; polling for `button[aria-label='Send message']:not(:disabled)` via `waitFor` is robust against autoselect re-render timing.
- Decision-3: For the negative case, wait on the fetch-spy seeing `/api/agent-configs/generate` (not on a DOM signal) because `data.config=null` produces no observable post-fetch DOM change (no editor route push, no title update); asserting the fetch landed is the cleanest signal that `MetaAgentChat.svelte:76`'s `if (data.config)` branch has been evaluated.
- Decision-4: `__resetCapabilityCacheForTests` in beforeEach instead of `vi.resetModules()` — the latter resets the whole module graph and is far heavier; the explicit test seam at `attachment-client.ts:64` exists exactly for this use case.
- Sacred-12-stash invariant preserved (12 → 12 → 12); zero `git stash` operations
- Explicit-path `git add web/src/__tests__/api-agent-configs-generate.server.test.ts web/src/__tests__/MetaAgentChat.component.test.ts` discipline — zero touches to parallel-session dirty files (`.planning/ROADMAP.md`, `.planning/STATE.md`, `web/src/lib/hljs-theme.css`)
- Pattern established: Server-test branch triad for regex+JSON.parse extractors (happy-path-with-match + no-match + match-but-parse-fails) — locks the contract that parse failure is a silent fall-through to null, not a 500. Generalizable to any handler that extracts structured data from LLM output via `text.match(/<tag>(...)<\/tag>/) + JSON.parse`.
- Pattern established: Component-test fetch stub ordering for substring-overlap routes — most-specific-first matching in if-ladder predicates (capabilities BEFORE models). Generalizable to any URL hierarchy with prefix overlap.
- Pattern established: Component-test driver for ChatInput-based components — wait for `button[aria-label='Send message']:not(:disabled)` before pressing Enter + `__resetCapabilityCacheForTests` in beforeEach + full-quartet fetch stub (`/api/models` + `/api/models/capabilities` + `/api/modes` + the target API). Reusable for any future MetaAgentChat / ChatInput-derived coverage plan.
- Pattern established: For component-tests on conditional callbacks (`if (data.X) callback(data.X)`), the negative case must wait on a fetch-spy signal (not a DOM signal) because the negative branch is defined by absence of DOM change.
- Phase 6 sub-plan 06-04 deliverable coverage closed for MetaAgentChat surface (4 of 5 Phase 6 coverage gaps now addressed across Phase 62: 62-05 ConversationSettings 06-04 branch + 62-07 layout-agents-nav 06-01 + 62-09 migrate-phase6-schema 06-04 columns + 62-06 MetaAgentChat 06-04 onconfig).
- Phase 62 fully complete: 9 of 9 plans landed.

### Plan 55-01 outcome
- Per-CONNECT Bun.dns.lookup recheck against isInternalHost — every A/AAAA record validated
- Rebind→403, NXDOMAIN→502 fail-closed (PDP never consulted on DNS failure)
- EZCORP_MCP_STAGE1_DNS_RECHECK=0 kill-switch with one-time MCP_NETNS_FALLBACK boot row
- Reuses MCP_HOST_BLOCKED with metadata.reason='rebind' (taxonomy stable; SIEM filters on reason)
- src/extensions/runtime/dns.ts mock-able seam (single chokepoint for future MCP-06 TOCTOU close)
- internal-host.ts comment refresh: Phase 7 netns reference → Phase 55 / MCP-06 reality
- 29/29 mcp-proxy.test.ts cases GREEN (7 new "DNS rebind recheck" + 22 pre-existing)

### Plan 55-02 outcome
- Pattern B confirmed: outer `unshare -U -m` + inner `bwrap` with userns inherited (minimum-diff, preserves MCP_NETNS_CREATED audit semantics)
- probeBwrapAvailability() exported on mcp-netns.ts; mirrors probeNetnsAvailability shape
- BuildNetnsSpawnArgsResult extended with bwrapAvailable / bwrapReason / tmpfsKillSwitchActive (composable — Plan 03 will add seccomp fields)
- mcp-launcher.sh conditional `bwrap --proc /proc --dev /dev --bind / / --size 67108864 --tmpfs /tmp --` exec branch gated by EZCORP_MCP_BWRAP_ENABLED=1
- EZCORP_MCP_STAGE1_TMPFS=0 kill-switch with one-time MCP_NETNS_FALLBACK boot row (mirrors Plan 01 pattern)
- MCP_NETNS_FALLBACK reason='bubblewrap unavailable' per-spawn discriminator for Linux hosts without bwrap
- 12 new tests: 6 probe + 3 sandbox-wrap unit + 3 Linux+bwrap-gated integration (SKIP on dev hosts without /usr/bin/bwrap)
- 1 deviation auto-fixed (Rule 1 bug): dropped `--userns=keep-current` from launcher argv — flag absent in bwrap 0.8 (Debian bookworm production target); non-setuid bwrap inherits parent userns automatically when --unshare-user is not requested

### Plan 55-03 outcome
- Trimmed Docker v25.0.0 default seccomp profile (407 entries, all SCMP_ACT_LOG) committed at src/extensions/mcp-seccomp.json; precompiled to cBPF at Docker build time by build/compile-seccomp.c (~150-line hand-rolled JSON→cBPF C helper linking -lseccomp)
- Dockerfile: apt line gains bubblewrap + libseccomp2; new build-stage RUN compiles the C helper and produces /app/src/extensions/mcp-seccomp.bpf, then purges gcc + libseccomp-dev + libc6-dev. Image growth ~23 MB (was ~20 MB)
- MCP_SECCOMP_VIOLATION audit-action constant added; ExtensionAuditMetadata extended with optional syscall/code/pid/arch fields
- openSeccompBpfFd() loader seam (src/extensions/runtime/seccomp-loader.ts) — opens the BPF on Linux when present, returns null otherwise (silent degrade)
- parseAndEmitSeccompViolations() soak parser (src/extensions/runtime/seccomp-soak-reader.ts) — order-insensitive per-field regex (pid/syscall/code/arch) on journalctl `audit: type=1326` lines; fire-and-forget audit row emission
- BuildNetnsSpawnArgsResult extended with seccompFd + seccompKillSwitchActive (composable additions; no breaking changes)
- mcp-sandbox.ts: seccomp kill-switch boot row emission (one-time-per-process MCP_NETNS_FALLBACK with reason='kill-switch: seccomp disabled' — uniform with Plans 01/02 per checker B1); FD threading via spawn env EZCORP_MCP_BWRAP_SECCOMP_FD=3 + wrapped.seccompFd; new exported runMcpSeccompSoakReader(pid, spawnAt, ctx) for post-shutdown soak read
- mcp-launcher.sh: bwrap branch refactored into two-arm if-else; appends --seccomp $EZCORP_MCP_BWRAP_SECCOMP_FD between --tmpfs /tmp and -- argv terminator when env var is set
- McpServerStdio type gained optional seccompFd field — channel for FD-passthrough into Bun.spawn's stdio array
- scripts/check-seccomp-bpf-fresh.sh CI guard: re-runs the C helper, cmp's output against committed BPF
- docs/deployment.md: new "Stage 1 kill-switches" (uniform boot-row callout), "MCP soak signal — bundled-corpus caveat" (with SCMP_ACT_LOG / SIGSYS-impossibility note per W3), and "Stage 1 seccomp — manual verification fallback" (operator checklist per W4) subsections
- 20 new tests: 10 in mcp-seccomp-profile.test.ts (5 profile-shape + 3 Dockerfile-shape W2 + 2 .bpf-presence) + 6 in mcp-stage1-soak-reader.test.ts + 3 in mcp-sandbox.test.ts "seccomp log mode" + 1 Linux-gated integration in mcp-netns-integration.test.ts
- 2 auto-fixed deviations (both Rule 1 bugs surfaced during Task 2): combined-regex field ordering broke real type=1326 line parsing → replaced with order-insensitive per-field regexes; lexicographic .sort() on number[] in test fixture → explicit numeric comparator
- All 5 RESEARCH.md Open Questions resolved (Pattern B confirmed; commit BPF + CI guard; per-MCP shutdown reader + fleet daemon deferred; bundled-corpus caveat documented + synthetic MCP deferred; kill-switches boot-time-only documented)
- Deferred items: fleet-wide sweep daemon, synthetic test MCP, production wiring of runMcpSeccompSoakReader from registry.ts, CI workflow integration of check-seccomp-bpf-fresh.sh, commit the generated .bpf artifact from first CI build

### Plan 56-01 outcome
- AlwaysAllowRecord additively widened with `ttlOverrideMs?: number | null` and `expiresAt?: number | null` (zero DB migration; lazy upgrade on next write)
- `readTtlOverrideMs(value: unknown): number | null | undefined` exported from src/extensions/permissions.ts — three-branch parser: null → "Never", positive finite number → override, undefined → fallback. 0/negative/NaN/Infinity all collapse to undefined (Pitfall 2 — null is sole Never sentinel)
- `buildAlwaysAllowValue(allowed, now, options?)` gains optional third arg `{ ttlOverrideMs?, expiresAt? }`. 2-arg legacy callers stay byte-identical (fields ABSENT, not undefined). Empty options object ≠ explicit null
- perm-expiry-sweep.ts `runSweep` always-allow loop's TTL-resolution branch replaced with precedence-aware version: null short-circuits BEFORE TTL_CONFIG[kind]/foreverTtlMs lookup (Pitfall 6 — honest Never even on scope=forever); positive number wins over BOTH TTL_CONFIG and foreverTtlMs; undefined falls through to legacy path
- 19 new RED→GREEN cases in always-allow-value-shape.test.ts (parse tolerance + readTtlOverrideMs branches + buildAlwaysAllowValue options) — 48/48 GREEN
- 7 new RED→GREEN cases in perm-expiry-sweep.test.ts (override-wins-over-TTL_CONFIG, override-null-skip, override-absent fallback REGRESSION, Pitfall 6 override-vs-foreverTtlMs, override-null on scope=forever, TTL_CONFIG=never+no-override skip REGRESSION, TTL_CONFIG=never+override=1h revoke) — 57/57 GREEN
- 16/16 GREEN on perm-expiry-sweep.integration.test.ts (REGRESSION — real PGlite always-allow row write/apply path still works)
- 0 deviations (Rules 1-4) — plan executed exactly as written
- Plans 56-02 (UI + endpoints) and 56-03 (formatTtl + sticky KV) unblocked

### Plan 58-01 outcome
- JSON profile flipped from observability (SCMP_ACT_LOG) to kernel enforce (SCMP_ACT_ERRNO + defaultErrnoRet: 38 — Pitfall 5 verbatim-38 lock asserted in test; EPERM=1 would break Bun JIT pkey_alloc fallback + Python 3.12 glibc clock_gettime64 probe)
- build/compile-seccomp.c rewrite: parse_default_action + parse_default_errno_ret + parse_syscall_action + find_entry_end helpers; seccomp_init reads parsed default (no SCMP_ACT_LOG hardcode); SCMP_FLTATR_ACT_BADARCH set to SCMP_ACT_ERRNO(ENOSYS) for multi-arch safety; per-syscall seccomp_rule_add reads parsed action
- src/mcp/client.ts: new getChildProcess() public method exposes SDK's internal StdioClientTransport._process via documented cast; returns null for http/sse + future SDK shape changes (degrade-soft); 12-line JSDoc documents the stability risk
- src/extensions/registry.ts: runMcpSeccompSoakReader wired into getMcpClient on McpClient child exit (fire-and-forget, defensive typeof guard for test stubs); spawnAt captured BEFORE connect() so journalctl --since window anchored to actual spawn time; closes Plan 55-03 deferred-item #3 (production wiring of soak reader)
- audit-actions.ts MCP_SECCOMP_VIOLATION JSDoc + inline field comment document 0x7ffc0000 (log) vs 0x00050001 (errno) metadata.code discriminator for SIEM dashboard splitting; action name unchanged for SIEM stability
- 4 new test files (14 cases + 1 Linux-gated): mcp-seccomp-enforce-flip (5 — JSON shape lock), mcp-seccomp-compile (5 — source-text grep on C file), registry-soak-reader-wire (3 — mocked McpClient + sandbox), mcp-seccomp-enforce-integration (1 — Linux+bwrap+gcc-gated, SKIPs cleanly on dev hosts)
- 3 deviations: Rule 1 plan-text correction (kexec_load absent from Phase 55 corpus — kernel-config-gated; test asserts the 4 actually-present canonical dangerous syscalls); Rule 3 blocking (mcp-registry.test.ts stubs lacked getChildProcess — added defensive typeof guard so pre-Phase-58 stubs pass unchanged); Rule 1 test maintenance (Phase 55 'defaultAction is SCMP_ACT_LOG' test updated to assert SCMP_ACT_ERRNO post-flip)
- 2 process incidents: git stash usage during pre-existing failure triage (mcp-e2e + mcp-api-routes are Bun 1.3.11 segfault — out of scope per REQUIREMENTS.md BUN-01; verified by running against main). All changes recovered cleanly via stash pop, but the global lesson at memory/feedback_agent_briefs_no_git_stash.md was violated; going forward use git worktree
- Plan 58-02 (veth-pair + nftables) unblocked; Plan 58-03 (conntrack + IPv6 disable + orphan sweep + docs) depends on 58-02
- RC#4 closed: kernel returns ENOSYS for any syscall outside the explicit allow-list; audit type=1326 row → MCP_SECCOMP_VIOLATION with metadata.code=0x00050001

### Plan 58-02 outcome
- probeVethCapability() + _setVethProbeOverridesForTests landed on mcp-netns.ts; mirrors probeBwrapAvailability shape verbatim (cache-once + test-seam injection over Bun-global mocking — 3rd Linux-only probe in the codebase joining probeNetnsAvailability + probeBwrapAvailability)
- allocVethSlot/releaseVethSlot + computeVethBridgeIp/computeVethMcpIp + _resetVethSlotAllocatorForTests landed on mcp-netns.ts; /30 IP math (slot N → bridge=10.42.0.{N*4+1}, MCP=10.42.0.{N*4+2}); 60-MCP concurrent cap with 4-slot headroom; slot 0 reserved for bridge gateway 10.42.0.1/24
- BuildNetnsSpawnArgsResult widened with 7 optional Stage 2 fields (vethAvailable/Reason/Id/Ipv4/HostSideName/NsSideName, stage2KillSwitchActive); fully additive — no breaking change to Phase 55 callers
- MCP_VETH_CREATED audit action added (CONTEXT-locked two-action split — Stage 1 keeps MCP_NETNS_CREATED, Stage 2 fires MCP_VETH_CREATED); JSDoc + metadata-shape doc
- McpServerStdio (types.ts) gains optional onChildSpawned + _internal_vethSetup fields; doc-blocks reference Open Question 1 (TOCTOU handshake) + Blocker #3 (carrier name lock)
- mcp-launcher.sh: new outer Stage 2 block (~50 lines) inserted BEFORE existing bwrap branch, gated on EZCORP_MCP_STAGE2_VETH_ENABLED=1; sequence handshake (read -n 1 FD 0) → lo up → rename peer to eth0 → eth0 up → ip addr add → default route via 10.42.0.1 → IPv6 disable per-iface (eth0 + lo, transitional `|| true` mask; Plan 03 hardens) → nft -f - heredoc with single tcp accept exception
- mcp-sandbox.ts buildSandboxedMcpSpec branches Stage 1 / Stage 2 based on probeVethCapability + EZCORP_MCP_STAGE2_VETH kill-switch; Stage 2 success path: allocate slot → ip link add veth → ip link set master br-ezcorp-mcp → ip link set up → MCP_VETH_CREATED audit row → thread launcher env (EZCORP_MCP_STAGE2_VETH_ENABLED=1 + peer name + IPv4 + 10.42.0.1:proxyPort gateway); bridge-missing path cleans up orphan veth + releases slot + falls back to Stage 1
- stage2KillSwitchBootRowEmitted module-scope flag mirrors tmpfs + seccomp kill-switch pattern; EZCORP_MCP_STAGE2_VETH=0 emits one-time MCP_NETNS_FALLBACK boot row with reason='kill-switch: stage2 veth disabled' — fourth identical pattern (DNS_RECHECK, TMPFS, SECCOMP, STAGE2_VETH)
- HookedStdioClientTransport subclass (src/mcp/client.ts) overrides start() to invoke onChildSpawned AFTER spawn AND BEFORE the SDK's JSON-RPC initialize; writeByte callback abstracts stdin reach so mcp-sandbox stays decoupled from SDK transport internals; pre-Phase-58 specs (no hook) fall back to bare StdioClientTransport — zero behavior change
- registry.ts: connect-failure tear-down at lines 695-704 reads sandboxedSpec._internal_vethSetup (transport-narrowed) and runs synchronous `ip link delete <hostSideName>` + releaseVethSlot before throwing — Open Question 2 belt+suspenders; happy-path schedules same cleanup on childProc.exited (mirrors soak-reader fire-and-forget pattern)
- Dockerfile: nftables added to apt-install line between iptables and libcap2-bin (~3 MB image growth on top of Phase 55's ~23 MB); doc-block updated with Phase 58 NET_ADMIN requirement for Stage 2 + degradation note (no NET_ADMIN → Stage 1 + seccomp enforce only)
- 5 new test files: mcp-veth-probe.test.ts (6 unit GREEN — probe), mcp-veth-allocator.test.ts (7 unit GREEN — slot math + cap + idempotent release), mcp-veth-bridge-integration.test.ts (Linux+CAP_NET_ADMIN+nft-gated: 2 GREEN + 2 test.todo for Plan 03 / Task 2 end-to-end), mcp-netns-raw-socket-blocked.test.ts (RC#1 scaffold; test.todo until Plan 03 bridge boot), tests/fixtures/raw-socket-probe/index.ts (ENETUNREACH probe fixture)
- 0 deviations (Rules 1-4) — plan executed exactly as written; 2 minor in-scope clarifications: (a) onChildSpawned uses writeByte callback abstraction instead of raw WritableStream (SDK uses node:child_process not Bun.spawn directly), (b) Case 2 + Case 4 of bridge integration test shipped GREEN immediately at Task 1 (raw ip-link invocations, no launcher needed)
- TDD discipline preserved: RED tests committed first (8fe8c0e), GREEN implementation committed second (a91dc4d); Task 2a + 2b kept separate commits (4dac6f6 + 53a1d5c) for surgical review
- Plan 58-03 (conntrack + IPv6 disable + orphan sweep + Stage 2 docs) unblocked — depends on Plan 02's naming pattern (`/^mcp-[a-f0-9]{8}$/`) for the orphan-sweep regex, slot allocator for conntrack pressure ties, and veth lifecycle for the boot sweep cleanup target
- RC#1 partial coverage: integration test scaffold + ENETUNREACH probe fixture exist; full GREEN gated on Plan 03 bridge boot. Until then test.todo holds the contract

### Plan 58-03 outcome
- mcp-bridge.ts lands as NEW module (~225 lines): ensureBridge (idempotent br-ezcorp-mcp create + ip addr add + ip link set up + sysctl IPv6 disable on bridge — defensive while-we're-here), ensureConntrackCeiling (idempotent only-write-if-lower; default floor 262144 = Debian bookworm ≥4GB RAM default per kernel docs — floor-guarantee not 4× increase), sweepOrphanVeths (regex /^mcp-[a-f0-9]{8}$/ — Pitfall 1 lock; matches Plan 02 host-side veth naming, NOT pre-correction mcp-<8hex>-host). _setBridgeOverridesForTests seam mirrors _setBwrapProbeOverridesForTests (Plan 55-02). BRIDGE_NAME + BRIDGE_CIDR_DEFAULT + VETH_HOST_NAME_PATTERN exports.
- audit-actions.ts: MCP_VETH_ORPHAN_SWEPT (one row per boot even count=0 — operator-visibility positive signal; metadata {count, names}) + MCP_CONNTRACK_HIGH (per spawn refusal at strict > 0.7 threshold; metadata {extensionName, conntrackCount, conntrackMax, ratio}). Both slotted adjacent to Plan 02's MCP_VETH_CREATED with full CONTEXT-locked JSDoc.
- mcp-netns.ts initStage2() boot routine: runs sweepOrphanVeths → ensureConntrackCeiling → ensureBridge in order; emits one-time MCP_NETNS_FALLBACK boot row on any failure with descriptive reason ('stage2 unavailable: ...', 'stage2 invalid bridge subnet'); sets module-scope stage2DegradedAtBoot flag. probeVethCapability honors flag AS SHORT-CIRCUIT BEFORE cache lookup (no per-spawn race). _resetInitStage2ForTests + isStage2DegradedAtBoot accessors. EZCORP_MCP_STAGE2_BRIDGE_SUBNET CIDR-validated /8..30 only.
- mcp-sandbox.ts: pre-spawn conntrack guard at TOP of buildSandboxedMcpSpec (BEFORE proxy startup + Stage 2 branching) — strict `>` 0.7 not `>=` (CONTEXT verbatim; case at exactly 70% passes through); throws descriptive Error + fire-and-forget MCP_CONNTRACK_HIGH row; SKIPs on /proc absent (non-Linux dev). _setConntrackOverridesForTests seam + readConntrackPressure helper.
- registry.ts private constructor calls void initStage2(null).catch(() => {}) — fire-and-forget; degradation flag is load-bearing signal, not the Promise resolution.
- mcp-launcher.sh: STRICT IPv6 disable assertions with discriminating exit codes 97 (eth0) and 96 (lo). Replaces Plan 02's transitional `|| true` mask. RC#3 contract now enforced at launcher level, not just nft filtering downstream.
- 5 new test files: mcp-bridge.test.ts (15 unit GREEN — 3 constants + 4 ensureBridge + 4 ensureConntrackCeiling + 4 sweepOrphanVeths; non-matching mcp-deadbeefXX 14-char + mcp-deadbeef-host 17-char both rejected), mcp-conntrack-guard.test.ts (5 unit GREEN — below/at/over threshold + /proc absent + metadata shape), mcp-stage2-orphan-sweep.test.ts (3 Linux+CAP_NET_ADMIN-gated integration cases — pre-seeded mcp-deadbeef swept + zero-orphan row count=0 + non-matching not swept), mcp-stage2-ipv6-disabled.test.ts (2 test.todo cases scaffolded — RC#3 needs full Stage 2 CI runner), mcp-stage2-conntrack-soak.test.ts (1 opt-in test.todo case — EZCORP_RUN_CONNTRACK_SOAK=1).
- scripts/mcp-conntrack-soak-24h.sh (executable bash): backs the documented 24h × 20 × 1000 manual-verification fallback for RC#2. Spawns 20 concurrent synthetic-MCP fixtures via tests/fixtures/synthetic-mcp/loop.ts, polls nf_conntrack_count every 30s, tails dmesg for nf_conntrack: table full, prints PASS/FAIL verdict + samples-file + per-fixture logs. Exit 0 PASS / 1 FAIL.
- tests/fixtures/synthetic-mcp/loop.ts: 30-line Bun script; loops fetch('http://example.com') for N iterations.
- docs/deployment.md +5 new subsections (Stage 2 prerequisites, Stage 2 readiness checklist for MCP-04 enforce flip, Stage 2 kill-switches extension, 24h conntrack soak manual fallback, MCP_SECCOMP_VIOLATION metadata.code shift callout 0x7ffc0000 log / 0x00050001 errno).
- .planning/ROADMAP.md RC#5 grep pattern lock-step correction: `'^.*veth-mcp-.*-host'` → `'^mcp-[a-f0-9]\{8\}$'` (Pitfall 1 / Blocker #1; without this gsd-verifier would assert against the wrong pattern downstream).
- 1 deviation auto-fixed (Rule 1 test maintenance): mcp-veth-probe.test.ts beforeEach/afterEach now call _resetInitStage2ForTests — the new probe short-circuit on stage2DegradedAtBoot would otherwise contaminate veth-probe tests after another test triggered initStage2 via ExtensionRegistry construction.
- Phase 58 fully complete: all 3 plans (58-01 seccomp enforce flip, 58-02 veth-pair + nftables, 58-03 conntrack + IPv6 + orphan sweep + docs) landed; RC#1-5 covered by tests + manual verification fallbacks.

### Plan 59-01 outcome
- Phase-start Playwright baseline pinned at HEAD `83781b5dd7bed7026d843f5c083f990d5bff23a7` ("wip: JsonBlock component + image-gen timeout extensions"). `baseline-passing.txt` = 1678 lines, one row per passing test case as `<file>::<line>:<column>::<title> [<projectName>]` (stable identifier across re-runs).
- `baseline-meta.txt` records: head_sha, captured_at `2026-05-12T22:07:45Z`, git_stash_count_at_capture 12 (sacred — pre- and post-execution match), playwright_run_duration_sec 664, full result breakdown (2724 total / 1678 passed / 695 failed / 162 skipped / 189 timedout), unique files with passing tests 150.
- Drift from triage doc's "868" figure: that figure was at commit `f16b427` (2026-05-09); today's HEAD is 3 days newer with more specs landed. Today's 1678 is the conservative-upward baseline; downstream `comm -23` regression invariant remains correct.
- Canonical jq selector documented in baseline-meta.txt notes + SUMMARY `patterns-established`: `jq -r '.. | objects | select(has("specs")) | .specs[]? | . as $spec | $spec.tests[]? | select(.results[]?.status == "passed") | "\($spec.file)::\($spec.line):\($spec.column)::\($spec.title) [\(.projectName)]"' run.json | sort -u`. Downstream wave-merge gates (59-05, 59-06, 59-08) MUST use this exact selector for `/tmp/phase-59-wave-N-passing.txt` so `comm -23` is comparable.
- 2 Rule-3 deviations auto-fixed: (1) plan's literal jq emitted only `.file` (would yield 150 files, below `min_lines: 800` gate + unusable for per-test diff) — followed plan INTENT; (2) Playwright auto-`webServer` block timed out on cold build with `ERR_CONNECTION_REFUSED` on first run (193 passed only) — pre-spawned `bun run preview` background process, re-ran honouring `reuseExistingServer: true` (1678 passed). Zero `playwright.config.ts` edits per plan scope ("Touching playwright.config.ts... is out of scope").
- Zero SUT (`web/src/`) edits, zero test (`web/e2e/`) edits, zero `api-mocks.ts` edits per plan's pure-capture contract.
- Stash count 12 verified pre-Task-1, post-Task-1, post-commit (`stash@{0}: On main: TEMP-56-03-regression-check`). Zero `git stash` operations performed.
- Downstream 59-02/59-04/59-05/59-06/59-07/59-08 all unblocked — every wave-merge gate now has a canonical, committed baseline to diff against.

### Plan 59-02 outcome
- 14 additive endpoint handler blocks landed in `web/e2e/fixtures/api-mocks.ts` before the (post-Task-1-shifted) L1460 default `{}` catch-all: `/api/audit` (GET) + `/api/audit-log` (GET legacy alias) + `/api/audit/stats` (GET) + `/api/extensions/[id]/audit` (GET) + `/api/extensions/[id]/expired-grants` (GET — Phase-56 banner consumer) + `/api/extensions/[id]/reapprove` (POST) + `/api/extensions/[id]/violations` (GET) + `/api/extensions/[id]/settings` (GET, PUT) + `/api/extensions/[name]/events/[event]` (POST — SEC-06 sentinel) + `/api/conversations/[id]/audit` (GET) + `/api/conversations/[id]/extension-toolbar` (GET — primary streaming-toolbar.spec.ts unblocker, 12 prior fails) + `/api/conversations/[id]/active-run` (GET — chat-page mount) + `/api/tool-calls/[id]/permission` (POST — F-trio fixme-flip unblocker) + `/api/active-agents` (GET).
- 8 new optional `MockOverrides` fixture fields wired (auditEntries / auditStats / expiredGrants / activeRun / extensionToolbarItems / extensionSettings / extensionViolations / activeAgents) with empty-default bindings so existing specs compile + run unchanged.
- Strict additive-only verified: `git diff 132eb70^..HEAD -- web/e2e/fixtures/api-mocks.ts | grep '^-' | grep -v '^---' | wc -l` = 0. Reviewer gate passes (zero `-` lines on existing handlers).
- Smoke run on the three predicted-unblock specs (streaming-toolbar / multi-agent / audit-global, --workers=1, 10.6 min): 12 passed, 42 failed with locator/visibility errors (NOT `waitForResponse 30000ms exceeded`) — the entire `waitForResponse` 30s timeout class is eliminated as predicted in plan done-criteria.
- Two full-suite runs both above-baseline: 1678 → 1679 (run 1) / 1681 (run 2). Baseline-regression `comm -23` "lost" set rotated across runs (1 then 4, only 1 overlap), proving the lost entries are flakes rather than regressions. The single overlap (`tool-card-rendering CopyButton mobile-chromium`) passes 356ms in isolation, fails 30s under full-suite contention — pre-existing flake on a file already on the 59-08 sweep target list.
- Zero SUT changes (all edits in `web/e2e/fixtures/`). Stash count 12 (sacred). 2 deviations auto-fixed (both Rule 3 blocking — preview-server pre-spawn + explicit-path `git add` discipline against parallel-agent file pollution).
- Unblocks 59-05 (chat-page cluster — `extension-toolbar` + `active-run` + audit chain) and 59-07 (F-trio fixme flips — `/tool-calls/[id]/permission` POST handler).

### Plan 59-03 outcome
- TEST-05 retro-claimed: pre-existing assertion `expect(meta_.projectIds).toEqual(["proj-a", "proj-b"])` at `web/src/__tests__/api-memories-patch.server.test.ts:367` is strictly stronger than CONTEXT's `expect.arrayContaining([...])` form — exact-match implies array-containment, so success-criterion #5 satisfied without weakening the assertion.
- Three-line coverage retained: L357 `toMatchObject` containment + L366 `Array.isArray` type guard + L367 `toEqual` exact-match — defense-in-depth against future shape drift (e.g. accidental swap to `[{id: ...}]` shape or stringification).
- Verification: `bun test ./web/src/__tests__/api-memories-patch.server.test.ts` → 17 pass / 0 fail / 67 expect() calls / 553ms on HEAD `83781b5dd7bed7026d843f5c083f990d5bff23a7`. Vitest path documented as fallback per project memory `project_vitest_must_run_from_web_subdir.md`.
- Zero source-code changes per plan's read-only contract; test file `web/src/__tests__/api-memories-patch.server.test.ts` confirmed untouched (`git diff` returns empty).
- 0 deviations (Rules 1-4) — plan executed exactly as written; 1 single-task doc-only retro-claim plan.
- Stash count 12 (sacred baseline preserved per memory `feedback_agent_briefs_no_git_stash.md`).
- TEST-05 marked Complete in REQUIREMENTS.md; ROADMAP.md plan-progress row updated (1/8 plans complete for Phase 59).

### Plan 59-04 outcome
- 10 pre-existing backend test failures triaged in `.planning/v1.4-backend-test-triage.md` (verdict shape locked by CONTEXT.md: real-bug | stale-assertion | bun-1.3.11-upstream). All 10 verdicts = stale-assertion (user resume signal: `auto-assign-all`). 0 real-bug filings; 0 BUN-01 deferrals.
- 7 test-only fixes shipped one-commit-per-file (commit hashes: 8d297f5 / cd5677b / 0e922cc / 9335f77 / 2f758e0 / 0d2527c / 7e00efc):
  - `executor-attachment-resolver-wiring.test.ts` — re-pointed to `stream-chat/setup-tools.ts::setupTools()` (resolver wiring moved at f912990) + `stream-chat/load-history.ts::loadHistory()` (rehydrate moved).
  - `executor-task-tracking-autowire.test.ts` — extended mocked `ToolExecutor` stub class with `setArgsResolver()` + `setCurrentAgentConfigId()` no-op methods so path-3 in setup-tools.ts:371-398 doesn't throw + swallow.
  - `memory-validation.test.ts` — `test.skip` on 3 user-data-drift tests (description-length, index-line length, name≠filename); 36 other tests in same describes remain active.
  - `helpers/mock-cleanup.ts` — `MODULE_PATHS` allowlist expanded with 10 missing snapshot targets (drizzle-orm + 9 internal modules across `extensions/runtime/*`, `extensions/{schedule-daemon, host-maintenance-daemon, mcp-sandbox, permission-engine}`, `db/queries/sdk-capability-calls`, `providers/llm`, `runtime/tools/permissions`). Closes #8/#9/#10 full-suite pollution.
  - `queries-lessons.test.ts` — replaced stale `rejects.toThrow()` with positive coexistence assertion (migrate.ts:1227-1236 evolved `idx_lessons_shared_slug_unique` to include `visibility`).
  - `scope-enforcement.test.ts` — accept `requireAuth` as valid gate + `PUBLIC_ROUTE_ALLOWLIST` for 3 anonymous endpoints (marketplace/categories, version, ready).
  - `spawn-assignment-handler.test.ts` — bumped `makeCtx()` defaults from `{ maxPerHour: 10, maxConcurrent: 3 }` to `{ maxPerHour: 1000, maxConcurrent: 100 }` so the rate-limiter (not the PDP per refactor 5ce8d59) gates the 60-tight-loop test.
- Files #8/#9/#10 (agent-configs-handler, agent-input-form, api-tool-invoke.server) need no per-file edits — already green in isolation pre-fix; full-suite pollution closes via fix #4.
- All 10 files verified green in isolation post-fix. Pollution-cluster smoke checks pass: `mock-cleanup + agent-configs + agent-input-form` = 44 pass / 0 fail.
- 0 SUT modifications. `git diff --name-only HEAD~9..HEAD -- src/runtime/ src/extensions/ web/src/routes/ web/src/lib/components/` empty. All 7 modified files live under `src/__tests__/`.
- Stash count 12 (sacred). 0 `git stash` operations.
- 0 deviations (Rules 1-4) — plan executed exactly as written; resume signal `auto-assign-all` short-circuited per-verdict checkpoint loop as designed.
- TEST-04 closed; downstream 59-05/06/07/08 (Playwright stale-locator clusters) unaffected — independent contract.

### Plan 59-05 outcome
- **Chat-page cluster repair (TEST-02): 5 of 8 specs hardened, +26 passing tests, 0 baseline regression** (commits `81e1442` SUT testids + `bdb6bb8` agent-chat + `9c02de0` multi-agent + `db934af` sub-conversations + `c52c862` streaming-race + `da85900` file-mentions).
- 3 SUT data-testid additions (all behavior-neutral, 1981/1981 vitest pass): `agent-chat-cta` on agents/[name]/+page.svelte (config-source path Chat button), `agent-chip` on AgentChip.svelte wrapper, `sub-convo-agent-name` on SubConversationBlock.svelte agent-name span.
- Agent-chat L5+L32 (the v1.3-triage Section E3 deterministic failures) repaired: testid swap fixes the locator, `localStorage.activeProjectId` seed fixes the `handleChat()` "Select a project first" semantic check that was added post-v1.3.
- File-mentions strict-mode collision fix via `{ exact: true }` (mention popover renders each entry as TWO spans both containing the basename) — net +22 tests.
- 3 specs SCOPED BACK with fix-shape notes in 59-05-SUMMARY.md: `shared-ui-components.spec.ts` (38 fails — Enter-after-mention listbox-stays-open behavior race in `openToolForm` helper), `streaming-toolbar.spec.ts` (Ctrl+/ Svelte 5 reactivity bug + WS-mock streaming-flow drift), `team-orchestration.spec.ts` (multiple tool-picker testid collisions need per-purpose `tool-picker-allowed/denied` testids in ToolSearchPicker.svelte). All require broader SUT-touch authority than testid-only.
- 2 specs partially repaired (locator hardening committed, deeper failures remain): `multi-agent.spec.ts` + `sub-conversations.spec.ts` — testid swaps stand alone as correct work; the underlying sendAndWaitForStream timeout (Send button stays [disabled]) and obsolete sub-conversations endpoint mock are scope-back items for a follow-up streaming-fix plan.
- Pre-flight rebuild discovery (Rule-3 deviation): SUT testid additions don't take effect until preview server is rebuilt + restarted. Documented for future plans.
- Stash invariant preserved (12 sacred). Mandatory `git add <explicit-file>` for every commit prevented bundling parallel agent's hljs-theme.css edit into 59-05 commits.

### Plan 59-06 outcome
- `theme-sidebar.spec.ts` repaired via testid hardening + (app)-route fix (commit `ca1de59`): 12 newly-passing tests on chromium + mobile-chromium projects (was 2/24 passing, now 14/24 passing + 10 fixme'd; 0 fail). All edits use `getByTestId(...)` or testid-anchored `getByRole(...)`. No `.first()`, no class-based locators, no widened `{ timeout: ... }` values.
- 3 pure-additive `data-testid` attributes on `(app)/+layout.svelte`: `desktop-sidebar` on `<aside>` (L237), `sidebar-collapse-btn` on Collapse button (L259-268), `sidebar-expand-btn` on Expand button (L331-340). SUT diff is testid-only (verified: `git diff main -- web/src/`).
- Patterns established: viewport-aware `themeToggle()` helper using `[data-testid='X']:visible button[aria-label='Y']` CSS selector chain to disambiguate dual-mounted components (ThemeToggle in both desktop-sidebar + mobile-header) — avoids `.first()` while resolving strict-mode collision; `waitForSplashGone()` helper using `waitForFunction(() => !document.getElementById('splash'))` so click handlers reliably land on interactive elements rather than the half-faded #splash overlay (z-index:9999, 300ms fade); title-based baseline regression diff (line-stripped) to avoid false positives when spec files are edited.
- **Critical Svelte 5 reactivity bug discovered + filed in `.planning/phases/59-test-debt-repair/deferred-items.md`:** click handlers correctly flip `store.sidebarCollapsed = true` / `store.mobileMenuOpen = true` (verified via `localStorage.getItem(...)` probe in test harness), but the corresponding `{#if store.sidebarCollapsed}` block at `(app)/+layout.svelte:332` and `<SwipeDrawer open={store.mobileMenuOpen}>` prop binding at L405 do NOT re-render reactively. 5 tests in this plan flagged `test.fixme` with explicit UN-BLOCKER comments. Likely affects MANY other specs across the suite (every test that mutates a singleton AppStore field and waits for UI re-render). Filed as highest-priority follow-up — should be addressed BEFORE 59-08 sweep proceeds.
- 8 of 9 plan-target spec files SCOPED BACK to follow-up plan after triage (filed in `deferred-items.md`): `mobile-navigation.spec.ts` tests removed mobile-tab-bar feature; `swipe-drawer.spec.ts` mobile chat-route flow refactor (Open menu button replaced by Back-to-project-menu on chat routes); `menu-keyboard-nav.spec.ts` mention search returns "No matches found" with seeded agents (likely 59-02 territory); `command-palette-v2.spec.ts` same root cause as theme-sidebar (visits `/`, expects sidebar — ~30 min following theme-sidebar pattern); `teams.spec.ts` settings-page expand/collapse interaction churn; `tool-card-rendering.spec.ts` PermissionGate +282-line v1.3 expansion + `.bg-gray-900` strict-mode collisions; `task-card-actions-full.spec.ts` `getByTitle("Start task")` strict-mode collisions; `selector-keyboard-nav.spec.ts` class-based locators (works today, preventative refactor needed). All filed with per-spec fix-shape, root-cause, and effort estimate.
- Plan-text correction (1678 vs 868 baseline figure) and baseline-diff line-fragility workaround documented in `deferred-items.md`.
- Baseline regression diff (title-based, line-shift-stable) shows 2 entries — both verified pre-existing flake / unrelated to 59-06 SUT changes: `agent-detail.spec.ts::Chat and Test buttons remain accessible in edit mode [mobile-chromium]` (passes in isolation, flaky); `tools-popover.spec.ts::handles API failure gracefully - shows 0 tools [mobile-chromium]` (likely 59-02 api-mocks default behavior interaction; my SUT changes don't touch this surface — verified by grep).
- Stash count 12 (sacred). 0 `git stash` operations.
- 4 deviations auto-fixed: Rule 3 blocking — per-batch triage instead of full-suite to avoid 59-02 conflict; Rule 1 bug — pre-set `ezcorp-theme=dark` on toggle test (default `prefers-color-scheme=light` under Playwright); 2 × Rule 4 architectural-STOP — Svelte 5 reactivity bug + 8-spec scope-back, both filed in deferred-items.md, no SUT logic changes attempted.
- Unblocks 59-08 (long-tail sweep should consume 8 deferred specs, OR file separate plans per category — Svelte reactivity fix likely warrants its own plan).
- 1 process incident: parallel 59-02 executor running concurrent full-suite test → killed my full-suite background job, switched to per-batch triage (3 batches of 2-3 specs each). Once 59-02 finished + committed (`132eb70` + `1c43e0c` + `d9d1356`), rebuilt + restarted preview before re-running. Documented for future plans — preview server doesn't HMR; rebuild + restart needed before each round of SUT-touching tests.

### Plan 59-08 outcome
- **FULL SCOPE-BACK to Phase 61.** All 84 sweep-target spec files deferred after exhaustive per-batch reconnaissance exposed ~530+ failing test cases dominated by feature-rework / mock-seed-gaps / component restructure — beyond the testid-only hardening surface this plan was scoped for. Mirrors 59-06's precedent (8 of 9 specs scoped back).
- **Reconnaissance complete for all 9 batches.** Per-batch run logs at `/tmp/phase-59-08-batches/{batch-1,batch-2,batch-3,batches-4-5-6,batches-7-8-9}.log`; aggregated triage at `/tmp/phase-59-08-recon.md`. Per-batch failure counts: batch 1 (chat) 47p/43f in 6.3min; batch 2 (canvas-dock) 28f heavy; batch 3 (ez-* + claude-design) 2p/28f in 7.0min; batches 4-6 (memory/task/settings, workers=4) 324p/158f in 7.1min; batches 7-9 (lists/nav/mobile/misc, workers=4) 137p/299+f in 4.3min.
- **Cross-cutting issues catalogued for Phase 61:** (1) `/api/account` mock missing per `.planning/debug/svelte5-layout-reactivity-2026-05-12.md` debug doc — affects any (app)-route navigation; same fix unblocks the 5 fixme'd `theme-sidebar.spec.ts` tests added by 59-06 commit `ca1de59`; (2) multi-mount component strict-mode collisions (ez-button across 3 ProjectRail mounts; same dual-mount pattern as theme-toggle from 59-06); (3) stale text-based assertions; (4) `.first()` usage in baseline specs forbidden by plan; (5) feature removals (mobile-tab-bar — same finding as 59-06's mobile-navigation.spec.ts).
- **Debug-doc verdict propagated.** Updated `deferred-items.md` to flag the Svelte 5 singleton-store reactivity bug filed by 59-06 as OBSOLETE — actual root cause is missing `/api/account` mock crashing Svelte 5's effect scheduler; Phase 61 should un-fixme the 5 theme-sidebar tests using a 3-line `page.route` mock.
- **Per-batch fix-shape blueprint + effort estimates filed in deferred-items.md (~250 lines appended).** Each of the 9 original batches has: failure-mode characterization, per-spec fix-shape estimate, effort estimate (~30min to ~6h per spec), cross-spec dependency hints. Phase 61 picks up exactly where 59-08 left off.
- **Zero SUT changes / zero spec changes:** baseline regression-diff invariant preserved by inaction (`comm -23` is provably empty because nothing in 59-08 touches the test surface). Saves ~12-min full-suite re-run cost while remaining provably correct.
- 2 deviations: Rule 4 architectural-STOP scope-back (the dominant decision); Rule 3 blocking-mitigated discipline measure for concurrent-agent file pollution (`hljs-theme.css` and `sub-conversations.spec.ts` were unstaged edits from concurrent agents — used explicit `git add <path>` to avoid bundling).
- Stash count 12 (sacred). 0 `git stash` operations.
- Phase 59 status: 6 of 8 plans complete (59-01/02/03/04/06/08 done; 59-05/07 still outstanding — 59-05 actively committing per recent `9c02de0`, `bdb6bb8`).

### Plan 60-03 outcome
- `docs/permissions/four-scope-modal.md` L29 row 1 (Allow this time) rewritten — both cells now match engine session-scope reality (commit `24849a9`). Column 2: `All future calls of this capability in this server session.`; column 3: `Until the server restarts (the grant lives in memory only).` Verbatim per 60-CONTEXT.md L74-76.
- Surgical 1-line diff (`1 file changed, 1 insertion(+), 1 deletion(-)`) — no incidental edits to header, other rows, worked example, or supporting prose.
- L83-84 supporting prose (`Session-scoped grants disappear on server restart automatically — they live in memory, never on disk.`) confirmed byte-identical post-edit via re-grep; clears doc-vs-engine contradiction surfaced in 60-RESEARCH.md.
- Engine ground truth referenced (per RESEARCH.md): `src/extensions/permissions.ts:206` (Scope docstring), `src/extensions/perm-expiry-sweep.ts:489` (sweep skips session rows), `src/extensions/permission-engine.ts:587-590` (session-scope candidate at PDP).
- Atomic-commit-per-artifact policy honored: this commit is "fix the lies about what the button does"; the rename "Allow this time" → "Allow until restart" + L33/L35 label-consistency ripple + in-product `PermissionGate.svelte:301` ships in plan 60-04 (Wave 2, `depends_on: ["60-03"]`) as a separate commit, independently revertable.
- 0 deviations (Rules 1-4) — plan executed exactly as written. All 6 verification gates passed (3 grep-content + L83-84 byte-identical re-grep + git-log commit-message grep + git-diff --stat single-line confirmation).
- DOCS-01 part 1/2 (semantic correction) landed; plan 60-04 ships part 2/2 (button rename + label-consistency) to fully close. REQUIREMENTS.md DOCS-01 status updated to "In Progress (Plan 60-03 landed — L29 table-row semantics corrected; Plan 60-04 ships button rename + L33/L35 label-consistency to fully close)" — checkbox left unchecked pending 60-04.
- Plan 60-04 unblocked (same-file `depends_on` satisfied; surviving "Allow this time" references at L33 + L35 documented in hand-off note of 60-03-SUMMARY.md).

### Plan 60-01 outcome
- `.planning/v1.4-retro-claim-audit.md` lands as a new tracked artifact (commit `0499e47`, force-added per established `.planning/*.md` precedent — directory is gitignored at L49 but PROJECT.md / ROADMAP.md / REQUIREMENTS.md / v1.4-backend-test-triage.md are all tracked via `git add -f`).
- 95-line doc: top-summary `**Audited 6 commits: 6 invariants-met, 0 gaps**` + h3-per-commit (preceded by `<a id="<sha>"></a>` anchor for renderer-independent cross-link binding from plan 60-02's PROJECT.md table cells) with `Failure-path test / Doc surface / Audit row on block-path / Verdict / Gap` structured bullets — mirrors Phase 59 `v1.4-backend-test-triage.md` format verbatim.
- 6 retro-claimed SHAs covered (no typos): `cae5c06` (env-key-leak install gate), `14b16ca` (compaction-interval setting), `f6c3790` (`!EZ:` generic dispatch), `7cc5efc` (install-gate integration close), `b652f2b` (compaction-key snake_case rename), `763d718` (wrangler ^4.90.0 pin — real SHA, NOT source-doc typos `763d738` / `763d778`).
- HEAD sha `ebf1ed1f726b059e517b61fab25519a6523d3ba5` pinned in doc for future-audit baseline diffing.
- Every `yes` verdict cites canonical `file:line` (e.g., `src/extensions/__tests__/env-key-leak-install-block.test.ts:86`, `src/extensions/audit-actions.ts:332`, `src/extensions/clamp-permissions.ts:354-383`) AND inline `Verify:` command — re-auditability is load-bearing.
- Every `n/a` verdict has a one-line reason (no bare `n/a` / `—` dashes) per CONTEXT.md invariant.
- Task 2 re-auditability gate GREEN: 35 tests pass / 0 fail / 78 expect() calls across 6 audit-cited verify commands — `bun test ./src/extensions/__tests__/env-key-leak-install-block.test.ts -t 'returns EnvKeyLeakInstallError'` (1 pass, 1.22s) + same file full run (13 pass, 1.59s) + `bun test ./src/runtime/ez-actions/__tests__/resolve-bundled.test.ts -t 'non-bundled rejection'` (3 pass, 0.51s) + `cd web && bunx vitest run src/__tests__/api-ez-actions-generic.server.test.ts` (15 pass, 1.09s) + `bun test ./extensions/memory-extractor/manifest-load.test.ts -t 'does NOT declare camelCase'` (1 pass, 0.51s) + `bun test ./src/__tests__/memory-extractor-settings-migration.test.ts -t 'clamps to nearest'` (2 pass, 1.22s).
- 1 deviation auto-fixed (Rule 1 plan-text correction): plan's Task-2 step 4 literal `bun test ./web/src/__tests__/api-ez-actions-generic.server.test.ts` fails because `*.server.test.ts` files under `web/src/__tests__/` are vitest-owned (per `web/scripts/test.sh:5` filter rule + file imports from `"vitest"`). Ran via `cd web && bunx vitest run` instead per project memory `project_vitest_must_run_from_web_subdir.md`. Audit doc's own `Verify:` lines unaffected — they cite only the bun-test-owned resolve-bundled helper for f6c3790.
- Zero `763d738` / `763d778` source-doc typos propagated to audit doc — fix in `.planning/ROADMAP.md` + `.planning/REQUIREMENTS.md` L50/L103 is plan 60-02's responsibility.
- Stash count 12 (sacred preserved per `feedback_agent_briefs_no_git_stash`). Zero `git stash` operations.
- Pre-existing unstaged working-tree modifications (`.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` / `.planning/STATE.md` / `.github/workflows/pullfrog.yml`) untouched — only `.planning/v1.4-retro-claim-audit.md` staged + committed via explicit-path `git add -f` (no `git add .` / `-A`) to avoid orchestrator-coordination conflicts during parallel wave-1 execution.
- CLAIM-01 part 1/2 (audit doc creation) closed; gsd-tools marked CLAIM-01 complete in REQUIREMENTS.md per plan frontmatter `requirements: [CLAIM-01]`. Plan 60-02 ships PROJECT.md cross-link wiring + SHA-typo fix (CLAIM-01 part 2/2) — anchors in audit doc are pre-built and ready for 60-02 table cells to point at.
- Plan 60-02 (PROJECT.md table-row updates + cross-link wiring) unblocked. Plan 60-04 (gap-list capture) unblocked with zero-gap path — `tasks/v1.4-retro-claim-gaps.md` ships empty (or absent) per CONTEXT.md "Zero-gap outcome".

### Plan 60-02 outcome
- `.planning/PROJECT.md` "Already-landed v1.4 commits" table extended from 4 rows × 2 cols to 6 rows × 3 cols (commit `6057eb1`): added `b652f2b` (compaction-key snake_case rename) + `763d718` (wrangler-pin / CVE-2023-3348); new "Audit" column with per-row anchor cross-links `[invariants](v1.4-retro-claim-audit.md#<sha>)` to plan 60-01's HTML anchors. Intro paragraph at L39 updated: "4 commits" → "6 commits"; "the v1.4 audit needs to claim them" rewritten as forward-link to the audit doc — bidirectional discoverability now lands.
- Source-doc SHA-typo invariant restored (commit `6791870`): `.planning/REQUIREMENTS.md` L50 + L103 + `.planning/ROADMAP.md` L231 + L232 corrected from `763d778` / `763d738` to the real `763d718` (verified via `git log --oneline -1 763d718`). Zero typo occurrences remain across all 3 source-of-truth docs (verified post-commit). ROADMAP.md L118 was already correct (`763d718`) per revised plan — left untouched.
- ROADMAP.md L231 prose rewritten beyond pure substitution: original "`763d778` / `763d738`-class wrangler-pin (`763d738`)" → "`763d718` (wrangler-pin / CVE-2023-3348)" — drops awkward typo-pair phrasing that was tortuous BECAUSE of the underlying typo confusion.
- Two atomic commits per CONTEXT.md L93 guidance: `docs(60-02)` (feature-add: table extension) + `chore(60-02)` (errata: SHA typo cleanup) — independent revertability + blame scoping.
- Cross-link form is relative-path (`v1.4-retro-claim-audit.md#<sha>`, no `.planning/` prefix) — correct from PROJECT.md's authoring location since both docs live under `.planning/`; renders correctly on GitHub web UI, local previewers, and IDE link-jump.
- 0 deviations (Rules 1-4) — plan executed exactly as written. All 4 final verification gates GREEN first run: 6 cross-links present in PROJECT.md, zero `763d738` / `763d778` matches across PROJECT.md + REQUIREMENTS.md + ROADMAP.md, both atomic commits present in `git log`, 6 commit rows in PROJECT.md table.
- Stash count 12 (sacred preserved per `feedback_agent_briefs_no_git_stash`). Zero `git stash` operations.
- Force-add (`git add -f`) used for staged `.planning/` artifacts per established precedent (directory is gitignored at L49; PROJECT.md / REQUIREMENTS.md / ROADMAP.md / v1.4-*.md all tracked via force-add).
- Concurrent 60-04 executor pollution in working tree (`docs/permissions/four-scope-modal.md` + `web/src/__tests__/extension-permission-modal.component.test.ts` + `web/src/lib/components/tool-cards/PermissionGate.svelte`) left untouched — staged only PROJECT.md / REQUIREMENTS.md / ROADMAP.md explicitly by path. No interference between parallel executors.
- CLAIM-01 fully closed: part 1/2 (audit doc creation, plan 60-01) + part 2/2 (PROJECT.md cross-link + source-doc errata, this plan) both landed. Phase 60: 3 of 4 plans complete; 60-04 (button rename + label-consistency, depends_on 60-03) is the only remaining — running concurrently in parallel wave.
- Future planners reading source-of-truth docs (PROJECT.md / REQUIREMENTS.md / ROADMAP.md) will see the correct `763d718` SHA and follow new cross-link chain to the audit doc.

### Plan 60-04 outcome
- Atomic 5-site rename `Allow this time` → `Allow until restart` landed in commit `462fb58` per CONTEXT.md atomic-blast-radius policy: 3 files / 5 insertions / 5 deletions, all in ONE independently-revertable commit.
- Surfaces: `web/src/lib/components/tool-cards/PermissionGate.svelte:301` (production button) + `web/src/__tests__/extension-permission-modal.component.test.ts:132` (component test title) + `docs/permissions/four-scope-modal.md:29` (table row 1 button-label cell — 60-03's deliberate hand-off) + `docs/permissions/four-scope-modal.md:33` (Deny row label-consistency) + `docs/permissions/four-scope-modal.md:35` (default-focus paragraph). Verified zero `Allow this time` hits remain in `web/` + `docs/` (excluding build artifacts) post-commit.
- Testid `permission-allow-session` unchanged — all 5 e2e references in `web/e2e/v1.3-permission-backbone.spec.ts` (L336/337/338/339/417) remain stable across the rename. Component test body uses `getByTestId(...)` and required zero changes beyond the test-title update.
- Component test (11 cases) GREEN via `cd web && bunx vitest run src/__tests__/extension-permission-modal.component.test.ts` (1.04s); Playwright permission backbone spec 10 passed / 12 skipped (skipped = pre-existing F-trio `test.fixme` per Phase 59-07, unrelated).
- Pre-flight grep confirmed exactly 5 surface hits matching RESEARCH.md inventory before any edits; post-commit grep confirmed exactly 5 new-label hits at the same 5 lines (no spurious additions, no missed sites).
- Manual button-layout check PASSED via Claude's Discretion review: label `Allow until restart` is 19 chars, sibling `Allow for this conversation` is 27 chars, container uses `flex flex-wrap gap-2`. No overflow risk; CONTEXT.md "Exact button width / styling tweaks" stayed unused.
- Phase-gate verification: 6/6 audit-doc h3 verdicts present, 6/6 PROJECT.md cross-links anchor to matching `<a id="<sha>"></a>` (renderer-independent — verified by SHA round-trip grep), zero `763d738` / `763d778` off-by-one typos in source-of-truth docs, zero `Allow this time` hits in `web/` + `docs/`.
- 2 deviations auto-fixed (both Rule 1 plan-text corrections, verification-command-only — zero functional code changes vs plan body): (a) `bun test ./web/src/__tests__/extension-permission-modal.component.test.ts` → `cd web && bunx vitest run src/__tests__/extension-permission-modal.component.test.ts` per project memory `project_vitest_must_run_from_web_subdir.md` (Bun runner lacks `vi.stubGlobal`); (b) `bunx playwright test v1.3-permission-backbone.spec.ts -g 'permission-allow-session'` → full spec because the F-trio tests using that testid are `test.fixme`'d per Phase 59-07 — `-g` filters titles and matches zero. Both align verification commands with project reality; the atomic 5-site rename itself executed exactly as written.
- `tasks/v1.4-retro-claim-gaps.md` zero-gap stub created (gitignored working state) — formalizes Phase 60 audit terminal state per CONTEXT.md L68 zero-gap-outcome rule (audit doc summary line: "Audited 6 commits: 6 invariants-met, 0 gaps"). Template comments embedded for future auditor convenience.
- Stash count 12 (sacred preserved per `feedback_agent_briefs_no_git_stash`). Zero `git stash` operations.
- Parallel 60-02 executor (`6791870` + `6057eb1`) committed PROJECT.md / REQUIREMENTS.md / ROADMAP.md between this plan's start and commit. No file overlap — both executors completed cleanly.
- DOCS-01 fully closed: part 1/2 (semantic correction of L29 row 1, plan 60-03) + part 2/2 (5-site rename + L33/L35 label-consistency, this plan).
- **Phase 60 fully complete:** all 4 plans landed; both phase requirements (CLAIM-01 + DOCS-01) satisfied; ready for `/gsd:verify-work` handoff.

### Plan 61-00 outcome
- Phase-start Playwright baseline pinned at HEAD `6d852cfe5871329672d17ba272f32a69ff95a852` ("docs(61-01): complete theme-sidebar 5-fixme flip plan"). `baseline-passing.txt` = 1726 lines, one row per passing test case as `<file>::<line>:<column>::<title> [<projectName>]` (stable identifier across re-runs).
- `baseline-meta.txt` records: head_sha, captured_at `2026-05-13T02:08:04Z`, git_stash_count_at_capture 12 (sacred — pre- and post-execution match), playwright_run_duration_sec 626, full result breakdown (2724 total / 1726 passed / 670 failed / 162 skipped / 166 timedout), unique files with passing tests 150.
- Drift from Phase 59-01's `83781b5d` baseline (1678 lines, 2026-05-12): +48 passing, -25 failed, -23 timedOut. Drift sources attributed in baseline-meta.txt notes to 59-02 api-mocks expansion, 59-05/59-06 testid hardening on chat-page cluster + theme-sidebar, 61-01 theme-sidebar 5-fixme flip via mockAccountEndpoints helper (commit `ea3f8a6`). Downstream `comm -23` regression invariant remains correct.
- Canonical jq selector documented in baseline-meta.txt notes + SUMMARY `patterns-established`: `jq -r '.. | objects | select(has("specs")) | .specs[]? | . as $spec | $spec.tests[]? | select(.results[]?.status == "passed") | "\($spec.file)::\($spec.line):\($spec.column)::\($spec.title) [\(.projectName)]"' run.json | sort -u`. Downstream wave-merge gates (61-01, 61-02, 61-03) MUST use this exact selector for `/tmp/phase-61-wave-N-passing.txt` so `comm -23` is comparable.
- 1 Rule-3 deviation auto-fixed: `gsd-tools commit` short-circuits on gitignored `.planning/` paths with `{ committed: false, reason: 'skipped_gitignored' }`; fell back to `git add -f` + `git commit -m "<plan-step-9 subject>"` (mirrors 59-01 commit `029e234`'s historical workflow). No SUT/spec edits.
- Pre-spawned `bun run build && bun run preview` background process on :4173 before Playwright invocation — `playwright.config.ts`'s `reuseExistingServer: true` honored the pre-spawned server. Zero `playwright.config.ts` edits.
- Concurrent-agent overlap with Phase 59-05's wave-N capture (workers=1) observed in process table during chromium window; result shape (+48 passing, -23 timedOut) confirms no observable contention impact.
- Stash count 12 verified pre-Task-1, post-Task-1, post-commit (`stash@{0}: On main: TEMP-56-03-regression-check`). Zero `git stash` operations performed.
- Zero SUT (`web/src/`) edits, zero test (`web/e2e/`) edits, zero `api-mocks.ts` edits per plan's pure-capture contract.
- Commit `788f990` on `main` contains exactly two files (`baseline-passing.txt` + `baseline-meta.txt` under `.planning/`); 1795 insertions.
- Downstream 61-01/61-02/61-03 wave-merge gates now have a fresh reference for Layer 2 title-based `comm -23` regression diffs per 61-VALIDATION.md.

### Plan 61-01 outcome
- All 5 `test.fixme` markers in `web/e2e/theme-sidebar.spec.ts`'s `Sidebar` describe block flipped to active passing tests (commit `ea3f8a6`): `collapse button hides sidebar`, `expand after collapse restores sidebar`, `Ctrl+\ shortcut toggles sidebar`, `mobile drawer opens on hamburger click`, `mobile drawer closes on backdrop click`. Both `chromium` and `mobile-chromium` projects green — `bunx playwright test theme-sidebar.spec.ts --workers=1` returns 24 passed (12 cases × 2 projects), zero failures, zero timeouts, 14.8s.
- Disposition recorded in commit body: **REPAIR (Bucket B) — 5 fixmes flipped via per-test /api/account mock helper; Svelte 5 verdict test-env-only confirmed**. Commit body cites `.planning/debug/svelte5-layout-reactivity-2026-05-12.md`.
- 1 deviation auto-fixed (Rule 1 bug): the plan's prescribed `test.beforeEach` shape inside `test.describe("Sidebar", ...)` failed verification on first run because Playwright runs `page.route` handlers in **reverse-registration order** — registering `**/api/account` in `beforeEach` (which runs before test body) lost to the broader `**/api/**` catch-all registered LATER by `mockApi` inside each test body. Replaced with file-scope `mockAccountEndpoints(page)` helper called inline AFTER `await mockApi(...)` in each affected test. Narrower-match route registered LAST now wins. Verification probe (since-deleted `_debug-61-01.spec.ts`) confirmed: `pageErrors: []`, `asideClass: w-56 → w-0 border-r-0`, `expandBtnExists: false → true`, `storeCollapsed: null → "true"`.
- Zero SUT touches: `git diff HEAD~1 HEAD -- web/src/ web/e2e/fixtures/ web/playwright.config.ts` returns empty. Single-file change confined to `web/e2e/theme-sidebar.spec.ts` (47 ins / 28 del).
- Stash invariant preserved: 12 sacred entries pre- and post-commit. Zero `git stash` operations performed (per `feedback_agent_briefs_no_git_stash`).
- Layer 2 baseline-diff DEFERRED: `61-VALIDATION.md` Layer 2 prescribes `comm -23` against `.planning/phases/61-test-debt-followup-feature-rework-specs/baseline-passing.txt`, which doesn't exist yet (Plan 61-00 owns the baseline capture and was NOT executed before this plan). Mitigation: single-file blast radius cannot regress Layer 2 within its scope; 61-00 baseline-capture against post-`ea3f8a6` HEAD will retroactively close the gap.
- Confirms Svelte 5 singleton-store reactivity is NOT broken (debug doc verdict `test-env-only` validated by real test run): root cause was `/account/+page.svelte:308` `account.name.charAt(0)` throwing TypeError when `/api/account` returned `{}` from api-mocks default catch-all; the thrown TypeError aborted Svelte 5's effect scheduler mid-flush, so click handlers ran (localStorage flipped) but dependent `{#if}` blocks never re-rendered. With proper mock + correct registration ordering, reactivity propagates correctly.
- Pattern locked: helper-function over describe-block beforeEach when Playwright route ordering matters. When test body's `mockApi` registers a broader `**/api/**` catch-all, any narrower-match `page.route` MUST be registered AFTER mockApi to win reverse-resolution. File-scope async helper called inline preserves code-locality without losing the ordering race.
- Hand-off: 61-02 reuses `mockAccountEndpoints(page)` pattern if `command-palette-v2.spec.ts` pivots to `/account` instead of `/extensions`. 61-03 closing task retitles `deferred-items.md` Svelte 5 entry from "Svelte 5 singleton-store reactivity bug" to "test-env: missing `/api/account` mock + Playwright reverse-route-registration ordering gotcha".

### Plan 61-02 outcome
- 4 Bucket A specs disposed in 4 atomic commits per CONTEXT.md L92 atomic-blast-radius policy: `3ae5a2f` (selector-keyboard-nav), `1b55881` (command-palette-v2), `c770969` (tool-card-rendering), `dcbd3a2` (task-card-actions-full). Combined-spec run: 8 baseline-passing → 26 post-plan (+18), 0 failed, 0 timedout (1 documented pre-existing flake on `tool-card-rendering CopyButton mobile-chromium` per 59-02-SUMMARY).
- **`command-palette-v2.spec.ts` materially repaired (20/20 pass):** route pivot from `/` (no `<aside>`) to `/extensions` ((app)-layout + pre-mocked `/api/extensions` at api-mocks.ts:888 — no per-test `/api/account` mock needed); helper redesign from sidebar-button click to `page.keyboard.press("Control+k")` (viewport-agnostic — button lives in `<aside class="hidden lg:flex">` so click-based helper failed all 9 mobile-chromium cases); stale assertions fixed: `aside h1` → `getByRole("heading", { name: "Extensions" })` for `/extensions` route and `waitForLoadState("networkidle")` for `/project/<id>` route (which redirects per viewport — `/project/<id>/chat` on mobile, last-chat redirect on desktop), `Go to Dashboard` → `Go to Home` (per `command-registry.ts:43` — `isProject ? "Go to Overview" : "Go to Home"`), `Back` button assertion with `{ exact: true }` to avoid mobile-chromium `Back to project menu` substring strict-collision.
- **3 specs testid-scaffolded preventatively + 36 cases FIXME'd with shared top-of-file UN-BLOCKER block** (`selector-keyboard-nav` 9 cases, `tool-card-rendering` 10 cases, `task-card-actions-full` 9 cases — plus `command-palette-v2` 0 cases since materially repaired). All 4 specs pass CONTEXT.md Layer 4 audit (every `test.fixme` has `UN-BLOCKER CONDITION:` comment within 10 preceding lines). Upstream blocker for 30 of the 36 FIXMEs: chat-page composer streaming race where `emitWs(tool:start)` arrives before composer progresses past `Thinking...` (error-context snapshots confirm: `paragraph "Thinking... (29s)"`, `combobox "Send a message..." [disabled]`, `button "Stop generating"`). When the race is fixed (likely Phase 62+ or 59-05 streaming continuation), flipping `.fixme` → `test` per case is a one-character revert with testids already in place.
- **11 SUT components testid-hardened** (all behavior-neutral attribute-only additions, zero logic/style/conditional/child-element/prop/import/`<script>` changes — verified per-task via `git diff -- <files> | grep -vE '(^[+-]\{3\}|data-testid)'`): 3 selector wrappers on chat composer toolbar (`model-selector`, `thinking-selector`, `mode-selector`), 7 tool-card root containers (`tool-card-{terminal,diff,search-results,task-list,task-detail,permission,default}`), 1 per-row wrapper inside `TaskListCard.svelte` `{#each items as task}` (`task-card-{task.id}` via Svelte template expression — interpolates row's task id at render time, zero logic change). `PermissionGate.svelte` had legacy `data-testid="permission-gate"` (zero consumers) renamed to `tool-card-permission` for naming-pattern symmetry.
- 2 Rule-1 deviations auto-fixed (plan-assumption invalidation by baseline): plan claimed `selector-keyboard-nav` was "preventative; already-green" and `tool-card-rendering` / `task-card-actions-full` failures were strict-mode collisions. Baseline-passing.txt + error-context snapshots disagreed — only 2 cases per spec are passing pre-61-02, the rest are blocked by an upstream streaming race. Refined disposition to `REPAIR (testid hardening + N-case FIXME with UN-BLOCKER)` per CONTEXT.md L60-66.
- 2 Rule-3 deviations auto-fixed: (1) `gsd-tools commit` returned `{committed: false, reason: 'skipped_gitignored'}` (same pattern as 61-00); fell back to explicit `git add <files>` + `git commit -F /tmp/commit-msg.txt` for all 4 commits, which had the bonus of explicitly excluding the parallel-session dirty files (`web/e2e/v1.3-permission-backbone.spec.ts`, `web/src/lib/hljs-theme.css`) per 59-05's `git add <explicit-file>` discipline lesson. (2) Preview-server rebuild + restart required after each SUT testid addition (per `bundled_manifest_boot_refresh.md`); did so before Tasks 1, 3, and 4 (Task 2 was spec-only).
- Sacred-12-stash invariant preserved pre-execution (12), after each of 4 commits (12), post-plan (12). Zero `git stash` operations performed.
- Patterns established: (1) per-variant testid naming `tool-card-{kind}` for sub-component variants; (2) per-row wrapper testid via Svelte template expression `{task.id}` inside `{#each ...}` — interpolates at render time, no logic change; (3) route-pivot to `(app)` layout route as Bucket B alternative when `aside h1` assertion doesn't match SUT (avoids needing `/api/account` per-test mock); (4) helper redesign from sidebar-button click to `page.keyboard.press("Control+k")` for viewport-agnostic palette open (button is `lg:flex` only); (5) shared top-of-file UN-BLOCKER block + per-test `UN-BLOCKER CONDITION:` comment to keep Layer 4 awk audit clean while consolidating context.
- Hand-off to 61-03: the 4 remaining Bucket A specs (`teams`, `swipe-drawer`, `menu-keyboard-nav`, `mobile-navigation`) are higher-complexity per CONTEXT.md L69 cheap-first ordering. `swipe-drawer` may benefit from the Ctrl+K-style viewport-agnostic helper pattern proven here. `mobile-navigation` Path A REWRITE (per CONTEXT.md L77) remains default. **Critical:** when baseline-passing entry count < spec total case count, disposition must be `REPAIR (X) + FIXME with UN-BLOCKER` per CONTEXT.md L60-66 — same pattern that landed cleanly for 3 of 4 specs here.

### v1.4 phase ordering (with dependencies)

1. **Phase 54** — Security Backbone Hardening (SEC-01..06). No deps. MUST precede Phase 59 (CC1 changes error shape).
2. **Phase 55** — MCP Stage 1: DNS rebind + tmpfs + seccomp LOG mode (MCP-01..03). Parallelizable with 54. Starts seccomp soak clock — MUST start ≥7 days before Phase 58.
3. **Phase 56** — Per-Capability TTL UI (TTL-01). Parallelizable with 54 + 55.
4. **Phase 57** — Phase 49 Mobile UX Deferred (UX-01..04). Parallelizable with security track.
5. **Phase 58** — MCP Stage 2: seccomp enforce + netns veth-pair (MCP-04..05). Depends on Phase 55 + ≥7-day soak.
6. **Phase 59** — Test Debt Repair (TEST-01..05). Depends on Phase 54 (post-CC1 error shape).
7. **Phase 60** — Audit-Claim & Docs Polish (CLAIM-01, DOCS-01). Depends on Phase 59 (triage informs retro-claim invariants).

## Performance Metrics

**Velocity:**
- v1.0: 42 plans across 11 phases (shipped in 3 days)
- v1.1: 49 plans across 19 phases (shipped in 2 days)
- v1.2: 58 plans across 17 phases (shipped in ~7 weeks)
- v1.3: N/A plans (no GSD per-phase tracking) across 11 phases (shipped in ~3 days, 184 commits, +90,483 / −6,589 LOC)
- v1.4: 7 phases planned, plans TBD per phase

**Per-plan execution metrics:**

| Phase | Plan | Duration | Tasks | Files | Notes |
|-------|------|----------|-------|-------|-------|
| 54 | 01 | ~3 min | 2 | 3 | TDD: RED test → GREEN impl, no refactor; 0 deviations |
| 54 | 02 | ~37 min | 2 | 9 | TDD: 5 RED test surfaces → GREEN impl; 2 auto-fixes (test maintenance + test setter), both within plan scope |
| 54 | 03 | ~7 min | 2 | 4 | TDD: 3 route-level RED + 3 handler-level GREEN-on-creation + 2 e2e fixme'd; 1-line source fix; 3 deviations (plan's RED prediction off + engine mock + e2e infra deferral via test.fixme) |
| 55 | 01 | ~4 min | 2 | 4 | RED→GREEN: 7 new "DNS rebind recheck" cases (incl. one-time MCP_NETNS_FALLBACK boot-row case); module-scope kill-switch flag mirrors Plan 02 pattern; 0 unplanned deviations (4 within-scope clarifications recorded in SUMMARY) |
| 55 | 02 | ~9 min | 3 | 6 | RED→GREEN+integration: 6 probe + 3 sandbox-wrap unit + 3 Linux-gated integration cases; 1 auto-fix (Rule 1 bug — dropped `--userns=keep-current` flag absent in bwrap 0.8 Debian bookworm); test-only injection seam preferred over Bun-global mocking; 1 process incident (git stash usage recovered without loss); pre-existing 55-01 regression in HTTPS_PROXY round-trip integration test documented in deferred-items.md (out of 55-02 scope) |
| 55 | 03 | ~14 min | 3 | 17 | RED→GREEN+integration+docs: 20 new tests (10 profile-shape/Dockerfile-shape W2 + 6 soak-reader + 3 seccomp-log-mode in sandbox + 1 Linux-gated integration); 2 auto-fixes (Rule 1 bugs — combined-regex field-order broke real type=1326 parsing → replaced with order-insensitive per-field regexes; lexicographic .sort() on number[] in fixture → numeric comparator); hand-rolled ~150-line C JSON→cBPF helper; commit-the-BPF + CI freshness guard discipline (mirrors manifest.lock.json); 5 deferred items (fleet sweep daemon, synthetic test MCP, registry.ts wiring, CI workflow YAML, .bpf artifact commit from first CI build) |
| 56 | 01 | ~4 min | 2 | 4 | RED→GREEN: 19 always-allow-shape cases + 7 sweep precedence cases (incl. Pitfall 6 — override wins over scope=forever foreverTtlMs); additive widening (Pitfall 1 — extra fields tolerated, NO Object.keys length checks); three-branch sentinel (null=Never, positive=override, undefined=fallback; Pitfall 2 — 0 is malformed, not Never); 0 deviations; 16/16 integration tests GREEN as REGRESSION |
| Phase 56 P00 | 7 min | 3 tasks | 7 files |
| Phase 56-per-capability-ttl-ui P02 | ~47 min | 3 tasks | 15 files |
| Phase 56-per-capability-ttl-ui P03 | ~61 min | 3 tasks | 11 files |
| Phase 57 P01 | 6min | 3 tasks | 12 files |
| Phase 57-mobile-ux-deferred-items P02 | 6min | 3 tasks | 7 files |
| Phase 57-mobile-ux-deferred-items P05 | 5min | 3 tasks | 6 files |
| Phase 57-mobile-ux-deferred-items P04 | 11min | 3 tasks | 13 files |
| Phase 57-mobile-ux-deferred-items P03 | ~11 min | 2 tasks | 12 files |
| Phase 57-mobile-ux-deferred-items P06 | 4min | 2 tasks | 3 files |
| Phase 57-mobile-ux-deferred-items P07 | 2 min | 3 tasks | 3 files |
| Phase 58-mcp-stage-2-seccomp-enforce-netns-veth-pair P01 | 8 min | 2 tasks | 10 files |
| Phase 58-mcp-stage-2-seccomp-enforce-netns-veth-pair P02 | 13 min | 3 tasks | 11 files |
| Phase 58-mcp-stage-2-seccomp-enforce-netns-veth-pair P03 | 16 min | 2 tasks | 13 files |
| Phase 59-test-debt-repair P03 | 2 min | 1 tasks | 1 files |
| Phase 59 P01 | 30 min | 1 tasks | 2 files |
| Phase 59-test-debt-repair P04 | 30 min | 4 tasks | 8 files |
| Phase 59 P02 | 53 min | 2 tasks | 1 files |
| Phase 59 P06 | 75min | 3 tasks | 2 files |
| Phase 60 P03 | 1min | 1 tasks | 1 files |
| Phase 60 P01 | 2min | 3 tasks | 1 files |
| Phase 60-audit-claim-docs-polish P02 | 5min | 2 tasks | 3 files |
| Phase 60-audit-claim-docs-polish P04 | 2min 28s | 4 tasks | 4 files |
| Phase 59-test-debt-repair P05 | 1h 37m | 3 tasks | 8 files |
| Phase 61-test-debt-followup-feature-rework-specs P00 | 22min | 1 tasks | 2 files |
| Phase 61-test-debt-followup-feature-rework-specs P02 | 26 minutes | 4 tasks | 14 files |
| Phase 61 P03 | 25min | 5 tasks | 6 files |
| Phase 59 P07 | 25 min | 2 tasks | 1 files |
| Phase 62 P03 | 30min | 2 tasks | 1 files |
| Phase 62 P04 | 8min | 2 tasks | 1 files |
| Phase 62 P07 | 1min | 2 tasks | 1 files |
| Phase 62 P09 | 3min | 2 tasks | 1 files |
| Phase 62 P06 | 8min | 3 tasks | 2 files |
| Phase 63 P02 | 15m | 2 tasks | 3 files |
| Phase 63-indexing-primitives P01 | 20min | 3 tasks | 5 files |
| Phase 63 P03 | 17min | 2 tasks | 4 files |
| Phase 64-embed-on-write-worker P01 | 15 | 2 tasks | 4 files |
| Phase 64-embed-on-write-worker P02 | 10 | 2 tasks | 4 files |
| Phase 65 P01 | 50min | 3 tasks | 4 files |
| Phase 65 P02 | ~5min | 3 tasks | 6 files |
| Phase 66 P01 | 6min | 3 tasks | 8 files |
| Phase 66 P02 | 5min | 2 tasks | 3 files |
| Phase 66 P03 | 6min | 3 tasks | 5 files |
| Phase 66 P04 | 35min | 2 tasks | 3 files |

## Accumulated Context

### Decisions

All prior decisions logged in PROJECT.md Key Decisions table.

Recent v1.4-shaping decisions:
- [Roadmap]: Phase split CC1 cache → null→deny swap as **two-step** (not one-line) — per Pitfall 1, three downstream specs flake without the cache.
- [Roadmap]: MCP Phase-8 split into 55 (B.1) → 58 (B.2) with a ≥7-day seccomp soak window between them — per Pitfall 3 (vDSO trap risk).
- [Roadmap]: Phase 56 (TTL UI) and Phase 57 (mobile UX) explicitly parallelizable with security track — no shared files per ARCHITECTURE.md two-DAG analysis.
- [Roadmap]: Test debt repair (Phase 59) sequenced AFTER Phase 54 — CC1 changes `loadConversationOverride` error shape, invalidating pre-CC1 mock triage.
- [Roadmap]: Audit-claim (Phase 60) last — triage findings inform what gaps retro-claims are hiding.

Plan 54-01 execution decisions:
- [54-01]: Override cache lives at module scope, NOT inside `createPermissionEngine` factory — the prime caller (`addConversationExtensions`) has no engine handle; `allowCache` stays factory-scoped because it ties to `resolvePrompt` lifecycle.
- [54-01]: Lazy expiry on cache `get` — no background `setInterval` sweeper. Bounded by (active conversations × extensions with overrides).
- [54-01]: Selective prime — `addConversationExtensions` primes ONLY entries with `effectiveGrantedPermissions !== undefined` (spawn-assignment path); auto-wire entries skip to keep cache lean.
- [54-01]: Static circular import (`permission-engine` ↔ `conversation-extensions`) accepted — both edges resolve at function-call time, never at module load.
- [54-01]: Test 3 codifies Plan 1 fail-OPEN boundary as a canary — it WILL flip RED when Plan 02 lands, which is the expected transition signal.

Plan 54-02 execution decisions:
- [54-02]: F.1 (re-anchor) chosen over F.2 (delete) for Plan 01 cache test 3. The cache-miss-then-DB-call code path is still distinct from the new `permission-engine-override-fail-closed.test.ts` (different setup paths). Re-anchored test asserts post-swap fail-CLOSED semantic.
- [54-02]: AUDIT_PERM_DENIED re-used for SEC-03 cap-exceeded denies (with structured `metadata.reason='Per-conversation call-depth cap exceeded'`) instead of a new audit-action constant. Keeps audit-action taxonomy stable; SOC 2 / SIEM filters on `metadata.reason`.
- [54-02]: AUDIT_PERM_DENIED imported from `./audit-actions` (canonical source) in tool-executor.ts, NOT from `./permission-engine`. No circular-dep risk — `tool-executor.ts` doesn't import from `permission-engine.ts`.
- [54-02]: MAX_CALL_DEPTH=10 (per-chain) preserved alongside MAX_CALL_DEPTH_PER_CONVERSATION=50. Per-chain cap fails fast for runaway recursion; per-conversation cap bounds parallel fan-out.
- [54-02]: Counter decrement uses lazy delete on hit-zero so the conversationCallDepth Map doesn't grow unboundedly across process lifetime (mirrors Plan 01's overrideCache no-sweeper pattern).
- [54-02]: New ToolExecutor.setCurrentConversationId() setter added to support tests pinning conversation id without dispatching a real tool call. Production code unchanged (executeToolCall still wires currentConversationId at line 599).
- [54-02]: logger.error chosen over a custom alarm path because the recursion-guarded fire-and-forget hook in src/logger.ts:41-46 already persists `error`-level lines to `error_logs` — wiring through logger.error gets BOTH stderr observability AND DB durability for free.
- [54-02]: D.3 grep result confirmed — 0 additional read-sites needed updating. Audit endpoint accepts user-supplied `?action=` passthrough; admin first-time-grant write sites are semantically distinct from reapprove and intentionally left alone.

Plan 54-03 execution decisions:
- [54-03]: Created dedicated `src/__tests__/append-message-handler-pdp.test.ts` per plan spec; the 3 handler-level cases pass GREEN immediately because the handler already supports the engine path. Added the GENUINELY RED route-level cases at `web/src/__tests__/extensions-events-route.test.ts` SEC-06 block — those drove the wiring change. Both files have value (handler-level adds the new deny-prevents-write assertion for SEC-06 traceability; route-level proves integration wiring).
- [54-03]: Mocked `$server/extensions/permission-engine` to a sentinel singleton object (MOCK_ENGINE) in the route test. Three benefits: (1) prevents `getPermissionEngine() not initialized` throws once the call moves outside the existing try/catch, (2) lets SEC-06 cases compare ctx.engine BY IDENTITY (proving wiring), (3) keeps the existing 8 messageToolbar tests stable post-fix.
- [54-03]: e2e block added as 2 `test.fixme` cases in v1.3-permission-backbone.spec.ts (NOT live tests) with comprehensive rationale — mock-fixture e2e harness has no live audit_log to query against. Mirrors the F-test fixme pattern already in the file. Route + handler layer coverage is comprehensive; the e2e cases document the user-flow contract for v1.5 e2e infra to flip live.
- [54-03]: Pitfall 5 cleared — `ezcorp:chat:append` NOT in `SENSITIVE_KINDS` (only shell + fs.write at capability-types.ts:56-59). PDP returns allow without prompting; the wiring change introduces no UI gate that would block the user-visible click → toast → audio sequence.
- [54-03]: 1-line source change reused the existing `getPermissionEngine` import at the route's line 18 (already in use at line 319 for the subprocess wirer). No new imports, no static-analysis tool drift, minimal blast radius.
- [Phase 55]: [55-01]: Kill-switch boot row uses insertAuditEntry directly (NOT auditBlocked extension) so MCP_NETNS_FALLBACK action is consistent across all three Stage 1 kill-switches; auditBlocked is hard-wired to MCP_HOST_BLOCKED only.
- [Phase 55]: [55-01]: MOCK_INTERNAL_HOST_RESULT boolean refactored to MOCK_INTERNAL_HOST_PREDICATE function in mcp-proxy.test.ts — rebind cases need false for hostname AND true for resolved IP; flat boolean was too coarse.
- [Phase 55]: [55-01]: Audit row writes config.userId/extensionId, NOT the 'system' sentinel suggested in plan — config is already threaded through handleConnect(); real values give operators richer /audit filtering.
- [Phase 55]: [55-02]: Dropped `--userns=keep-current` from launcher bwrap argv — flag absent in bubblewrap 0.8 (Debian bookworm — the production target) and rejected by setuid bwrap 0.11 on NixOS dev hosts. Non-setuid bwrap (Docker production) inherits parent userns automatically when --unshare-user is not requested; Pattern B intent preserved.
- [Phase 55]: [55-02]: Test-only injection seam (_setBwrapProbeOverridesForTests) preferred over mock.module(Bun) — Bun globals (Bun.which, Bun.spawnSync) aren't cleanly module-mockable; the seam keeps production unchanged when overrides are null. Pattern repeated for Plan 03's probeSeccompAvailability.
- [Phase 55]: [55-02]: Kill-switch boot row is module-scope flag (one-time-per-process), bwrap-missing fallback row is per-spawn — different semantics: operators want one signal that a feature is disabled; every MCP that runs in degraded mode matters.
- [Phase 55]: [55-02]: BuildNetnsSpawnArgsResult kept fully composable (3 new optional fields) — Plan 03 will splice seccomp fields without breaking existing callers.
- [Phase 55]: [55-03]: Build-time JSON→cBPF compile via a ~150-line C helper (build/compile-seccomp.c) linked against -lseccomp. Avoids runtime libseccomp linkage in Bun; runtime image needs only libseccomp2 (bwrap dlopens). Hand-rolled minimal JSON parser ~80 LOC vs pulling in libcjson-dev.
- [Phase 55]: [55-03]: Commit `mcp-seccomp.bpf` + CI freshness guard (`scripts/check-seccomp-bpf-fresh.sh`) — mirrors `manifest.lock.json` precedent for tamper-resistant build artifacts. Dev hosts without libseccomp-dev (NixOS, macOS) skip the local regenerate; CI is the canonical compile site.
- [Phase 55]: [55-03]: JSON schema flattened to one-name-per-entry. Docker default has ~63 entries with ~300 names gated by args/includes/excludes; Phase 55 collapses to 407 unconditional-log entries (passes test's ≥250 floor).
- [Phase 55]: [55-03]: Order-insensitive per-field regex in the soak parser. Real kernel type=1326 lines emit fields in order pid → arch → syscall → ip → code on x86_64; a combined regex with fixed order silently drops every line. Independent `\bname=value\b` captures are order-agnostic.
- [Phase 55]: [55-03]: runMcpSeccompSoakReader exported (not auto-invoked from registry.ts). Production caller wiring is deferred to a follow-up — keeping mcp-sandbox.ts's spec-builder synchronous + unit-testable.
- [Phase 55]: [55-03]: Three Stage 1 kill-switches now uniform — DNS_RECHECK, TMPFS, SECCOMP all emit one-time MCP_NETNS_FALLBACK boot rows with `kill-switch: <feature> disabled` reason discriminators. Closes checker B1.
- [Phase 55]: [55-03]: FD-passthrough via Bun.spawn stdio[3] (seccomp BPF blob). McpServerStdio type extended with optional `seccompFd` field; transport caller plumbs it through stdio array. Parent closes FD after spawn returns.
- [Phase 55]: [55-03]: gcc purged from runtime image (build-stage `apt-get purge -y --auto-remove`). Task-3 integration test SKIPs when Bun.which("gcc") === null; manual-verification fallback in docs/deployment.md is the operator's path.
- [Phase 56]: [56-01]: null is the SOLE Never sentinel — 0 collapses to undefined (Pitfall 2). A future refactor adding `0 === Never` would silently brick the override-vs-fallback decision in the sweep; 0 was deliberately chosen to be malformed.
- [Phase 56]: [56-01]: parseAlwaysAllowValue stays unchanged — structural typing already tolerates extra fields (Pitfall 1). Adding `Object.keys(v).length === 2` would break legacy `{allowed, grantedAt}` rows the moment the shape widens.
- [Phase 56]: [56-01]: buildAlwaysAllowValue empty options object ≠ explicit null. `{}` keeps fields ABSENT (byte-identical to pre-Phase-56 output); `{ ttlOverrideMs: null }` writes null. Lets Plan 56-02 pass through caller-supplied options without injecting null on bypass.
- [Phase 56]: [56-01]: Override precedence wins everywhere — including `TTL_CONFIG[kind] === "never"`. Rare but contractual (a user who picks a 1h override on a storage grant gets revoked at 1h despite the never-tier fallback). Plan 56-01 task 2 case 7 locks this.
- [Phase 56]: [56-01]: extension-grant scope per-row override is OUT OF SCOPE this plan — per-row override applies ONLY to always-allow rows (settings table JSONB). `extensions.grantedPermissions.grantedAt` entries continue to use TTL_CONFIG fallback. Per the must_haves frontmatter.
- [Phase 56-00]: Vitest include[] extended explicitly with Phase 56 Wave 0 basenames — plan <files_modified> contract pinned bare .test.ts suffix; renaming would either collide with existing .unit.test.ts or break downstream verify commands.
- [Phase 56-00]: Mock both buildAlwaysAllowValue AND setSensitiveAlwaysAllow in reapprove-route test — Plan 56-02 will choose one as the ttlOverrideMs writer; RED scaffold captures whichever lands.
- [Phase 56-00]: test.fixme over live e2e for ttl picker — mirrors existing F-describe / L797 fixme pattern. Phase 59 TEST-03 (or opportunistic 56-03 flip) owns un-fixme'ing.
- [Phase 56-00]: Sticky-pick chat-side write tested in same file as reapprove route — CONTEXT.md states both surfaces share KV namespace; one-file lock documents contract symmetry.
- [Phase 56-00]: Accept wrong-reason RED for 2/11 cases (scope='conversation' rejected by current validator; admin-gate already wired) — Plan 56-02 widens scope vocab and flips them RED for the intended reason.
- [Phase 56-per-capability-ttl-ui]: [56-02]: null branch in expiryCopy() yields 'Approve forever' (distinct from admin 'Approve forever (admin only)' via parenthetical). Mental model: 'no expiry' vs 'scope escalation'.
- [Phase 56-per-capability-ttl-ui]: [56-02]: TTL_OPTIONS + DEFAULT_TTL_FIRST_USE_MS exported ONCE from expiry-copy.ts; both UI surfaces import. A future 8th option needs a one-line edit. Verbatim-copy contract (Pattern 2).
- [Phase 56-per-capability-ttl-ui]: [56-02]: ttl-validate.ts placed at src/extensions/ — Bun chat handler AND Vite web reapprove route both import. One verbatim error string, one Pitfall 2 lock (0/neg/NaN/Infinity → 400).
- [Phase 56-per-capability-ttl-ui]: [56-02]: Reapprove endpoint scope vocabulary widened from {forever,undefined} to AlwaysAllowScope (session/conversation/project/forever). Defense-in-depth scope=forever admin gate UNTOUCHED. Picker Never on non-forever scope is allowed for any auth'd user (NOT scope escalation).
- [Phase 56-per-capability-ttl-ui]: [56-03]: Parallel-formatter idiom — formatTtl('absolute') is a one-line delegate to humanizeDuration; preserves modal 'Approve N days' verbatim-copy contract while past/future modes flip to Intl.RelativeTimeFormat prose. Future i18n: change one locale binding.
- [Phase 56-per-capability-ttl-ui]: [56-03]: Locale pinned to 'en' at module-scope RelativeTimeFormat construction (Pitfall 5). Default-locale path would silently fall back on stripped-ICU jsdom — pinning makes the failure mode 'unsupported locale' instead of 'arbitrary locale in effect'.
- [Phase 56-per-capability-ttl-ui]: [56-03]: Defensive getSetting try/catch on /expired-grants enrichment — a settings DB failure cannot brick the banner load. Avoids forcing every pre-existing test in the codebase to mock the settings module just to satisfy a new optional enrichment (cap-expiry-flow.server.test.ts stays GREEN unchanged).
- [Phase 56-per-capability-ttl-ui]: [56-03]: Banner per-row TTL uses ttlOverrideMs (user's picker selection), NOT audit metadata ttlMs (sweep's applied TTL). The 'Approved for N days' label must reflect user intent, not sweep derivation. Legacy rows (no ttlOverrideMs) fall through to age-only cell — preserves pre-Phase-56 visual shape.
- [Phase 56-per-capability-ttl-ui]: [56-03]: Chat-side capability kind via getPendingExtensionGate + mapAlwaysAllowCapabilityToExpiryKind (now exported from perm-expiry-sweep.ts as the single source-of-truth helper); falls through to 'unknown' suffix only when the gate registry is un-queryable. Picker intent never silently dropped.
- [Phase 57]: [57-01]: Wave 0 RED discipline preserved — 12 test files commit BEFORE any production code; mirrors Phase 56 W0. Sampling matrix materialized as code so Wave 1-3 tasks each have a pre-existing <automated> verify target.
- [Phase 57]: [57-01]: dndzone sortable contract asserted via aria-roledescription='sortable' (public surface) instead of data-is-dnd-shadow-item-* (internal). Test stays stable across svelte-dnd-action minor versions.
- [Phase 57]: [57-01]: Perf bench p95<50ms gate split-tolerated per VALIDATION.md Manual-Only — if PGlite WASM ilike can't hit budget post-Wave-2, criterion gate is external-Postgres path; PGlite number documented in Plan 04 SUMMARY.
- [Phase 57]: [57-01]: GIN sweep test uses two-arm RED proof (tick 1-5 expects 0 calls AND tick 6 expects >=1) so cadence regression is distinguishable from missing-block regression.
- [Phase 57]: [57-01]: 8 of 9 picker entry points use TBD-comment trigger test-ids in bottom-sheet-pickers.spec.ts — Wave 2 Track A (Plan 57-03) tightens them as each picker mount point ships a deterministic data-testid.
- [Phase 57-mobile-ux-deferred-items]: [57-02]: CSS-only BottomSheet impl chosen over @abhivarde/svelte-drawer — simpler verbatim contract for W0 tests; dep installed for future swap-in if gesture polish needed.
- [Phase 57-mobile-ux-deferred-items]: [57-02]: env(safe-area-inset-bottom) MUST be set via element.setAttribute('style', ...) — Svelte's interpolated style attribute routes through CSSStyleDeclaration which silently drops env() in jsdom/older parsers.
- [Phase 57-mobile-ux-deferred-items]: [57-02]: Window-level Escape listener attached on visible (mount-on-open) NOT entering (next-frame flag) — synchronous keydown after open must fire onclose; gating on entering races with the test and real users hitting ESC during entry animation.
- [Phase 57-mobile-ux-deferred-items]: [57-02]: Assignment-picker e2e cases left fixme (deviation from plan's un-fixme step) — W0 scaffold URL /teams/builder doesn't exist; TaskPanel route requires Docker auth. Component-layer wrap verified by 8/8 BottomSheet.component.test.ts GREEN; Wave 2 Track A wires real entry-point URLs + auth fixtures.
- [Phase 57-mobile-ux-deferred-items]: [57-02]: AssignmentPicker conditional-wrap pattern uses {#snippet pickerBody()} for DRY markup share between BottomSheet (bp.below) and anchor-positioned dropdown (>=lg) branches. Wave 2 Track A copy-pastes this to 8 more pickers verbatim.
- [Phase 57-mobile-ux-deferred-items]: [57-05]: aria-roledescription='sortable' authored explicitly on dndzone container — svelte-dnd-action does NOT auto-stamp it; library only sets role='list'/'listitem' on keyboard-mode init (Space-to-grab), not on initial render. W0 RED test's comment was incorrect; component-layer truth is the explicit ARIA contract.
- [Phase 57-mobile-ux-deferred-items]: [57-05]: WCAG 2.5.1 single-pointer-equivalent satisfied by svelte-dnd-action's keyboard mode — NO separate up/down buttons. Keyboard mode IS the equivalent; documented decision (RESEARCH Open Question 4).
- [Phase 57-mobile-ux-deferred-items]: [57-05]: SelectedPill gains optional chipId prop (forwarded as data-chip-id) instead of wrapping each pill — svelte-dnd-action requires items as direct children. Legacy callers stay byte-identical (attribute absent when prop unset).
- [Phase 57-mobile-ux-deferred-items]: [57-05]: onconsider AND onfinalize both route through onchange — svelte-dnd-action requires the items reference to mutate during drag; without onconsider→onchange, the parent's $state doesn't mirror in-flight order and the chip snaps back.
- [Phase 57-mobile-ux-deferred-items]: [57-05]: Chip-reorder e2e cases kept fixme — Plan 57-02 precedent (assignment-picker e2e fixme'd for same reason). Blocker: /agents/[name] is in (app) protected route group requiring auth fixture + test-agent DB seed (extensions pre-attached). Component-layer 4/4 GREEN is the binding regression contract.
- [Phase 57-mobile-ux-deferred-items]: [57-05]: mobile-chromium playwright project added pre-emptively (Pixel 5 devices preset) — currently unused (all chip-reorder cases fixme), wired now so future un-fixme is a one-line operation.
- [Phase 57-mobile-ux-deferred-items]: [57-04]: word_similarity(q, doc) > 0.4 over similarity(doc, q) for browseMarketplace — full-string similarity scores ≤0.1 for short query vs long doc even on exact prefix match; word_similarity finds the best matching window. Inline threshold (0.4) over SET LOCAL GUC anti-pattern.
- [Phase 57-mobile-ux-deferred-items]: [57-04]: pg_trgm contrib registration MUST happen at PGlite construction in EVERY test PGlite site that calls migrate() — not just production. 'CREATE EXTENSION pg_trgm' throws 'extension not available' (not a no-op) when contrib isn't registered. 9 test files updated for parity.
- [Phase 57-mobile-ux-deferred-items]: [57-04]: GIN sub-tick rides existing 1h HostMaintenanceDaemon schedule via tickCount modulo 6 — every 6th tick = 6h sweep matching UX-02 cadence. tickCount increments AFTER TTL sweep settles; try/catch around gin_clean_pending_list tolerates PGlite-doesn't-implement-it path. No new daemon, no background-timers.ts edit.
- [Phase 57-mobile-ux-deferred-items]: [57-04]: Drizzle SQL chunks in 0.x lack public .sql property — both sql`...` and sql.raw(...) stringify to '[object Object]'. Test spy inspection must walk private queryChunks array recursively. Fixed countGinSweepCalls helper in Plan 57-01 RED scaffold (Rule 1).
- [Phase 57-mobile-ux-deferred-items]: [57-03]: vitest-setup.ts gains permanent window.matchMedia stub — jsdom 29 does NOT ship a polyfill (despite stale comment in use-breakpoint.unit.test.ts); per-file Object.defineProperty overrides had been masking the gap. Stub returns matches: false (desktop branch); tests asserting <lg override per-test. Unblocks every future component depending on useBreakpoint().
- [Phase 57-mobile-ux-deferred-items]: [57-03]: ProjectPicker testid renamed project-picker-trigger → open-project-picker with atomic landing-page.spec.ts update (8 refs); ExtensionAttachPicker testid renamed agent-config-form-attach-extensions → open-extension-attach-picker with atomic component test update (4 refs). Single source of truth — no dual-testid hack.
- [Phase 57-mobile-ux-deferred-items]: [57-03]: FilePicker mobile branch surfaces explicit trigger button (Pitfall 8 honored). SharedFilePicker's internal open is opaque to the shim; piping it would either break the shim contract or modify SharedFilePicker (forbidden). Trigger doubles as value display, mirroring native iOS form-input convention.
- [Phase 57-mobile-ux-deferred-items]: [57-03]: E2e fixmes preserved (Rule 3 deviation mirroring Plan 02) — (app)-prefixed routes need Docker auth fixture; un-fixme'ing converts ~40 cases to RED. Component-layer wrap verified by import BottomSheet + bp.below source-grep across all 9 pickers + Plan 02 BottomSheet.component.test.ts 8/8 GREEN. Phase 59 TEST-03 owns route+auth scaffolding.
- [Phase 57-mobile-ux-deferred-items]: [57-06]: Single REST endpoint (GET + PUT) at /api/user/agent-picker chosen over per-field routes; partial-body PUT wholesale-replaces each field. Short lists (~20 max each) make split-routes a negligible bandwidth win for double the surface area.
- [Phase 57-mobile-ux-deferred-items]: [57-06]: Orphan-trim ON READ (no background sweep) with no-write-amplification gate — re-persist via upsertSetting fires ONLY when pinned.length !== pinnedRawArr.length. Wave 0 test 5 locks the contract; protects settings-table write traffic on every picker open.
- [Phase 57-mobile-ux-deferred-items]: [57-06]: Settings-KV namespaced keys verbatim per CONTEXT.md: user:<id>:agentPicker:savedSearches and user:<id>:agentPicker:pinned. No new DB table, no Drizzle migration — JSONB column tolerates the per-user shape.
- [Phase 57-mobile-ux-deferred-items]: [57-06]: Pin button on each row uses onmousedown + e.stopPropagation + e.preventDefault — the pre-existing select handler is also onmousedown (fires BEFORE onclick), so the suggested onclick(e.stopPropagation) pattern in the plan wouldn't have suppressed select. Event-phase parity over pattern parity.
- [Phase 57-mobile-ux-deferred-items]: [57-06]: Click-outside detection extended with data-agent-picker-body wrapper guard — in-body affordances (Save/Pin/Unpin/Unsave) mousedown would otherwise trigger closeDropdown via document handler before the affordance fired. Affordance-aware click-outside is the bridge.
- [Phase 57-mobile-ux-deferred-items]: [57-06]: SavedSearch shape locked as { query: string, createdAt: number } (RESEARCH Claude's Discretion). Wave 0 test 2 asserts the shape; future i18n-friendly 'last saved' UI can format via formatTtl('past').
- [Phase 57-mobile-ux-deferred-items]: [57-06]: E2e cases remain test.fixme — same precedent as Plans 57-02/03/05. /agents lives under SvelteKit's (app) protected group requiring authenticated session + agent-config DB seed; non-docker playwright config has no auth setup. Server-route 9/9 GREEN is the binding regression contract.
- [Phase 57-mobile-ux-deferred-items]: [57-07]: GAP-57-A landed as .unit.test.ts (not .test.ts) — vitest.config.ts include glob requires the .unit.test.ts suffix for pure-utility tests without DOM. File content unchanged from VERIFICATION.md intent; frontmatter corrected pre-execution in commit b713304.
- [Phase 57-mobile-ux-deferred-items]: [57-07]: Source-text grep (node:fs + toContain) chosen over full mount-render for the 9-picker BottomSheet wrap regression — distinct prop/fetch setups across 9 pickers would add ~200 lines of mocks with no extra regression bite over the textual contract Plans 57-02/03 explicitly shipped.
- [Phase 57-mobile-ux-deferred-items]: [57-07]: fireEvent.mouseDown over fireEvent.click in GAP-57-B — AgentSearchPicker save/pin/unsave/unpin buttons use onmousedown so the affordance fires BEFORE the click-outside dropdown-close; fireEvent.click would silently no-op.
- [Phase 57-mobile-ux-deferred-items]: [57-07]: hasAttribute(false) over getAttribute()===null for GAP-57-C ABSENCE assertion — production emits data-chip-id={chipId}; Svelte 5 omits when undefined. hasAttribute is the precise contract and gives a clearer failure message if a refactor accidentally rendered the literal 'undefined' string.
- [Phase 58-01]: Pitfall 5 verbatim-38 lock — defaultErrnoRet === 38 (ENOSYS), NOT 1 (EPERM). EPERM breaks Bun JIT pkey_alloc fallback AND Python 3.12 glibc clock_gettime64 probe; test uses .toBe(38) not tolerant >0 check so a future flip fails loudly.
- [Phase 58-01]: Per-syscall action fields stay SCMP_ACT_LOG (redundant under default-deny enforce but documents explicit-allow-list-mirror intent). compile-seccomp.c parse_syscall_action threads them through but they are effectively no-ops with defaultAction=SCMP_ACT_ERRNO.
- [Phase 58-01]: SCMP_FLTATR_ACT_BADARCH hardcoded to SCMP_ACT_ERRNO(ENOSYS), NOT threaded from defaultErrnoRet. The badarch semantic is orthogonal to per-syscall errno_ret; ENOSYS is the only sensible answer regardless of the JSON's default.
- [Phase 58-01]: McpClient.getChildProcess() returns null for unsupported transports + future SDK shape changes (SDK does not expose transport._process publicly). Plan 55-03 deferred-items documented the stability risk; 12-line JSDoc accepts it, wire-up degrades-soft when null (soak signal goes quiet, nothing breaks).
- [Phase 58-01]: Defensive typeof guard on client.getChildProcess() in registry.ts — test fixtures stub McpClient with bare {connect, close, listTools, callTool} (mcp-registry.test.ts). Guard lets pre-Phase-58 stubs pass; production always carries the method. Avoided touching the existing test fixtures.
- [Phase 58-01]: spawnAt captured BEFORE client.connect() (not after this.mcpClients.set as plan suggested). connect() resolves <100ms typical but could be slower on cold MCPs; anchoring journalctl --since window to actual spawn time is correct semantically.
- [Phase 58-02]: Pitfall 1 lock — host-side veth naming is `mcp-<8hex>` (12 chars), ns-side is `mcp-<8hex>-ns` (15 chars at IFNAMSIZ ceiling). CONTEXT.md's `mcp-<8hex>-host` (17 chars) is REJECTED — kernel returns "name too long". Plan 03's orphan-sweep regex MUST match `/^mcp-[a-f0-9]{8}$/`, NOT the suffixed CONTEXT proposal.
- [Phase 58-02]: Open Question 1 resolution — 1-byte stdin handshake between host process and launcher. Host writes byte AFTER `ip link set <ns> netns <pid>` completes; launcher's `read -n 1` from FD 0 gates the `ip addr add eth0 ...` step. Without the handshake, the launcher races the host's netns-move and `ip addr add` fails with "no such device".
- [Phase 58-02]: Open Question 2 resolution — belt+suspenders slot release. Connect-failure path runs synchronous `ip link delete <hostSideName>` + releaseVethSlot BEFORE the throw; happy-path schedules same cleanup on childProc.exited. Slot is only safe to release AFTER host-side veth deletion — otherwise a re-spawn could pick the same /30 IP before the old peer is gone.
- [Phase 58-02]: HookedStdioClientTransport subclass instead of SDK monkey-patch. Overrides `start()` to fire onChildSpawned AFTER spawn AND BEFORE the SDK's `super.connect → transport.start` chain. Pre-Phase-58 specs (no hook) fall back to bare StdioClientTransport — zero behavior change.
- [Phase 58-02]: writeByte callback abstraction in onChildSpawned signature. mcp-sandbox.ts stays decoupled from SDK transport internals (no `_process` access leaking outside src/mcp/client.ts). client.ts owns the SDK escape-hatch reach.
- [Phase 58-02]: 60-MCP concurrent cap with 4-slot headroom (63 mathematical). In-memory `Set<number>` with lowest-free wins on alloc. No DB persistence — orphan veth sweep at boot (Plan 03) reconciles leftover host-side interfaces from a crashed prior process.
- [Phase 58-02]: Stage 2 success path emits MCP_VETH_CREATED (NOT MCP_NETNS_CREATED — two-action split per CONTEXT). Stage 1 keeps MCP_NETNS_CREATED for unmodified Phase 55 behavior. Operators reading /audit see two distinct actions for the two modes.
- [Phase 58-02]: Bridge-missing degrades gracefully to Stage 1. `ip link set <host-side> master br-ezcorp-mcp` failure cleans up the orphan host-side veth + releases the slot before falling through. Plan 03 owns ensureBridge.
- [Phase 58-02]: Stage 2 kill-switch boot row mirrors Stage 1 — fourth identical pattern (DNS_RECHECK, TMPFS, SECCOMP, STAGE2_VETH). Module-scope flag, exactly one row per process lifetime, MCP_NETNS_FALLBACK action with reason='kill-switch: stage2 veth disabled'.
- [Phase 58-02]: IPv6 disable in the launcher uses `|| true` mask (Plan 02 transitional). Plan 03 Task 2 hardens to strict abort-on-fail. Inline comment in mcp-launcher.sh documents the hand-off so the verifier doesn't flag Plan 03's rewrite as regression.
- [Phase 58-02]: Hardcoded `10.42.0.1` bridge gateway in mcp-sandbox.ts is pragmatic — Plan 03's bridge boot will own the canonical BRIDGE_CIDR_DEFAULT constant. Plan 02 stays minimum-diff.
- [Phase 58-03]: Pitfall 1 lock-step: orphan sweep regex /^mcp-[a-f0-9]{8}$/ (12 chars) NOT pre-correction mcp-<8hex>-host (17 chars rejected by IFNAMSIZ=15). ROADMAP RC#5 grep pattern lock-step corrected to match implementation (Blocker #1).
- [Phase 58-03]: Pre-spawn conntrack guard threshold is strict > 0.7 (not >=). Test case at exactly 70% passes through (no refusal); locks the contract against a future flip to >=. Conntrack floor 262144 is single constant (CONNTRACK_MAX_FLOOR); idempotent only-write-if-lower so re-runs don't spam audit.
- [Phase 58-03]: MCP_VETH_ORPHAN_SWEPT row UNCONDITIONALLY per boot (even count=0) — operator-visibility positive 'the sweep ran' signal beats 'no rows' silence. probeVethCapability honors stage2DegradedAtBoot AS A SHORT-CIRCUIT BEFORE cache lookup — every subsequent spawn consults the flag, no race possible.
- [Phase 58-03]: Launcher IPv6 disable strict abort-on-fail with discriminating exit codes (97 eth0, 96 lo) replaces Plan 02's transitional || true mask. RC#3 contract enforced at launcher level, not just nft filtering. Integration test cases for IPv6 (RC#3) + conntrack soak (RC#2) stay test.todo on dev hosts; full Stage 2 stack needs Linux+CAP_NET_ADMIN+bridge boot succeeded.
- [Phase 59-test-debt-repair]: TEST-05 projectIds audit assertion retro-claimed: already-shipped toEqual([proj-a, proj-b]) form at line 367 is strictly stronger than CONTEXT's expect.arrayContaining([...]) form; bun test → 17/17 GREEN on HEAD 83781b5; no source-code changes per plan's read-only contract
- [Phase 59]: Phase-start Playwright baseline captured: 1678 passing test cases at HEAD 83781b5dd7be, with per-test-case stable identifiers — Plan's literal jq selector emitted only .file (would yield 150 unique files, below min_lines:800 acceptance gate). Followed plan intent (diffable baseline for downstream comm -23 wave gates) by emitting file::line:column::title [projectName] per passing test case.
- [Phase 59]: Stash invariant preserved: 12 entries pre- and post-execution, top stash@{0}: TEMP-56-03-regression-check — Sacred baseline per memory feedback_agent_briefs_no_git_stash.md. Zero git stash operations performed during this plan.
- [Phase 59-test-debt-repair]: All 10 backend test verdicts auto-assigned as stale-assertion (user resume signal: auto-assign-all). 7 test-only fixes shipped; 3 files (#8/#9/#10) need no per-file edit — pollution closes via fix #4 mock-cleanup MODULE_PATHS expansion.
- [Phase 59]: Strict additive-only api-mocks.ts handler insertion before the L1305 default catch-all (post-Task-1 shifted to L1460). All new endpoint families are NEW if-blocks; zero edits to existing handlers preserves the 1678-line 59-01 baseline. — Phase-59 CONTEXT.md "Baseline-preserving discipline" mandates additive-only on api-mocks.ts to keep the 868→1678 v1.3-passing-spec baseline green. Adding new conditional blocks before the catch-all is the only safe way to introduce route handlers without risking shape drift on existing routes; reviewer gate verifies via `git diff` filtered for `^-` non-header lines.
- [Phase 60]: Plan 60-03: preserved 'Allow this time' button label in this commit — rename to 'Allow until restart' ships in plan 60-04 (atomic-commit-per-artifact policy)
- [Phase 60]: Plan 60-03: used verbatim wording locked in 60-CONTEXT.md L74-76 — no rewording during execution
- [Phase 60]: Force-add (`git add -f`) used for new .planning/ artifact — .planning/ is gitignored at L49, but PROJECT.md/ROADMAP.md/REQUIREMENTS.md/v1.4-backend-test-triage.md precedents are all tracked via force-add
- [Phase 60]: Plan-text correction (Rule 1) for f6c3790 web-route Task-2 verify: `*.server.test.ts` files under web/src/__tests__/ are vitest-owned (per web/scripts/test.sh filter); ran via `cd web && bunx vitest run` instead of `bun test`. Audit doc Verify: lines unaffected — only the auxiliary Task 2 verification command needed framework correction
- [Phase 60-audit-claim-docs-polish]: Plan 60-02: PROJECT.md table extended to 6 rows x 3 cols with audit cross-links (v1.4-retro-claim-audit.md#<sha>); SHA typos 763d738/763d778 purged from REQUIREMENTS.md + ROADMAP.md, all cite real 763d718. Two atomic commits (docs + chore) for blame scoping. CLAIM-01 fully closed.
- [Phase 60-audit-claim-docs-polish]: Plan 60-04: atomic 5-site rename Allow this time -> Allow until restart shipped in ONE commit (462fb58) per CONTEXT.md atomic-blast-radius policy; testid permission-allow-session unchanged for e2e regression stability
- [Phase 60-audit-claim-docs-polish]: Plan 60-04: vitest-from-web/ invocation used for component test (plan-text correction per project memory project_vitest_must_run_from_web_subdir.md - Bun runner lacks vi.stubGlobal); same pattern as plans 60-01 + 59-03
- [Phase 61-test-debt-followup-feature-rework-specs]: Plan 61-01: file-scope `mockAccountEndpoints(page)` helper preferred over `test.beforeEach` block (Rule-1 deviation from plan). Playwright runs `page.route` handlers in reverse-registration order; mockApi's `**/api/**` catch-all registered later in test body wins against beforeEach-registered narrower routes. Helper called inline AFTER `mockApi(...)` registers the narrower `/api/account` route LAST, winning resolution. Pattern reusable for any future (app)-route spec mounting `/account`.
- [Phase 61-test-debt-followup-feature-rework-specs]: Plan 61-01: debug doc `svelte5-layout-reactivity-2026-05-12.md` verdict `test-env-only` confirmed by live probe — Svelte 5 singleton-store reactivity is NOT broken. Root cause: `/account/+page.svelte:308` TypeError aborts effect scheduler mid-flush when `/api/account` returns `{}` from default catch-all. With proper mock + correct registration ordering, reactivity propagates correctly.
- [Phase 61-test-debt-followup-feature-rework-specs]: Plan 61-01: Layer 2 baseline-diff DEFERRED to 61-00 — `baseline-passing.txt` not yet captured on disk (61-00 is wave-0 baseline-capture plan, not yet executed). Single-file blast radius means 61-01's commit cannot regress Layer 2 within its scope; 61-00 capture against post-`ea3f8a6` HEAD retroactively closes the gap.
- [Phase 59-test-debt-repair]: Phase 59-05 used  additions only on SUT; 3 of 8 specs scoped back due to feature/setup drift exceeding test-only authority — fix-shape notes filed for follow-up SUT-touching plan
- [Phase 61]: Phase 61 COMPLETE: 8/8 Bucket A specs disposed (4 REPAIR via testid hardening + route pivots + sigil correction; 1 REWRITE Path A SwipeDrawer; 3 REPAIR with FIXME for chat-page streaming race) + 5/5 Bucket B Sidebar fixmes flipped via /api/account mock helper + deferred-items.md Svelte 5 entry retitled to test-env-only verdict; net +56 active passing tests across 9 specs; 1 documented pre-existing flake (swipe-drawer Escape chromium); zero baseline regressions; TEST-02 residual closed
- [Phase 59]: 59-07: Test-side stale-assertion fix authorized by user (approve-stale-fix-L248) — F-trio's tool:permission_request SSE payload omitted cardType, which the SUT handler at stores.svelte.ts:1066 explicitly overwrites; added cardType: 'terminal' to all 3 F-trio SSE payloads matching the working sibling pattern at sub-agent-permission-routing.spec.ts:168/217/262. 3 atomic per-flip commits (eeb049c/a909692/25201c8); +6 passing test cases; zero SUT changes; sacred-12-stash invariant preserved
- [Phase 62]: Refined plan's Edit 1 with visible-container locator-scope (page.getByTestId('swipe-drawer')) to dodge strict-mode collision when both desktop CSS-hidden + mobile drawer-mounted ConversationLists are in DOM. No .first() used.
- [Phase 62]: Reverted plan's Edit 2 (:85 preventive drawer-open guard) per plan's explicit revert-on-regression clause — SwipeDrawer backdrop intercepts pointer events on ChatHeader 'Conversation settings' button.
- [Phase 62]: 62-04: {:else} not-found branch at /agents/[name]/+page.svelte:436 uses agentName route param (not agent.name) because agent is undefined in else branch; rounded panel matches existing Run Agent / Test Conversations card style; no data-testid (regex /not found/i matches copy directly).
- [Phase 62]: 62-04: Disposition: FIX (product) trailer convention — Phase 62's ONLY product-code change uses FIX disposition (vs REPAIR for the 8 test-layer commits). Pattern: product changes use FIX, spec-side repairs use REPAIR, both cite debug docs.
- [Phase 62]: 62-04: Phase 61-02 § Rule 3 (Preview server rebuild + restart for SUT testid changes) confirmed to apply to ALL Svelte template-body edits, not just testid additions — Vite preview serves baked production bundle so stale server reuse caused initial test failure until kill+rebuild+restart.
- [Phase 62]: 62-07: Source-read test on (app)/+layout.svelte Agents nav link pins both ternary branches (Build at L190, Platform at L202) + defensive exactly-2-occurrence guard. Pattern mirrors Phase 49.1 layout-mobile-breakpoint.test.ts. New TEST-ADD (coverage) disposition trailer established for Phase 62 net-new coverage plans, distinct from REPAIR (test-layer) and FIX (product).
- [Phase 62]: Plan 62-06: Stub the full /api/models + /api/models/capabilities + /api/modes + /api/agent-configs/generate quartet (with most-specific-first if-ladder ordering since /api/models is a substring prefix of /api/models/capabilities) + __resetCapabilityCacheForTests in beforeEach + wait for button[aria-label='Send message']:not(:disabled) before fireEvent.keyDown to drive any ChatInput-based component test past its model-autoselect submit gate; pattern reusable for future MetaAgentChat / ChatInput-derived coverage plans
- [Phase 63-indexing-primitives]: 63-01: EMBEDDING_MODEL_ID is the single source of truth for model+dim identity; later plans import it, never re-literal
- [Phase 63-indexing-primitives]: 63-01: token-aware chat chunker (256/32-overlap, stride 224) is separate from char-based chunking.ts (KB files); IDX-06 truncation is input-only via extractor opts
- [Phase 63]: 63-03: First db.transaction() in the codebase wraps createMessage (insert + conversation touch + eligibility-gated outbox enqueue) atomically; updateMessageContent re-enqueues edits via message_id-PK upsert. enqueueEmbedJob never calls getDb() (runs on the passed-in tx).
- [Phase 64-embed-on-write-worker]: NULL sentinel for next_attempt_after (no DEFAULT) — NULL = never backed off; presence = backoff timestamp
- [Phase 64-embed-on-write-worker]: DrainDb structural type mirrors EmbedJobTx pattern — module never calls getDb() internally
- [Phase 64-embed-on-write-worker]: claimBatch uses subquery UPDATE (SELECT...LIMIT inside WHERE IN) — PGlite does not support UPDATE...LIMIT
- [Phase 64-embed-on-write-worker]: runBacklogRecovery is a standalone exported function — allows tests to call it without a worker instance
- [Phase 64-embed-on-write-worker]: Sequential embed loop enforced (no Promise.all) to honor Transformers.js singleton constraint
- [Phase 64-embed-on-write-worker]: mock.module paths in test file use '../memory/...' (one level up from src/__tests__/), not '../../memory/...' which overshoots src/
- [Phase 65]: 65-01: SRCH-05 needs SET hnsw.iterative_scan='relaxed_order' + a single-table ANN scan (message_chunks alone, conversation_id=ANY(ARRAY(scoped ids)), role/DISTINCT/joins moved OUTSIDE) to drive idx_message_chunks_embedding on PGlite 0.3.16/pgvector 0.8.0 — the plan's 'default behavior, no GUC' premise was live-probed FALSE.
- [Phase 65]: 65-01: Wave 2 (65-02) builds /api/search/messages directly on searchMessages({projectId, query, mode, queryEmbedding, userId?, limit?, offset?}) → MessageSearchHit[]. searchMessages itself SETs the iterative_scan GUC and returns [] for a <2-char query or a null queryEmbedding in a vector-needing mode. RRF_K=60, exported. Snippet asymmetry: <mark> (ts_headline) for lexical/both, plain 35-word slice for semantic-only.
- [Phase 65]: 65-01: PGlite-seed quirks for the route test — raw `INSERT INTO message_chunks` MUST pass an explicit id (raw SQL skips drizzle $defaultFn); a 'lexical-only' row needs a NULL embedding (ANN returns nearest-K regardless of distance, so a 'far' vector still surfaces in a tiny corpus); the SRCH-05 EXPLAIN proof needs a LARGE corpus (~2.5k chunks) for the HNSW cost crossover, NOT the plan's ≥100 floor.
- [Phase 65]: 65-02: limit/offset handler-clamped to [1,50]/[0,inf) (not zod-rejected); only unknown mode 400s via zod enum
- [Phase 65]: 65-02: degraded gate is server-owned — hybrid/semantic fall back to keyword (degraded:true, servedMode:keyword) on embedder-down or throw; clients always get a 200 envelope
- [Phase 66]: 66-01: deep-link test runs under vitest (.unit.test.ts) not bun test — resolveDeepLink transitively imports Svelte-rune inline-tool-store via load-messages.ts
- [Phase 66]: 66-01: window-grow policy grows directly to distanceFromTail (minimal covering window); distanceFromTail = path.length - idx (pathToRoot is root->leaf, tail=1)
- [Phase 66]: 66-04: deep-link must consume ?m= reactively (not onMount-only) because the chat route is a persistent non-remounting ChatThread — the sidebar click-journey is a client goto, not a fresh mount

### Pending Todos

None tracked yet. Use `/gsd:add-todo` to capture v1.4 ideas during execution.

### Blockers/Concerns

**Phase 58 (MCP Stage 2) prerequisites — verify during Phase 55 planning:**
- `CAP_NET_ADMIN` availability in `oven/bun:1-slim` Docker base image. If not granted by default, deployment doc must require `--cap-add NET_ADMIN`.
- Ephemeral veth naming convention (`veth-mcp-<extId>-<spawnUuid>`) and boot-time orphan sweep — guards against unclean-restart collisions.
- `nf_conntrack_max ≥ 131072` boot probe — Docker default of 65536 is insufficient under MCP churn.
- IPv6 leak guard via `sysctl net.ipv6.conf.<veth-ns>.disable_ipv6=1` inside the namespace.

**Phase 57 (Mobile UX) verification needed during planning:**
- Smoke-test `@abhivarde/svelte-drawer` in one picker before refactoring all 9 (library is 1 month old per research).
- Check `svelte-dnd-action` presence in `web/package.json` — falls back to native HTML5 DnD if missing.

**v1.3 carried-forward tech debt (covered by v1.4):**
- ✓ Bun 1.3.11 segfault tests (af1-mcp-sandbox-regression, mcp-api-routes, mcp-e2e) — explicitly out of scope per REQUIREMENTS.md (BUN-01 deferred to v1.5).
- ✓ ~8 pre-existing test failures — covered by TEST-04 in Phase 59.
- ✓ Capability expiry v1.5 (per-cap TTL banner) — covered by TTL-01 in Phase 56.
- ✓ Phase 49 mobile UX deferred items — covered by UX-01..04 in Phase 57.

## Session Continuity

Last session: 2026-05-29T22:18:41.995Z
Stopped at: Completed 66-04-PLAN.md
Resume: Plan 56-02 (UI + endpoints) is unblocked — wires `buildAlwaysAllowValue(allowed, now, { ttlOverrideMs, expiresAt })` at the reapprove endpoint + first-time-grant write site, and surfaces `readTtlOverrideMs(row.value)` at admin/UI read sites. Plan 56-03 (formatTtl + sticky KV) is unblocked — `expiresAt` is the materialized timestamp formatTtl renders; sticky KV pattern writes to settings (orthogonal to the always-allow row). Phase 57 (mobile UX) remains parallelizable per v1.4 DAG. Phase 58 still blocked on ≥7-day clean seccomp soak signal. v1.3 deferred items still recorded in 55-03-SUMMARY.md.
