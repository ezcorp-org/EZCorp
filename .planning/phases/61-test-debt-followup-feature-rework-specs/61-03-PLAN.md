---
phase: 61-test-debt-followup-feature-rework-specs
plan: 03
type: execute
wave: 3
depends_on: ["61-00", "61-02"]
files_modified:
  - web/e2e/teams.spec.ts
  - web/e2e/swipe-drawer.spec.ts
  - web/e2e/menu-keyboard-nav.spec.ts
  - web/e2e/mobile-navigation.spec.ts
  - web/src/routes/(app)/settings/+page.svelte
  - .planning/phases/59-test-debt-repair/deferred-items.md
  # Optional SUT testid additions (if needed; surface in-flight):
  # - web/src/lib/components/SwipeDrawer.svelte (testid additions only if missing)
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "Bucket A #5 `teams.spec.ts` is disposed: passing tests via `data-testid=\"team-expand-{team.id}\"` (or equivalent) on settings expand-button at `web/src/routes/(app)/settings/+page.svelte:710`, OR explicit `test.fixme` with UN-BLOCKER comment if a deeper interaction-flow refactor is required. Disposition recorded in commit body."
    - "Bucket A #2 `swipe-drawer.spec.ts` is disposed: hamburger-flow tests navigate to a non-chat route (e.g., `/agents` or `/settings`) so the mobile header renders (`(app)/+layout.svelte:360 {#if !isChatRoute}`), OR use the chat-route equivalent button. Disposition recorded."
    - "Bucket A #3 `menu-keyboard-nav.spec.ts` is disposed: classified after INVESTIGATE (one test in isolation with `--trace=on`); REPAIR via per-test mock override OR spec timing fix, OR FIXME with UN-BLOCKER comment if a real SUT mention-search bug is confirmed. Disposition recorded."
    - "Bucket A #1 `mobile-navigation.spec.ts` is disposed: REWRITE Path A (default) — tests exercise SwipeDrawer behavior (hamburger opens drawer, Dashboard/Chat/Settings links inside drawer are reachable, backdrop closes drawer), replacing tab-bar coverage with drawer-link coverage. If user redirects to Path B during execution, disposition flips to FIXME with UN-BLOCKER comment citing the MobileTabBar restoration follow-up."
    - "Closing-commit task updates `.planning/phases/59-test-debt-repair/deferred-items.md`: § \"Svelte 5 singleton-store reactivity bug\" entry retitled to \"test-env: missing /api/account mock — 3-line per-test fix shipped in Phase 61, fixmes flipped\" with citation to `.planning/debug/svelte5-layout-reactivity-2026-05-12.md` verdict path."
    - "Each per-spec commit body records the spec's Phase 61 disposition verdict (REPAIR / REWRITE / FIXME) so Layer 3 audit (`git log --grep=\"Phase 61\\|Disposition:\"`) finds 8 dispositions across all of Phase 61's 8 Bucket A commits."
    - "Baseline title-diff invariant remains green after EACH per-spec commit AND after the closing-commit (Layer 2 of `61-VALIDATION.md`)."
    - "No timeout widening, no `git stash`, no `--no-verify`, no global `setupApiMocks()` edits."
    - "Every retained or newly-added `test.fixme` has an UN-BLOCKER comment block within the 10 preceding lines (Layer 4 of `61-VALIDATION.md`)."
    - "Stash invariant preserved: `git stash list | wc -l` returns 12 pre- and post-plan."
  artifacts:
    - path: "web/e2e/teams.spec.ts"
      provides: "Settings expand-button assertions use testid (or stable equivalent); spec passes OR is `test.fixme`'d with UN-BLOCKER."
    - path: "web/e2e/swipe-drawer.spec.ts"
      provides: "Hamburger-flow tests navigate to non-chat route OR use chat-route equivalent; spec passes."
    - path: "web/e2e/menu-keyboard-nav.spec.ts"
      provides: "Mention-search classified after INVESTIGATE; spec passes OR `test.fixme`'d with UN-BLOCKER."
    - path: "web/e2e/mobile-navigation.spec.ts"
      provides: "Tests REWRITTEN to exercise SwipeDrawer behavior (Path A); spec passes."
    - path: "web/src/routes/(app)/settings/+page.svelte"
      provides: "`data-testid=\"team-expand-{team.id}\"` (or equivalent) on team-expand button at L710 (testid-only addition)."
    - path: ".planning/phases/59-test-debt-repair/deferred-items.md"
      provides: "Svelte 5 entry retitled to reflect test-env-only verdict; cites debug doc verdict path."
      contains: "test-env"
  key_links:
    - from: "Each per-spec commit body"
      to: "Disposition verdict trailer + deferred-items.md entry citation"
      via: "git commit body line"
      pattern: "Disposition: (REPAIR|REWRITE|FIXME)"
    - from: ".planning/phases/59-test-debt-repair/deferred-items.md § Svelte 5 entry"
      to: ".planning/debug/svelte5-layout-reactivity-2026-05-12.md"
      via: "doc-edit citing verdict path"
      pattern: "svelte5-layout-reactivity-2026-05-12"
    - from: "Layer 3 disposition audit"
      to: "8 Bucket A specs touched + 8 dispositions across Phase 61"
      via: "git log --oneline --name-only main..HEAD | grep <8 specs>"
      pattern: "Phase 61|Disposition:"
---

<objective>
Repair the 4 complex Bucket A specs from `deferred-items.md` — each requiring per-test investigation, route-flow rewrites, or product-shape decisions — and close Phase 61 with the deferred-items.md retitling commit.

The 4 specs (researcher's cheap-first order per CONTEXT.md L69):
1. **`teams.spec.ts`** (Bucket A #5) — REPAIR via `data-testid` on settings expand button at `(app)/settings/+page.svelte:710`.
2. **`swipe-drawer.spec.ts`** (Bucket A #2) — REPAIR via route pivot (hamburger tests navigate to non-chat route, since `{#if !isChatRoute}` hides mobile header on chat routes per Pitfall 3).
3. **`menu-keyboard-nav.spec.ts`** (Bucket A #3) — INVESTIGATE first (capture `/api/mentions/search` trace via `--trace=on` for one test in isolation), then REPAIR (per-test mock override OR spec timing fix) or FIXME (with UN-BLOCKER citing real SUT mention-search bug).
4. **`mobile-navigation.spec.ts`** (Bucket A #1) — REWRITE Path A (default per CONTEXT.md L77-80). Replace tab-bar assertions with SwipeDrawer behavior tests against the actual current SUT at `(app)/+layout.svelte:407-444`. If user redirects to Path B during review, flip to FIXME with UN-BLOCKER citing MobileTabBar restoration.

After the 4 spec dispositions land, the final task updates `deferred-items.md` (retitle Svelte 5 entry to reflect `test-env-only` verdict) per CONTEXT.md L56 + the closing-commit requirements.

Purpose: closes the remaining 4 of 8 Bucket A specs (the complex ones requiring deeper investigation or rewrites) and ships the deferred-items.md re-categorization that keeps the audit trail accurate. This plan completes Phase 61.

Output: up to 5 atomic commits (4 spec dispositions + 1 deferred-items.md retitling), each citing the relevant `deferred-items.md` entry and recording its disposition verdict.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-test-debt-followup-feature-rework-specs/61-CONTEXT.md
@.planning/phases/61-test-debt-followup-feature-rework-specs/61-RESEARCH.md
@.planning/phases/61-test-debt-followup-feature-rework-specs/61-VALIDATION.md
@.planning/phases/61-test-debt-followup-feature-rework-specs/baseline-passing.txt
@.planning/phases/61-test-debt-followup-feature-rework-specs/baseline-meta.txt
@.planning/phases/59-test-debt-repair/deferred-items.md
@.planning/debug/svelte5-layout-reactivity-2026-05-12.md

<interfaces>
<!-- Pattern 4 — settings expand-button testid (teams #5). -->

```svelte
<!-- Source: web/src/routes/(app)/settings/+page.svelte:710 (pre-edit) -->
<button onclick={() => toggleTeamExpand(team.id)} class="flex-1 text-left ...">{team.name}</button>

<!-- POST-EDIT (testid-only addition; data-testid uses Svelte template expression to interpolate team.id at render): -->
<button data-testid="team-expand-{team.id}" onclick={() => toggleTeamExpand(team.id)} class="flex-1 text-left ...">{team.name}</button>
```

Spec-side swap:
```typescript
// Before: await page.getByText("Engineering").click();   // may match wrong ancestor (Pitfall 4)
// After:
await page.getByTestId("team-expand-team-1").click();    // direct hit on the button
```

<!-- Pattern 8 — swipe-drawer chat-route flow pivot (swipe-drawer #2). -->

```typescript
// Before (FAILS — chat route hides mobile header per (app)/+layout.svelte:360 {#if !isChatRoute}):
await page.goto(`/project/${proj.id}`);   // redirects to /project/${id}/chat
const hamburger = page.getByRole("button", { name: "Open menu" });   // HIDDEN on chat route
await hamburger.click();

// After (PASS — navigate to non-chat route so mobile header renders):
await page.goto(`/agents`);   // or `/settings`, `/extensions` — any non-chat (app) route
const hamburger = page.getByRole("button", { name: "Open menu" });
await hamburger.click();
const drawer = page.getByTestId("swipe-drawer");
await expect(drawer).toBeVisible();
```

Verified: `(app)/+layout.svelte:142 let isChatRoute = $derived(page.url.pathname.includes('/chat'));` and L360 `{#if !isChatRoute}` gates the mobile header. Tests #1-4 in `swipe-drawer.spec.ts` (mobile hamburger) are affected; tests #5-7 (`Open conversations` hamburger) already use chat route which is correct (those should remain untouched).

<!-- Pattern 9 — menu-keyboard-nav INVESTIGATE (menu-keyboard-nav #3). -->

INVESTIGATE step (before classifying):
```bash
cd web && bunx playwright test menu-keyboard-nav.spec.ts -g "Tab selects the first highlighted mention item" --trace=on --workers=1
# Open the resulting trace.zip — inspect /api/mentions/search request/response
```

Expected outcomes:
- **REPAIR (most likely):** Mock-shape or `type` query-param mismatch → fix via per-test `page.route("**/api/mentions/search**", ...)` override returning expected items.
- **FIXME:** Real SUT bug in mention-search handler at `api-mocks.ts:1280` returning empty for valid seeded data → file as follow-up; mark each affected test `test.fixme` with UN-BLOCKER comment.

UN-BLOCKER comment shape if FIXME chosen (canonical per CONTEXT.md L106-114):
```typescript
// UN-BLOCKER CONDITION: <one-line condition that flips this active>.
//   Example: "/api/mentions/search returns seeded extensions when type=ext"
// Reference: .planning/phases/59-test-debt-repair/deferred-items.md § menu-keyboard-nav.spec.ts
// Filed-on: 2026-05-13 (Phase 61)
test.fixme("...", async ({ page }) => { ... });
```

<!-- Pattern 6 — mobile-navigation REWRITE Path A (mobile-navigation #1). -->

Verified SUT surface at `(app)/+layout.svelte:407-444`:
- `<SwipeDrawer ariaLabel="Mobile navigation">` (the same `aria-label="Mobile navigation"` the old tab-bar tests asserted — re-anchor to the SwipeDrawer container).
- `navLinks` array at `(app)/+layout.svelte:184-208` containing Home / Chat / Settings / Marketplace — the same set the original spec asserted.
- Testid surface: `swipe-drawer` (panel: `swipe-drawer-panel`, backdrop: `swipe-drawer-backdrop`) per `SwipeDrawer.svelte:207,219,231`.
- Hamburger: `getByTestId("mobile-menu-toggle")` OR `getByRole("button", { name: "Open menu" })` — verify in `(app)/+layout.svelte`.

REWRITE template (replace each tab-bar test body with SwipeDrawer-equivalent):
```typescript
test("mobile drawer opens via hamburger and shows nav links", async ({ page, mockApi }) => {
    const proj = makeProject();
    await mockApi({ projects: [proj] });
    await page.setViewportSize(MOBILE_VIEWPORT);
    await page.goto(`/agents`);   // non-chat (app) route so mobile header renders

    const hamburger = page.getByTestId("mobile-menu-toggle");
    await expect(hamburger).toBeVisible();
    await hamburger.click();

    const drawer = page.getByTestId("swipe-drawer");
    await expect(drawer).toBeVisible();

    const panel = page.getByTestId("swipe-drawer-panel");
    await expect(panel.getByRole("link", { name: "Home" })).toBeVisible();
    await expect(panel.getByRole("link", { name: "Chat" })).toBeVisible();
    await expect(panel.getByRole("link", { name: "Settings" })).toBeVisible();

    // backdrop closes drawer
    await page.getByTestId("swipe-drawer-backdrop").click();
    await expect(drawer).not.toBeVisible();
});
```

(The exact test bodies depend on what the original tab-bar tests asserted — preserve the *intent* of each test while pivoting the *surface* from tab-bar to SwipeDrawer.)

<!-- Deferred-items.md retitling (closing-commit task). -->

Current entry (from .planning/phases/59-test-debt-repair/deferred-items.md L10-41):
```markdown
## 59-06 — Sidebar/Layout + Standalone Cluster (TEST-02)

### Discovered: Svelte 5 singleton-store reactivity bug in `(app)/+layout.svelte`
...
```

Retitle to (per CONTEXT.md L56):
```markdown
## 59-06 — Sidebar/Layout + Standalone Cluster (TEST-02)

### test-env: missing /api/account mock — 3-line per-test fix shipped in Phase 61, fixmes flipped

**Verdict reclassification (2026-05-13):** Originally filed as "Svelte 5 singleton-store reactivity bug",
this entry was reclassified to `test-env-only` per the debug investigation at
`.planning/debug/svelte5-layout-reactivity-2026-05-12.md`. The Svelte 5 reactivity is fine;
the root cause was `/account/+page.svelte:308` reading `account.name.charAt(0)` after `onMount`
fetches `/api/account`, which returned `{}` from the api-mocks default catch-all. The thrown
`TypeError` aborted Svelte 5's effect scheduler mid-flush, leaving subsequent reactive updates
unrendered.

**Fix shipped in Phase 61-01:** Single atomic commit added a `test.beforeEach` to the `Sidebar`
describe block in `web/e2e/theme-sidebar.spec.ts` mocking `/api/account`, `/api/account/sessions`,
`/api/account/login-history` with canonical AccountData / empty payloads. All 5 fixme'd tests
flipped to active passing tests. No SUT change required.

**Original entry preserved for audit trail:**

> [original content from L10-41]
```

(Retain the original entry body below the retitled header so the audit trail is intact; just add the new "Verdict reclassification" preamble at the top.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REPAIR teams.spec.ts (Bucket A #5 — settings expand-button testid)</name>
  <files>web/e2e/teams.spec.ts, web/src/routes/(app)/settings/+page.svelte</files>
  <action>
1. **Verify the expand-button shape at `(app)/settings/+page.svelte:710`:**
   ```bash
   sed -n '705,720p' 'web/src/routes/(app)/settings/+page.svelte'
   ```
   Expected: a `<button onclick={() => toggleTeamExpand(team.id)} ...>{team.name}</button>` element. Note exact attributes.

2. **Add `data-testid="team-expand-{team.id}"` to the team-name expand button** (testid-only attribute addition; Svelte interpolates `{team.id}` at render). DO NOT modify the onclick handler, class, or button text content.

3. **Update `web/e2e/teams.spec.ts`** to use the new testid:
   - Replace `page.getByText("<team-name>").click()` (or similar) with `page.getByTestId("team-expand-<team-id>").click()`.
   - Verify the test fixture seeds team ids that match the testid pattern (e.g., if fixture seeds `team.id = "team-1"`, the testid is `team-expand-team-1`).
   - If the spec uses a parametric helper, update it to take `teamId` and call `page.getByTestId(\`team-expand-${teamId}\`)`.

4. **Run the spec in isolation:**
   ```bash
   cd web && bunx playwright test teams.spec.ts --workers=1 --reporter=line
   ```
   Expected: zero failures, zero timeouts.
   - If failures persist due to a deeper interaction-flow refactor (the +115-line settings expansion may have changed how `toggleTeamExpand` behaves — e.g., expand now requires an async data fetch), classify as **FIXME**:
     - Add an UN-BLOCKER comment block above each affected test citing `deferred-items.md § teams.spec.ts`.
     - Convert `test(...)` → `test.fixme(...)`.
     - Surface to user; do NOT widen timeouts.

5. **Run regression diff (Layer 2).** Expected: 0 missing entries.

6. **Verify SUT diff is testid-only:**
   ```bash
   git diff -- 'web/src/routes/(app)/settings/+page.svelte' | grep -E '^[+-]' | grep -v 'data-testid' | grep -v '^[+-]\{3\}'
   # Expected: empty (only data-testid addition)
   ```

7. **Commit atomically:**
   - If REPAIR: `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "test(61-03): scope teams.spec.ts via team-expand testid on settings/+page.svelte" --files web/e2e/teams.spec.ts 'web/src/routes/(app)/settings/+page.svelte'`
     - Body: `Disposition: REPAIR (Bucket A #5, testid on settings expand-button)` + cite `deferred-items.md § teams.spec.ts`.
   - If FIXME: commit with body trailer `Disposition: FIXME (Bucket A #5, deeper interaction-flow refactor required)` + UN-BLOCKER comment block.

8. **Post-commit:** `git stash list | wc -l` returns 12.
  </action>
  <verify>
    <automated>cd web && bunx playwright test teams.spec.ts --workers=1 --reporter=json > /tmp/spec-result.json 2>&1; cd /home/dev/work/EZCorp/ez-corp-ai && test "$(jq '.suites[].specs[].tests[].results[].status' /tmp/spec-result.json | grep -E '"failed"|"timedOut"' | wc -l)" = "0" && test "$(git stash list | wc -l)" = "12" && awk '/UN-BLOCKER CONDITION/{ub=NR} /test\.fixme/{if(ub<NR-10||ub==0) print FILENAME":"NR}' web/e2e/teams.spec.ts | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>
- `teams.spec.ts` passes on chromium + mobile-chromium, OR FIXME'd with UN-BLOCKER comment block on each affected test.
- `(app)/settings/+page.svelte:710` has `data-testid="team-expand-{team.id}"` (testid-only addition).
- Atomic commit with `Disposition: REPAIR (Bucket A #5)` or `Disposition: FIXME (Bucket A #5)` trailer + deferred-items.md citation.
- Baseline regression diff returns 0 missing entries.
- `git stash list | wc -l` returns 12.
  </done>
</task>

<task type="auto">
  <name>Task 2: REPAIR swipe-drawer.spec.ts (Bucket A #2 — chat-route flow pivot)</name>
  <files>web/e2e/swipe-drawer.spec.ts</files>
  <action>
1. **Identify the affected tests** — per RESEARCH.md L309: tests L46-61, L63-76, L78-92, L94-109 (mobile hamburger flow). Tests #5-7 (`Open conversations` hamburger, already chat-route) are CORRECT and must NOT be modified.

2. **For each affected test**, change the navigation target from a chat-redirecting route (`/project/${proj.id}` redirects to `/project/${id}/chat`) to a non-chat `(app)` route so the mobile header (and hamburger button) renders. Recommended target: `/agents` (already mocked) or `/settings` (already mocked). Avoid `/account` (would need the Bucket B `/api/account` mock — unnecessary churn).

   ```typescript
   // Before:
   await page.goto(`/project/${proj.id}`);   // redirects to /chat, hides mobile header
   const hamburger = page.getByRole("button", { name: "Open menu" });   // never visible

   // After:
   await page.goto(`/agents`);   // mobile header renders here
   const hamburger = page.getByRole("button", { name: "Open menu" });
   await expect(hamburger).toBeVisible();
   await hamburger.click();
   ```

3. **Preserve the test INTENT** — each test asserts the SwipeDrawer behavior (open via hamburger, backdrop close, nav-link close, Escape close). Only the navigation target changes; the drawer interaction assertions remain the same.

4. **Verify `/agents` (or chosen target) is fully mocked by `setupApiMocks()`:**
   ```bash
   grep -n "/api/agents\|/api/agent-configs" web/e2e/fixtures/api-mocks.ts | head -5
   # Expected: at least one handler. If /agents has an unguarded fetch not in api-mocks, surface — do NOT add a per-test mock if Pitfall 2 applies elsewhere.
   ```

5. **Run the spec in isolation:**
   ```bash
   cd web && bunx playwright test swipe-drawer.spec.ts --workers=1 --reporter=line
   ```
   Expected: zero failures, zero timeouts. All 7 tests (4 modified + 3 unchanged chat-route tests) pass.
   - If failures persist on the modified tests: capture trace; the chosen non-chat route may have its own onMount fetch dependency. Surface — DO NOT widen timeouts.

6. **Run regression diff (Layer 2).** Expected: 0 missing entries.

7. **Verify zero SUT touches** (this disposition is spec-only):
   ```bash
   git diff -- web/src/ web/e2e/fixtures/
   # Expected: empty
   ```

8. **Commit atomically:**
   ```bash
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "test(61-03): pivot swipe-drawer.spec.ts hamburger tests to non-chat route" --files web/e2e/swipe-drawer.spec.ts
   ```
   Body: `Disposition: REPAIR (Bucket A #2, route-pivot — chat routes hide mobile header per (app)/+layout.svelte:360 {#if !isChatRoute})` + cite `deferred-items.md § swipe-drawer.spec.ts (mobile chat-route flow)`.

9. **Post-commit:** `git stash list | wc -l` returns 12.
  </action>
  <verify>
    <automated>cd web && bunx playwright test swipe-drawer.spec.ts --workers=1 --reporter=json > /tmp/spec-result.json 2>&1; cd /home/dev/work/EZCorp/ez-corp-ai && test "$(jq '.suites[].specs[].tests[].results[].status' /tmp/spec-result.json | grep -E '"failed"|"timedOut"' | wc -l)" = "0" && test "$(git stash list | wc -l)" = "12"</automated>
  </verify>
  <done>
- `swipe-drawer.spec.ts` passes on chromium + mobile-chromium (all 7 tests including the 3 unmodified chat-route tests).
- 4 hamburger-flow tests navigate to a non-chat `(app)` route (e.g., `/agents` or `/settings`) instead of `/project/${id}` (which redirects to chat).
- Zero SUT touches; spec-only change.
- Atomic commit with `Disposition: REPAIR (Bucket A #2, route-pivot)` trailer + deferred-items.md citation.
- Baseline regression diff returns 0 missing entries.
- `git stash list | wc -l` returns 12.
  </done>
</task>

<task type="auto">
  <name>Task 3: INVESTIGATE-then-DISPOSE menu-keyboard-nav.spec.ts (Bucket A #3)</name>
  <files>web/e2e/menu-keyboard-nav.spec.ts</files>
  <action>
1. **Capture the mention-search request/response for ONE test in isolation** (per RESEARCH.md Pattern 9 + Open Question 2):
   ```bash
   cd web && bunx playwright test menu-keyboard-nav.spec.ts -g "Tab selects the first highlighted mention item" --trace=on --workers=1
   # If that test name doesn't exist, pick the first `@`-typed mention test.
   ```
   - Open the resulting trace (`web/test-results/.../trace.zip`) in the Playwright UI: `bunx playwright show-trace web/test-results/<dir>/trace.zip`.
   - Inspect:
     - The `/api/mentions/search?type=...&q=...` request — what `type` and `q` params does the SUT send?
     - The response payload — does the api-mocks handler at `api-mocks.ts:1280` return the seeded items, or empty?
     - The DOM state after typing `@` — does `#mention-item-0` ever render?

2. **Classify based on evidence:**

   **Outcome A — REPAIR via per-test mock override (most likely per RESEARCH.md):**
   The api-mocks default handler returns items but the test asserts on a specific shape the handler doesn't produce; OR the `type` query-param routes to a branch returning empty. Fix via per-test `page.route("**/api/mentions/search**", route => route.fulfill({ json: { items: [...] } }))` returning the canonical seeded shape. Apply at the top of each affected test or in a `beforeEach`.

   **Outcome B — REPAIR via spec timing fix:**
   The popover hasn't rendered when the assertion fires. Add a `await expect(page.getByTestId("mention-popover")).toBeVisible()` (or equivalent) before the keyboard-nav assertion. Use existing testids; do not add new ones unless surfacing to user first.

   **Outcome C — FIXME with UN-BLOCKER:**
   Real SUT mention-search bug confirmed (e.g., the search-mock receives the request but returns empty for valid seeded data due to a missed branch — Pitfall 7). Add UN-BLOCKER comment to each affected test:
   ```typescript
   // UN-BLOCKER CONDITION: /api/mentions/search returns seeded extensions/agents when type=ext or type=agent.
   // Reference: .planning/phases/59-test-debt-repair/deferred-items.md § menu-keyboard-nav.spec.ts
   // Filed-on: 2026-05-13 (Phase 61)
   test.fixme("...", async ({ page }) => { ... });
   ```
   File a follow-up todo at `tasks/v1.4-mention-search-bug.md` (or surface to user for handling).

3. **Apply the chosen fix** (REPAIR via mock override / REPAIR via timing / FIXME). Do NOT widen timeouts (CONTEXT.md L70).

4. **Run the spec in isolation:**
   ```bash
   cd web && bunx playwright test menu-keyboard-nav.spec.ts --workers=1 --reporter=line
   ```
   - If REPAIR: zero failures, zero timeouts expected.
   - If FIXME: zero failures, zero timeouts (fixme'd tests are skipped, not failed) AND every retained `test.fixme` has an UN-BLOCKER comment within 10 preceding lines.

5. **Run regression diff (Layer 2).** Expected: 0 missing entries.

6. **Verify FIXME audit (Layer 4):**
   ```bash
   awk '/UN-BLOCKER CONDITION/{ub=NR} /test\.fixme/{if(ub<NR-10||ub==0) print FILENAME":"NR": fixme without UN-BLOCKER"}' web/e2e/menu-keyboard-nav.spec.ts
   # Expected: empty output
   ```

7. **Commit atomically:**
   ```bash
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "test(61-03): <repair|fixme> menu-keyboard-nav after mention-search investigation" --files web/e2e/menu-keyboard-nav.spec.ts
   ```
   Body MUST include:
   - Disposition: `REPAIR (Bucket A #3, mock-override)` / `REPAIR (Bucket A #3, timing-fix)` / `FIXME (Bucket A #3, real SUT mention-search bug)`.
   - Brief investigation result (~2 lines) — what the trace showed.
   - Citation: `Reference: .planning/phases/59-test-debt-repair/deferred-items.md § menu-keyboard-nav.spec.ts`.

8. **Post-commit:** `git stash list | wc -l` returns 12.
  </action>
  <verify>
    <automated>cd web && bunx playwright test menu-keyboard-nav.spec.ts --workers=1 --reporter=json > /tmp/spec-result.json 2>&1; cd /home/dev/work/EZCorp/ez-corp-ai && test "$(jq '.suites[].specs[].tests[].results[].status' /tmp/spec-result.json | grep -E '"failed"|"timedOut"' | wc -l)" = "0" && test "$(git stash list | wc -l)" = "12" && test "$(awk '/UN-BLOCKER CONDITION/{ub=NR} /test\.fixme/{if(ub<NR-10||ub==0) print FILENAME":"NR}' web/e2e/menu-keyboard-nav.spec.ts | wc -l)" = "0"</automated>
  </verify>
  <done>
- `menu-keyboard-nav.spec.ts` passes (REPAIR) OR every retained `test.fixme` has UN-BLOCKER comment within 10 preceding lines (FIXME).
- INVESTIGATE step produced a documented finding (in commit body); classification chosen with evidence.
- Atomic commit with explicit `Disposition: REPAIR (Bucket A #3, <reason>)` OR `Disposition: FIXME (Bucket A #3, <reason>)` trailer + deferred-items.md citation.
- No timeout widening, no global `setupApiMocks()` edits.
- Baseline regression diff returns 0 missing entries.
- `git stash list | wc -l` returns 12.
  </done>
</task>

<task type="auto">
  <name>Task 4: REWRITE mobile-navigation.spec.ts (Bucket A #1 — Path A default: SwipeDrawer behavior)</name>
  <files>web/e2e/mobile-navigation.spec.ts</files>
  <action>
1. **Confirm Path A (default per CONTEXT.md L77-80).** Path A is REWRITE to SwipeDrawer behavior (matches the actual shipped product). If user has explicitly redirected to Path B (restore MobileTabBar) during plan review, flip disposition to FIXME with UN-BLOCKER citing `deferred-items.md § mobile-navigation.spec.ts` and file a follow-up requirement for UX team — surface to user before committing.

2. **Verify the SwipeDrawer surface at `(app)/+layout.svelte:407-444`:**
   ```bash
   sed -n '407,444p' 'web/src/routes/(app)/+layout.svelte'
   # Expected: <SwipeDrawer ariaLabel="Mobile navigation" ...>...</SwipeDrawer>
   # Verify: navLinks array referenced inside (lines 184-208 per RESEARCH.md L185)
   ```

3. **Confirm hamburger button testid:**
   ```bash
   grep -n 'mobile-menu-toggle\|aria-label="Open menu"' 'web/src/routes/(app)/+layout.svelte' | head -3
   ```
   - If `data-testid="mobile-menu-toggle"` exists, use it.
   - Otherwise, use `getByRole("button", { name: "Open menu" })`.
   - If hamburger button has NO stable selector, surface to user — adding a testid is acceptable (testid-only addition) but should be flagged.

4. **REWRITE each test in `mobile-navigation.spec.ts`** to exercise SwipeDrawer behavior. The test file currently asserts:
   - `nav[aria-label="Mobile navigation"]` direct visibility with child `<a>` links (tab-bar shape) — REWRITE to: hamburger opens drawer; drawer contains nav links; backdrop/Escape closes drawer.
   - Each tab-bar link click navigates to a route — REWRITE to: click hamburger, then click drawer link, assert navigation completes.

   Use the REWRITE template in the `<interfaces>` section above. The test FILE NAME stays the same (`mobile-navigation.spec.ts`); only test bodies change. Reference original intent in commit body:
   ```
   Disposition: REWRITE (Bucket A #1, Path A — tab-bar SUT removed in v1.3; tests rewritten to exercise SwipeDrawer behavior at (app)/+layout.svelte:407-444. Original intent preserved: validate Dashboard/Chat/Settings links are reachable from mobile viewport.)
   ```

5. **At a minimum, the REWRITTEN tests must collectively assert** (per `61-VALIDATION.md` Manual-Only Verifications):
   - (a) Hamburger opens drawer on mobile viewport.
   - (b) Dashboard / Chat / Settings nav links are reachable inside drawer.
   - (c) Backdrop click closes drawer.
   - Additional drawer behaviors (Escape close, nav-link click navigates) are nice-to-have but not strictly required by the must_haves.

6. **Use `MOBILE_VIEWPORT` constant** (already defined in some spec files per Phase 59-06 convention — `web/e2e/theme-sidebar.spec.ts` exports `MOBILE_VIEWPORT = { width: 375, height: 667 }` or similar). Import or redefine consistently with sibling specs.

7. **Run the spec in isolation:**
   ```bash
   cd web && bunx playwright test mobile-navigation.spec.ts --workers=1 --reporter=line
   ```
   Expected: zero failures, zero timeouts on mobile-chromium project (the spec may have desktop-chromium tests that are no-op'd since mobile-navigation is mobile-only — preserve any `test.skip` for non-mobile projects in original spec).

8. **Run regression diff (Layer 2).** Expected: 0 missing entries.
   - Note: tab-bar test entries from the OLD spec will leave the baseline (they no longer exist by that title); their REWRITTEN replacements ADD new entries. Regression diff cares about BASELINE entries missing from NEW — if the baseline included tab-bar test titles, they would appear "missing". HOWEVER, the baseline was recaptured in 61-00 AFTER Phase 59-06 had already fixme'd or scoped-back these specs, so the affected tests should NOT be in the baseline as "passing". If diff shows unexpected missing entries, surface and audit.

9. **Verify zero SUT touches** (rewrites are spec-only unless missing testids are surfaced and added):
   ```bash
   git diff -- web/src/ web/e2e/fixtures/
   # Expected: empty (unless hamburger testid addition was surfaced and applied)
   ```

10. **Commit atomically:**
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "test(61-03): REWRITE mobile-navigation.spec.ts against SwipeDrawer (Path A)" --files web/e2e/mobile-navigation.spec.ts <optional SUT testid file>
    ```
    Body MUST cite:
    - `Disposition: REWRITE (Bucket A #1, Path A — SwipeDrawer behavior; tab-bar SUT removed in v1.3)`.
    - `Reference: .planning/phases/59-test-debt-repair/deferred-items.md § mobile-navigation.spec.ts`.
    - Original-intent preservation note (what the tab-bar tests asserted; how the SwipeDrawer tests cover equivalent surface).

11. **Post-commit:** `git stash list | wc -l` returns 12.
  </action>
  <verify>
    <automated>cd web && bunx playwright test mobile-navigation.spec.ts --workers=1 --reporter=json > /tmp/spec-result.json 2>&1; cd /home/dev/work/EZCorp/ez-corp-ai && test "$(jq '.suites[].specs[].tests[].results[].status' /tmp/spec-result.json | grep -E '"failed"|"timedOut"' | wc -l)" = "0" && test "$(git stash list | wc -l)" = "12"</automated>
  </verify>
  <done>
- `mobile-navigation.spec.ts` passes on mobile-chromium (zero failures, zero timeouts).
- Tests REWRITTEN to exercise SwipeDrawer behavior: hamburger opens drawer, Dashboard/Chat/Settings links reachable, backdrop closes drawer (at minimum).
- Atomic commit with `Disposition: REWRITE (Bucket A #1, Path A — SwipeDrawer behavior)` trailer + deferred-items.md citation + original-intent preservation note.
- Zero SUT logic changes (testid additions allowed only if surfaced and required).
- Baseline regression diff returns 0 missing entries (or surfaced and explained if baseline contained any tab-bar test entries).
- `git stash list | wc -l` returns 12.
  </done>
</task>

<task type="auto">
  <name>Task 5: Closing commit — retitle deferred-items.md Svelte 5 entry to reflect test-env-only verdict</name>
  <files>.planning/phases/59-test-debt-repair/deferred-items.md</files>
  <action>
1. **Read the current Svelte 5 entry** at `.planning/phases/59-test-debt-repair/deferred-items.md` L10-41 (verbatim source the orchestrator already loaded into context).

2. **Edit the entry header** (currently `### Discovered: Svelte 5 singleton-store reactivity bug in (app)/+layout.svelte`) to:
   ```markdown
   ### test-env: missing /api/account mock — 3-line per-test fix shipped in Phase 61, fixmes flipped
   ```

3. **Prepend a "Verdict reclassification (2026-05-13)" paragraph block** immediately under the new header (BEFORE the original `**Surface:**` line). Use the canonical phrasing from the `<interfaces>` section above:
   ```markdown
   **Verdict reclassification (2026-05-13):** Originally filed as "Svelte 5 singleton-store reactivity bug",
   this entry was reclassified to `test-env-only` per the debug investigation at
   `.planning/debug/svelte5-layout-reactivity-2026-05-12.md`. The Svelte 5 reactivity is fine;
   the root cause was `/account/+page.svelte:308` reading `account.name.charAt(0)` after `onMount`
   fetches `/api/account`, which returned `{}` from the api-mocks default catch-all. The thrown
   `TypeError` aborted Svelte 5's effect scheduler mid-flush, leaving subsequent reactive updates
   unrendered.

   **Fix shipped in Phase 61-01:** Single atomic commit added a `test.beforeEach` to the `Sidebar`
   describe block in `web/e2e/theme-sidebar.spec.ts` mocking `/api/account`, `/api/account/sessions`,
   `/api/account/login-history` with canonical AccountData / empty payloads. All 5 fixme'd tests
   flipped to active passing tests. No SUT change required.

   **Original entry preserved for audit trail:**
   ```
   (Followed by the existing entry body verbatim — `**Surface:**`, `**Symptom:**`, `**Affected tests**`, `**UN-BLOCKER:**`, `**Filed for:**` paragraphs unchanged.)

4. **Do NOT delete or alter** any other section of `deferred-items.md` (the 8 spec-file entries below are intentionally preserved as the canonical audit trail — Phase 61's spec dispositions are recorded in commit bodies, not by editing this file).

5. **Optional housekeeping (if scope permits — CONTEXT.md L196):**
   - The 868 vs 1678 baseline-figure correction (deferred-items.md §138-146) and the 7-spec vs 8-spec roadmap-text count drift (CONTEXT.md L195) are deferrable to v1.5 housekeeping.
   - If the diff for the retitling is < 30 lines, fold these in as inline corrections. If > 30 lines, leave for v1.5.

6. **Verify the doc edit:**
   ```bash
   grep -A2 "test-env: missing /api/account mock" .planning/phases/59-test-debt-repair/deferred-items.md
   # Expected: 3+ lines including "Verdict reclassification (2026-05-13)"

   grep -q "svelte5-layout-reactivity-2026-05-12" .planning/phases/59-test-debt-repair/deferred-items.md
   # Expected: exit 0

   grep -q "Svelte 5 singleton-store reactivity bug" .planning/phases/59-test-debt-repair/deferred-items.md
   # Expected: exit 0 — preserved in "Original entry preserved for audit trail" block
   ```

7. **Verify zero spec/SUT touches in this commit:**
   ```bash
   git diff -- web/
   # Expected: empty (this commit is doc-only)
   ```

8. **Commit atomically:**
   ```bash
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(61-03): retitle deferred-items.md Svelte 5 entry — test-env-only verdict" --files .planning/phases/59-test-debt-repair/deferred-items.md
   ```
   Body MUST cite:
   - `Reference: .planning/debug/svelte5-layout-reactivity-2026-05-12.md (verdict: test-env-only)`.
   - `Closes: Phase 61 — Bucket B fixmes flipped in 61-01 commit <hash>`.
   - Brief reason (~2 lines): the original "singleton-store reactivity bug" diagnosis was wrong; the actual cause was a test-env mock gap; Phase 61-01 shipped the 3-line per-test fix.

9. **Post-commit:** `git stash list | wc -l` returns 12.
  </action>
  <verify>
    <automated>grep -q "test-env: missing /api/account mock" .planning/phases/59-test-debt-repair/deferred-items.md && grep -q "svelte5-layout-reactivity-2026-05-12" .planning/phases/59-test-debt-repair/deferred-items.md && grep -q "Svelte 5 singleton-store reactivity bug" .planning/phases/59-test-debt-repair/deferred-items.md && test "$(git stash list | wc -l)" = "12" && test "$(git diff main..HEAD --name-only | grep -c '^web/')" -eq "$(git diff main..HEAD --name-only -- web/ | grep -v -E '\.spec\.ts$|\.svelte$' | wc -l | xargs expr -1 +)"</automated>
  </verify>
  <done>
- `.planning/phases/59-test-debt-repair/deferred-items.md` § Svelte 5 entry is retitled to "test-env: missing /api/account mock — 3-line per-test fix shipped in Phase 61, fixmes flipped".
- New "Verdict reclassification (2026-05-13)" paragraph cites the debug doc path and Phase 61-01 commit.
- Original entry body preserved verbatim under "Original entry preserved for audit trail" header.
- Doc-only commit (zero web/ changes); body cites debug doc + Phase 61-01.
- `git stash list | wc -l` returns 12.
  </done>
</task>

</tasks>

<verification>
**Plan-level checks** (run after all 5 tasks commit; this is the LAST plan of Phase 61):

1. **Per-spec quick run green (Layer 1) for all 4 Bucket A complex specs:**
   ```bash
   for spec in teams swipe-drawer menu-keyboard-nav mobile-navigation; do
       cd web && bunx playwright test "$spec.spec.ts" --workers=1 --reporter=line
   done
   # Expected: zero failures, zero timeouts
   ```

2. **Regression invariant holds (Layer 2):**
   ```bash
   # Title-strip + comm -23 per 61-VALIDATION.md Layer 2 command
   # Expected: /tmp/missing.txt has 0 lines
   ```

3. **Disposition audit (Layer 3) — 8 Bucket A specs disposed across Phase 61:**
   ```bash
   for spec in mobile-navigation swipe-drawer menu-keyboard-nav command-palette-v2 teams tool-card-rendering task-card-actions-full selector-keyboard-nav; do
       git log --grep="Disposition:" --oneline -- "web/e2e/$spec.spec.ts" | head -1
   done
   # Expected: 8 lines, one per Bucket A spec

   git log --oneline --name-only main..HEAD | awk '/^web\/e2e\/(mobile-navigation|swipe-drawer|menu-keyboard-nav|command-palette-v2|teams|tool-card-rendering|task-card-actions-full|selector-keyboard-nav)\.spec\.ts$/' | sort -u | wc -l
   # Expected: 8
   ```

4. **FIXME audit (Layer 4) — every retained test.fixme has UN-BLOCKER within 10 preceding lines:**
   ```bash
   for spec in mobile-navigation swipe-drawer menu-keyboard-nav command-palette-v2 teams tool-card-rendering task-card-actions-full selector-keyboard-nav theme-sidebar; do
       awk '
           /UN-BLOCKER CONDITION/ { ub_seen_at = NR }
           /test\.fixme/ {
               if (ub_seen_at < NR - 10 || ub_seen_at == 0) {
                   print FILENAME":"NR": test.fixme without UN-BLOCKER within 10 preceding lines"
               }
           }
       ' "web/e2e/$spec.spec.ts"
   done
   # Expected: empty output
   ```

5. **Bucket B Sidebar describe has zero fixmes (Layer 4 additional):**
   ```bash
   awk '/test\.describe\("Sidebar"/,/^test\.describe\(/' web/e2e/theme-sidebar.spec.ts | grep -c 'test\.fixme'
   # Expected: 0
   ```

6. **No timeout widening (Layer 5):**
   ```bash
   git diff main -- web/playwright.config.ts | wc -l   # Expected: 0
   for spec in teams swipe-drawer menu-keyboard-nav mobile-navigation; do
       git diff main -- "web/e2e/$spec.spec.ts" | grep -E "^\+.*setTimeout|^\+.*\{\s*timeout:\s*(6\d{3}|[7-9]\d{3}|\d{5,})" && echo "FAIL: $spec"
   done
   # Expected: no FAILs
   ```

7. **Deferred-items.md retitled:**
   ```bash
   grep -q "test-env: missing /api/account mock" .planning/phases/59-test-debt-repair/deferred-items.md && \
   grep -q "svelte5-layout-reactivity-2026-05-12" .planning/phases/59-test-debt-repair/deferred-items.md && \
   echo "OK"
   # Expected: OK
   ```

8. **Sacred-stash invariant:**
   ```bash
   test $(git stash list | wc -l) -eq 12
   ```
</verification>

<success_criteria>
- 4 Bucket A complex specs disposed (teams, swipe-drawer, menu-keyboard-nav, mobile-navigation): each passes OR has every retained `test.fixme` carrying an UN-BLOCKER comment block within 10 preceding lines.
- 4 atomic commits for the 4 spec dispositions (Tasks 1-4); each commit body has `Disposition: <REPAIR|REWRITE|FIXME>` trailer + cites `deferred-items.md` entry.
- 1 closing commit (Task 5) retitling `deferred-items.md` § Svelte 5 entry + preserving original entry under audit-trail block.
- Across all of Phase 61: 8 Bucket A specs disposed (4 from 61-02 + 4 from 61-03) — Layer 3 audit returns 8 spec touches with disposition trailers.
- 5 Bucket B fixmes flipped (from 61-01) — `theme-sidebar.spec.ts` `Sidebar` describe has 0 fixmes.
- Layer 4 audit returns zero `test.fixme` without UN-BLOCKER within 10 preceding lines.
- Title-based baseline regression diff returns empty after EACH commit.
- `playwright.config.ts`, `package.json`, CI yaml, `web/e2e/fixtures/api-mocks.ts` all untouched throughout Phase 61.
- `git stash list | wc -l` returns 12 (sacred baseline preserved throughout Phase 61 — 12 pre + 12 post per plan).
</success_criteria>

<output>
After completion, create `.planning/phases/61-test-debt-followup-feature-rework-specs/61-03-SUMMARY.md` AND `.planning/phases/61-test-debt-followup-feature-rework-specs/61-SUMMARY.md` (Phase-level summary) recording:

**61-03-SUMMARY.md:**
- 4 disposition verdicts (one per spec) with commit hashes + one-line reason each.
- INVESTIGATE finding for menu-keyboard-nav (what the trace showed + which classification path taken).
- mobile-navigation Path A REWRITE result — original intent preservation summary (which tab-bar assertions map to which SwipeDrawer behaviors).
- Baseline regression diff result after each commit.
- Sacred-stash invariant check.
- Any deviations encountered (e.g., user redirect to Path B for mobile-navigation; surfaced testid additions beyond `files_modified` frontmatter; surprise SUT findings).

**61-SUMMARY.md (Phase-level wrap-up):**
- Final disposition table for all 8 Bucket A specs + 5 Bucket B fixmes (13 dispositions total).
- Commit hashes for all 9 plan commits (1 from 61-00 + 1 from 61-01 + 4 from 61-02 + 5 from 61-03).
- Layer 1-5 validation gate results (per-spec passing, regression diff empty, disposition audit 8/8, FIXME audit empty, no timeout widening).
- TEST-02 residual status update — Phase 61 work complete; deferred-items.md updated; closes the residual from Phase 59-06.
- Sacred-stash invariant check pre+post Phase 61 (12 each plan).
- Patterns established or reused: `/api/account` per-test mock (Bucket B); per-variant tool-card testids; per-row task-card testid scoping; route-pivot to already-mocked `(app)` route; SwipeDrawer-equivalent REWRITE pattern; INVESTIGATE-then-DISPOSE for ambiguous mock-shape failures.
- Hand-off to `/gsd:verify-work`: ready for Phase 61 verification.
</output>
