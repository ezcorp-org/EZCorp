---
phase: 63-indexing-primitives
plan: 03
type: execute
wave: 2
depends_on: ["63-01", "63-02"]
files_modified:
  - src/db/queries/message-embed-outbox.ts
  - src/db/queries/conversations.ts
  - src/__tests__/message-embed-outbox.test.ts
autonomous: true
requirements: [IDX-04, IDX-05]
must_haves:
  truths:
    - "Creating a user/assistant message writes exactly one outbox row in the SAME transaction as the message insert"
    - "If the transaction throws after the message insert but before/during the outbox upsert, NEITHER row exists (atomic rollback)"
    - "Creating a system/extension/ez-action-result/capability-event message writes NO outbox row"
    - "Creating an empty/whitespace message writes NO outbox row"
    - "Editing a message's content (updateMessageContent) re-enqueues it (upsert resets status to pending, no duplicate row)"
    - "Toggling setMessageExcluded does NOT re-enqueue"
  artifacts:
    - path: "src/db/queries/message-embed-outbox.ts"
      provides: "enqueueEmbedJob(tx, messageId, conversationId) upsert helper accepting a tx handle"
      contains: "onConflictDoUpdate"
    - path: "src/db/queries/conversations.ts"
      provides: "createMessage wrapped in db.transaction with in-tx enqueue; updateMessageContent re-enqueue"
      contains: "transaction"
    - path: "src/__tests__/message-embed-outbox.test.ts"
      provides: "Atomicity (throw-mid-tx + inverse), role exclusion, edit re-enqueue, excluded no-enqueue"
      min_lines: 80
  key_links:
    - from: "src/db/queries/conversations.ts createMessage"
      to: "enqueueEmbedJob via tx"
      via: "db.transaction(async (tx) => { insert; enqueueEmbedJob(tx, ...) })"
      pattern: "enqueueEmbedJob"
    - from: "src/db/queries/conversations.ts updateMessageContent"
      to: "enqueueEmbedJob via tx (re-enqueue on content change)"
      via: "isEmbedEligible gate + tx enqueue"
      pattern: "isEmbedEligible"
---

<objective>
Wire the transactional write boundary: introduce the codebase's FIRST `db.transaction()`
in `createMessage`, enqueuing exactly one outbox row atomically with the message insert,
gated by the eligibility allowlist. Hook the edit path (`updateMessageContent`) for
re-enqueue; leave `setMessageExcluded` untouched.

Purpose: This is IDX-04 atomicity — no message persists without its embed job, and no
embed job exists without its message. It is the highest-risk item in the phase (first DB
transaction) and depends on the eligibility predicate (Plan 01) and the outbox table
(Plan 02), so it runs in Wave 2.
Output: `enqueueEmbedJob` helper; transactional `createMessage`; re-enqueue in
`updateMessageContent`; `message-embed-outbox.test.ts`.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-indexing-primitives/63-CONTEXT.md
@.planning/phases/63-indexing-primitives/63-RESEARCH.md
@.planning/phases/63-indexing-primitives/63-VALIDATION.md

@src/db/queries/conversations.ts
@.planning/phases/63-indexing-primitives/63-01-SUMMARY.md
@.planning/phases/63-indexing-primitives/63-02-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 01 + Plan 02 — consume directly. -->

From src/memory/message-chunker.ts (Plan 01):
  - `export function isEmbedEligible(role: string, content: string): boolean`
    (true iff role ∈ {user, assistant} AND content.trim() non-empty)

From src/db/schema.ts (Plan 02):
  - `messageEmbedOutbox` pgTable — `messageId` is the PRIMARY KEY (one row per message,
    the ON CONFLICT target). Columns: messageId, conversationId, status, attempts,
    createdAt, updatedAt.

Current createMessage (conversations.ts ~367, verified, NON-transactional):
  - `const db = getDb();` then `db.insert(messages).values({...}).returning();`
    then a SEPARATE `db.update(conversations).set({ updatedAt: sql\`NOW()\` })...`.
  - data has { role, content, ... } — role + content are what eligibility needs.

Current updateMessageContent (conversations.ts ~639, verified):
  - `db.update(messages).set({ content })...returning()` then conversation updatedAt touch.
  - Content-changing edit — MUST re-enqueue.

Current setMessageExcluded (conversations.ts ~664, verified):
  - flips `excluded` only — NOT a content change. MUST NOT re-enqueue. Do not touch it.

Drizzle transaction API (verified drizzle-orm/pg-core/db.d.ts:281):
  - `db.transaction(async (tx) => { ... })` — available on PgDatabase (PGlite + Bun.sql).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: enqueueEmbedJob upsert helper (accepts tx)</name>
  <files>src/db/queries/message-embed-outbox.ts, src/__tests__/message-embed-outbox.test.ts</files>
  <behavior>
    enqueueEmbedJob(tx, messageId, conversationId):
      - Test: first call for a message inserts one outbox row with status='pending', attempts=0.
      - Test: a second call for the SAME messageId does NOT create a duplicate — it upserts
        (ON CONFLICT (message_id) DO UPDATE) resetting status='pending', attempts=0,
        updated_at=NOW(). Row count for that message stays 1.
    Use setupTestDb() and call enqueueEmbedJob with the real db handle (db itself is a
    valid tx-like for a single call) for these helper-level cases.
  </behavior>
  <action>
    Create `src/db/queries/message-embed-outbox.ts`:

    ```typescript
    import { sql } from "drizzle-orm";
    import { messageEmbedOutbox } from "../schema";

    // Accepts a tx (or db) handle so it can run INSIDE createMessage's transaction.
    // PITFALL (research Pitfall 1): the caller MUST pass its tx handle here — never let
    // this helper call getDb(), or it escapes the enclosing transaction.
    export async function enqueueEmbedJob(
      tx: { insert: typeof import("../connection").getDb extends never ? never : any },
      messageId: string,
      conversationId: string,
    ): Promise<void> {
      await tx
        .insert(messageEmbedOutbox)
        .values({ messageId, conversationId, status: "pending", attempts: 0 })
        .onConflictDoUpdate({
          target: messageEmbedOutbox.messageId,
          set: { status: "pending", attempts: 0, updatedAt: sql`NOW()` },
        });
    }
    ```
    Use a precise tx type if one is readily importable (e.g. the Drizzle transaction
    type); otherwise a minimal structural type is acceptable — the critical contract is
    that the handle is PASSED IN, not fetched via getDb(). Keep this module pure of
    getDb() calls.

    Begin `src/__tests__/message-embed-outbox.test.ts` with the helper-level cases above
    (insert-once, upsert-no-duplicate). Use setupTestDb(); seed project → conversation →
    message first (FK on message_id + conversation_id).
  </action>
  <verify>
    <automated>bun test src/__tests__/message-embed-outbox.test.ts</automated>
  </verify>
  <done>enqueueEmbedJob inserts one pending row; a repeat call upserts to pending without a duplicate row; no getDb() inside the helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transactional createMessage + updateMessageContent re-enqueue</name>
  <files>src/db/queries/conversations.ts, src/__tests__/message-embed-outbox.test.ts</files>
  <behavior>
    createMessage (atomicity + eligibility):
      - Test: createMessage role='user' non-empty → exactly 1 message row AND exactly 1
        outbox row for that message id.
      - Test: createMessage role='assistant' non-empty → 1 message + 1 outbox row.
      - Test: createMessage role ∈ {system, extension, ez-action-result, capability-event}
        → 1 message row, 0 outbox rows.
      - Test: createMessage role='assistant' content='   ' (whitespace) → 1 message, 0 outbox.
      - Test (ATOMICITY throw-mid-tx): force the enqueue to throw inside the transaction
        AFTER the message insert → assert NEITHER a message row NOR an outbox row exists
        (both rolled back). Inject the throw via mock.module on the outbox helper module
        (mock enqueueEmbedJob to throw), OR pass an invalid conversationId that violates a
        FK inside the tx. Prefer the mock-module approach for determinism; if mocking the
        outbox module, REGISTER its canonical path in mock-cleanup.ts MODULE_PATHS and keep
        mock-cleanup-coverage.test.ts green.
    updateMessageContent (edit re-enqueue):
      - Test: create eligible message (1 outbox row), then updateMessageContent with new
        content → still exactly 1 outbox row (upserted, status reset to pending), updated_at advanced.
      - Test: updateMessageContent on a non-eligible existing message does NOT create an
        outbox row.
    setMessageExcluded (regression):
      - Test: create eligible message; capture outbox row's updated_at; call
        setMessageExcluded(true) → outbox row UNCHANGED (no re-enqueue; assert updated_at
        not advanced and attempts unchanged).
  </behavior>
  <action>
    Edit `src/db/queries/conversations.ts`:

    1. Import `isEmbedEligible` from `../../memory/message-chunker` and `enqueueEmbedJob`
       from `./message-embed-outbox`.

    2. Wrap `createMessage` body in `db.transaction(async (tx) => { ... })`. Move BOTH the
       message insert AND the conversation updatedAt touch onto `tx`. After the insert,
       gate on eligibility and enqueue:
       ```typescript
       return db.transaction(async (tx) => {
         const rows = await tx.insert(messages).values({ ...same fields... }).returning();
         const msg = rows[0]!;
         await tx.update(conversations).set({ updatedAt: sql`NOW()` }).where(eq(conversations.id, conversationId));
         if (isEmbedEligible(data.role, data.content)) {
           await enqueueEmbedJob(tx, msg.id, conversationId);
         }
         return msg;
       });
       ```
       CRITICAL (research Pitfall 1): inside the callback reference ONLY `tx`. Grep the new
       body — zero `getDb(` and zero bare `db.` (other than the outer `getDb()` that
       provides `.transaction`) inside the callback. A stray getDb() silently escapes the
       transaction and breaks atomicity.

       NOTE on finalize hot path (research anti-pattern): the enqueue is a cheap single-row
       upsert — fine in-tx. NEVER call generateEmbedding() here; embedding generation is
       Phase 64's worker. Do not block SSE finalize.

    3. Wrap `updateMessageContent` in `db.transaction` similarly: after the content update +
       conversation touch (both on tx), if the updated row is eligible
       (`isEmbedEligible(row.role, content)`), call `enqueueEmbedJob(tx, messageId, conversationId)`.
       The outbox PK upsert means a re-edit before drain just refreshes the pending job —
       no duplicate row.

    4. Do NOT touch `setMessageExcluded` — it is not a content change.

    Append the createMessage / updateMessageContent / setMessageExcluded behavior cases to
    `src/__tests__/message-embed-outbox.test.ts`. Use setupTestDb(); for the throw-mid-tx
    case, mock the outbox helper module to throw and assert full rollback (and register its
    path in MODULE_PATHS + verify mock-cleanup-coverage.test.ts stays green).
  </action>
  <verify>
    <automated>bun test src/__tests__/message-embed-outbox.test.ts src/__tests__/mock-cleanup-coverage.test.ts</automated>
  </verify>
  <done>Eligible message → 1 message + 1 outbox row atomically; throw-mid-tx rolls back both; ineligible/empty → 0 outbox; edit re-enqueues (upsert, no dup); setMessageExcluded does not re-enqueue; no getDb() inside any tx callback.</done>
</task>

</tasks>

<verification>
- `bun test src/__tests__/message-embed-outbox.test.ts` green (IDX-04, IDX-05)
- `bun test src/__tests__/mock-cleanup-coverage.test.ts` green (no leaked mock targets if outbox module mocked)
- Full suite at wave merge: `bun run test` — confirms no message-creation regressions across the 1000+ files that exercise createMessage
- Per-file coverage gate green on new paths
- Manual grep gate: zero `getDb(` inside the db.transaction callbacks in conversations.ts
</verification>

<success_criteria>
- createMessage is transactional: eligible message + exactly one outbox row, atomically; throw-mid-tx rolls back both
- Strict allowlist enforced at the write boundary (user/assistant non-empty only)
- updateMessageContent re-enqueues (upsert to pending, no duplicate); setMessageExcluded does not
- enqueueEmbedJob runs only on the passed-in tx handle (atomicity preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/63-indexing-primitives/63-03-SUMMARY.md`
</output>
