---
phase: 67-command-palette-search
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/e2e/command-palette-search.spec.ts
autonomous: true
requirements: [PAL-01, PAL-05, PAL-07, PAL-06, PAL-02]
must_haves:
  truths:
    - "A RED Playwright e2e spec exists describing the full Cmd+K / Cmd+Shift+P + cross-project deep-link + mobile BottomSheet journey before the UI is built"
  artifacts:
    - path: "web/e2e/command-palette-search.spec.ts"
      provides: "e2e spec: Cmd+K search-focus / Cmd+Shift+P command-first; cross-project result click → scroll+pulse+?m=stripped; BottomSheet at mobile-chromium with single-Escape close + auto-focused input"
      min_lines: 80
  key_links:
    - from: "web/e2e/command-palette-search.spec.ts"
      to: "/api/search/messages"
      via: "page.route mock returning hits with projectId/projectName"
      pattern: "api/search/messages"
---

<objective>
Scaffold the missing Wave-0 Playwright e2e spec (RED) for the full palette
journey so Plan 07's gate has a concrete target. Reuses the Phase 66
`/api/search/messages` route-mock + `makeSearchHit` helper (now extended with
`projectId`/`projectName`). Runs on `chromium` + `mobile-chromium`. This spec is
expected RED until the UI lands (Plans 04+06); Plan 07 turns it GREEN and closes
the coverage gate.

Purpose: e2e is a required layer of the 100% coverage bar; the spec must exist as
the executable acceptance contract for the palette UX.
Output: one RED e2e spec covering PAL-01/05/06/07/02 end-to-end on both viewports.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-command-palette-search/67-CONTEXT.md
@.planning/phases/67-command-palette-search/67-RESEARCH.md
@.planning/phases/67-command-palette-search/67-VALIDATION.md

# Phase 66 e2e precedents to mirror (mock + makeSearchHit + ?m= journey):
@web/e2e/sidebar-search-deeplink.spec.ts
@web/e2e/conversation-search.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED e2e spec — desktop Cmd+K / Cmd+Shift+P + cross-project deep-link</name>
  <files>web/e2e/command-palette-search.spec.ts</files>
  <action>
    Create the Playwright spec. Reuse the Phase 66 `/api/search/messages`
    `page.route` mock + `makeSearchHit` factory (copy/extend from
    sidebar-search-deeplink.spec.ts), adding `projectId`/`projectName` to the
    factory so hits span ≥2 projects. Cases:
    1. Cmd+K (Ctrl+K) opens the palette with the search input focused; the
       command list is visible below.
    2. Cmd+Shift+P (Ctrl+Shift+P) opens the SAME palette command-first (search
       input not focused); assert no browser private-window side effect leaks
       (palette is visible).
    3. Type ≥2 chars → Commands section AND message-hit sections render together;
       "In this conversation" + "Other conversations" headers present when a
       conversation is active.
    4. Click a result in a DIFFERENT project → URL becomes
       `/project/<otherProjectId>/chat/<convId>?m=<msgId>`, the target message
       scrolls into view + pulses, and `?m=` is stripped after consume (refresh
       does not re-pulse). Mirror the sidebar-search-deeplink `?m=` assertions.
    Use the Docker/seed or mock harness consistent with the Phase 66 specs
    (MEMORY: tool-card RENDER specs run in plain preview via mockApi+emitSse;
    SEND-flow needs Docker — follow whichever the sidebar deep-link spec uses).
    Expected RED until the UI lands.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/command-palette-search.spec.ts --project=chromium --list 2>&1 | tail -20</automated>
  </verify>
  <done>Spec lists desktop cases for Cmd+K focus, Cmd+Shift+P command-first, mixed sections, and the cross-project ?m= deep-link journey; reuses the Phase 66 mock + extended makeSearchHit.</done>
</task>

<task type="auto">
  <name>Task 2: RED e2e cases — mobile BottomSheet fallback (mobile-chromium)</name>
  <files>web/e2e/command-palette-search.spec.ts</files>
  <action>
    Append mobile-targeted cases (guarded by viewport / `--project=mobile-chromium`,
    mirror the viewport-aware helpers in conversation-search.spec.ts):
    1. On `<lg`, opening the palette renders it inside the BottomSheet
       (`data-testid="bottom-sheet"`), NOT the centered desktop modal.
    2. Same section structure inside the sheet (Commands / In this conversation /
       Other by project→conversation) — assert nesting is NOT flattened.
    3. The search input is auto-focused on open even on mobile.
    4. A single Escape closes the sheet (no double-close / double-trap).
    Expected RED until Plan 06 wraps the modal in BottomSheet at `<lg`.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/command-palette-search.spec.ts --project=mobile-chromium --list 2>&1 | tail -20</automated>
  </verify>
  <done>Mobile cases listed: BottomSheet render, non-flattened nesting, auto-focused input, single-Escape close — all on mobile-chromium, RED until the UI lands.</done>
</task>

</tasks>

<verification>
- `web/e2e/command-palette-search.spec.ts` exists and is discovered by Playwright
  on both `chromium` and `mobile-chromium` (`--list` enumerates the cases).
- The spec reuses the Phase 66 `/api/search/messages` mock + an extended
  `makeSearchHit` carrying `projectId`/`projectName`.
- No production source modified.
</verification>

<success_criteria>
One RED e2e spec covering the desktop shortcuts, cross-project deep-link, and
mobile BottomSheet fallback — discoverable on both Playwright projects, ready for
Plan 07 to turn GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/67-command-palette-search/67-02-SUMMARY.md`
</output>
