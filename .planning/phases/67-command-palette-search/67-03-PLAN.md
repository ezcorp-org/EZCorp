---
phase: 67-command-palette-search
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries/message-search.ts
  - src/__tests__/message-search.test.ts
  - src/__tests__/message-search-explain.test.ts
  - web/src/routes/api/search/messages/+server.ts
  - web/src/routes/api/search/messages/schema.ts
  - web/src/lib/api.ts
  - web/src/__tests__/api-search-messages.server.test.ts
  - web/src/__tests__/api-search-messages.client.test.ts
autonomous: true
requirements: [PAL-01, PAL-05]
must_haves:
  truths:
    - "A cross-project search (scope=all) returns ranked hits across all the requesting user's projects in one SQL round-trip"
    - "Cross-project search never returns another user's messages"
    - "Each MessageSearchHit carries projectId and projectName (enabling cross-project deep-link + project-header grouping)"
    - "The cross-project ANN scan keeps the HNSW index (tenant filter inside/before the scan; no Seq Scan on message_chunks)"
    - "scope=all does not require projectId; scope=project keeps the existing 400; degraded envelope + auth/read-scope gate unchanged"
  artifacts:
    - path: "src/db/queries/message-search.ts"
      provides: "scope=all branch generalizing scopedConvArray to the user's project set; projectId/projectName selected on every hit"
      contains: "scope"
    - path: "web/src/routes/api/search/messages/schema.ts"
      provides: "zod scope enum (project|all, default project)"
      contains: "scope"
    - path: "web/src/lib/api.ts"
      provides: "searchMessages opts.scope; MessageSearchHit gains projectId+projectName"
      contains: "projectId"
  key_links:
    - from: "web/src/routes/api/search/messages/+server.ts"
      to: "src/db/queries/message-search.ts"
      via: "scope=all → searchMessages({scope:'all', userId})"
      pattern: "scope"
    - from: "src/db/queries/message-search.ts"
      to: "message_chunks HNSW index"
      via: "single-table ANN scan with conversation_id = ANY(scoped ids)"
      pattern: "idx_message_chunks_embedding"
---

<objective>
Extend the Phase 65 search contract with a cross-project ("all my projects")
scope AND add `projectId`/`projectName` to `MessageSearchHit`. This is the
foundational backend change every palette deep-link depends on: a cross-project
deep-link URL is `/project/<projectId>/chat/<convId>?m=<msgId>`, and the hit does
not carry `projectId` today. Generalize `scopedConvArray()` to the user's project
set while preserving the single-table ANN scan (so the HNSW index stays in the
plan — SRCH-05 invariant), and re-run the EXPLAIN proof for the multi-project
scope.

Purpose: enables PAL-01 (search from anywhere across all projects) and unblocks
PAL-05 (cross-project deep-link). This is the single most consequential change in
the phase (RESEARCH §Open Questions 1, CRITICAL).
Output: a `scope=project|all` search path with `projectId`/`projectName` on every
hit, fully unit + integration covered.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-command-palette-search/67-CONTEXT.md
@.planning/phases/67-command-palette-search/67-RESEARCH.md
@.planning/phases/67-command-palette-search/67-VALIDATION.md

# The SUTs to extend (read the single-table-ANN header note + scopedConvArray):
@src/db/queries/message-search.ts
@web/src/routes/api/search/messages/+server.ts
@web/src/routes/api/search/messages/schema.ts
@web/src/lib/api.ts
# Existing tests to extend:
@src/__tests__/message-search.test.ts
@src/__tests__/message-search-explain.test.ts
@web/src/__tests__/api-search-messages.server.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-project scope + projectId/projectName in the query builder</name>
  <files>src/db/queries/message-search.ts, src/__tests__/message-search.test.ts, src/__tests__/message-search-explain.test.ts</files>
  <behavior>
    - searchMessages with scope=all (userId required) returns hits from ALL the user's projects, ranked in one CTE.
    - scope=all NEVER returns another user's messages (cross-user-leak test).
    - test=true conversations remain excluded under scope=all.
    - Every hit (all three mode branches: keyword/semantic/hybrid) carries projectId + projectName.
    - EXPLAIN under multi-project scope shows `Index Scan using idx_message_chunks_embedding` + `Filter: conversation_id` and NO `Seq Scan on message_chunks` (filter-inside-HNSW preserved).
  </behavior>
  <action>
    Generalize `scopedConvArray()` (current L89-96): keep the single-table ANN
    structure — the vector leg must still see one denormalized
    `conversation_id = ANY(ARRAY(<scoped ids>))` predicate, NO project→conversation
    JOIN inside the ANN scan (Pitfall 5 — a join collapses the plan to Seq Scan).
    For scope=all, drop the `c.project_id = ${projectId}` predicate and keep
    `c.user_id = ${userId}` + the test-null-safe filter (userId becomes REQUIRED).
    For scope=project, behavior is unchanged.
    For the DISPLAY join (outside the ANN leg), add `c.project_id AS project_id`
    and `JOIN projects p ON p.id = c.project_id` selecting `p.name AS
    project_name` to all three display SELECTs (L235/L268/L319), and add
    `projectId`/`projectName` to `toHit()` (L174) and the server-side
    `MessageSearchHit` type (L56). Thread a `scope` (and optional `projectId`)
    parameter through `searchMessages`'s signature.
    Then EXTEND src/__tests__/message-search.test.ts with: scope=all returns
    cross-project hits, cross-user-leak guard, test-exclusion under scope=all,
    projectId/projectName populated on hits (these turn the Wave-0-spirit cases
    green). EXTEND message-search-explain.test.ts with the multi-project EXPLAIN
    assertion (re-run `explainVectorLegSql()` for scope=all; assert the index
    scan + no Seq Scan). DRY: reuse the existing corpus-seed helpers in those
    test files; add a second project + a second user to the fixture.
  </action>
  <verify>
    <automated>bun test src/__tests__/message-search.test.ts src/__tests__/message-search-explain.test.ts</automated>
  </verify>
  <done>scope=all returns cross-project hits with projectId/projectName, never leaks other users, keeps test-exclusion, and the multi-project EXPLAIN shows the HNSW index scan with no Seq Scan; both backend suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Endpoint scope param + client contract (projectId/projectName)</name>
  <files>web/src/routes/api/search/messages/+server.ts, web/src/routes/api/search/messages/schema.ts, web/src/lib/api.ts, web/src/__tests__/api-search-messages.server.test.ts, web/src/__tests__/api-search-messages.client.test.ts</files>
  <behavior>
    - schema.ts: zod `scope: z.enum(["project","all"]).default("project")`.
    - Endpoint: when scope=all, projectId is NOT required (no 400); calls searchMessages({scope:"all", userId:user.id}). When scope=project, the existing projectId-required 400 stays. Auth/read-scope gate, limit/offset clamp, and degraded envelope unchanged.
    - Server response hits carry projectId + projectName.
    - Client: `searchMessages(projectId, query, {mode, limit, offset, scope})` forwards `scope`; `MessageSearchHit` (api.ts) gains `projectId: string; projectName: string;`.
  </behavior>
  <action>
    Add the `scope` enum to schema.ts. In +server.ts (current hard 400 at
    L26-27), branch: scope=all → skip the projectId requirement and pass
    `{scope:"all", userId:user.id}`; scope=project → unchanged. Keep the
    auth/read-scope gate, zod validation, clamp, and `{hits,degraded,
    requestedMode,servedMode}` envelope. In api.ts add `scope?: SearchScope`
    to the opts and `projectId`/`projectName` to `MessageSearchHit`
    (mirror the server type from Task 1).
    EXTEND web/src/__tests__/api-search-messages.server.test.ts (already exists,
    294 lines) with: scope=all → no projectId required (200, not 400);
    scope=project → still 400 without projectId; projectId/projectName present
    on returned hits. CREATE web/src/__tests__/api-search-messages.client.test.ts
    asserting `searchMessages` forwards `scope` in the query string and surfaces
    the new fields on the typed hit (mirror an existing api.ts client-test
    harness for fetch-spy + response shape).
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/__tests__/api-search-messages.server.test.ts src/__tests__/api-search-messages.client.test.ts</automated>
  </verify>
  <done>scope=all skips the projectId 400 and resolves tenant by userId; scope=project keeps the 400; client forwards scope + exposes projectId/projectName; both server + client suites green.</done>
</task>

</tasks>

<verification>
- `bun test src/__tests__/message-search.test.ts src/__tests__/message-search-explain.test.ts` green (cross-project + leak-guard + EXPLAIN).
- `cd web && bunx vitest run src/__tests__/api-search-messages.server.test.ts src/__tests__/api-search-messages.client.test.ts` green.
- EXPLAIN proof shows the HNSW index scan + `Filter: conversation_id`, no `Seq Scan on message_chunks` under multi-project scope.
- `MessageSearchHit` (server + client) carries projectId + projectName.
</verification>

<success_criteria>
A `scope=project|all` search path that ranks across the user's projects in one
round-trip, never leaks other users, keeps the filter-inside-HNSW invariant, and
returns projectId/projectName on every hit — fully unit + integration covered.
</success_criteria>

<output>
After completion, create `.planning/phases/67-command-palette-search/67-03-SUMMARY.md`
</output>
