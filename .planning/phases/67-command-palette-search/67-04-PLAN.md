---
phase: 67-command-palette-search
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/shortcuts.ts
  - web/src/lib/__tests__/shortcuts.test.ts
  - web/src/routes/(app)/+layout.svelte
autonomous: true
requirements: [PAL-02, PAL-01]
must_haves:
  truths:
    - "Cmd+K (palette) still opens the palette; Cmd+Shift+P opens it via a new palette-commands action"
    - "Adding palette-commands does not break users' custom shortcut overrides (merge-by-action preserved for both palette and palette-commands)"
    - "The layout routes palette → search-first initial view and palette-commands → command-first initial view, passing the initial view to the palette mount"
  artifacts:
    - path: "web/src/lib/shortcuts.ts"
      provides: "DEFAULT_SHORTCUTS += {key:'p', meta:true, shift:true, action:'palette-commands'}"
      contains: "palette-commands"
    - path: "web/src/routes/(app)/+layout.svelte"
      provides: "switch arms for palette + palette-commands; paletteInitialView state passed to CommandPalette"
      contains: "palette-commands"
  key_links:
    - from: "web/src/routes/(app)/+layout.svelte"
      to: "CommandPalette mount"
      via: "initialView prop from paletteInitialView state"
      pattern: "initialView|paletteInitialView"
---

<objective>
Rebind the command-palette entry point: keep `Cmd+K` (action `palette`) opening
the palette search-first, and add a new `palette-commands` action bound to
`Cmd+Shift+P` that opens the SAME palette command-first. Wire the layout switch +
an `initialView` prop so the palette knows which view to land in. The shortcut
registry's merge-by-action keeps users' custom overrides intact (no extension
shortcut system exists — RESEARCH flag 3 resolved).

Purpose: satisfies PAL-02 (rebind without breaking overrides) and the PAL-01
two-entry-view model. This plan owns the shortcut + layout wiring ONLY; the
palette's consumption of `initialView` lands in Plan 06.
Output: a safe `palette-commands` binding + layout routing, unit-proven.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-command-palette-search/67-CONTEXT.md
@.planning/phases/67-command-palette-search/67-RESEARCH.md
@.planning/phases/67-command-palette-search/67-VALIDATION.md

# SUTs:
@web/src/lib/shortcuts.ts
@web/src/routes/(app)/+layout.svelte
@web/src/lib/__tests__/shortcuts.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add palette-commands binding + prove override safety</name>
  <files>web/src/lib/shortcuts.ts, web/src/lib/__tests__/shortcuts.test.ts</files>
  <behavior>
    - DEFAULT_SHORTCUTS contains `{key:"p", meta:true, shift:true, action:"palette-commands", label:"..."}`.
    - `palette` (Cmd+K) is unchanged.
    - `matchShortcut` for Ctrl/Cmd+Shift+P resolves to `palette-commands` and does NOT match plain Cmd+P or Cmd+K.
    - `loadCustomShortcuts` merge-by-action: a custom override for `palette` survives AND a custom override for `palette-commands` survives (both keyed by action).
  </behavior>
  <action>
    Append the `palette-commands` binding to `DEFAULT_SHORTCUTS` (after `palette`).
    Leave `palette` untouched. EXTEND web/src/lib/__tests__/shortcuts.test.ts:
    assert the new default exists, that shift-modifier matching distinguishes
    Cmd+Shift+P from Cmd+P/Cmd+K, and that the existing merge-by-action case
    (currently proves `palette` override survives) is extended to also prove a
    custom `palette-commands` override survives. Reuse the existing
    `merges custom overrides by action name` + shift-matching test scaffolding.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/lib/__tests__/shortcuts.test.ts</automated>
  </verify>
  <done>palette-commands default present, Cmd+Shift+P matches it uniquely, both palette and palette-commands overrides survive merge; shortcuts suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Layout switch arms + initialView pass-through</name>
  <files>web/src/routes/(app)/+layout.svelte, web/src/lib/__tests__/shortcuts.test.ts</files>
  <behavior>
    - The global keydown switch has `case "palette"` → open palette + paletteInitialView="search".
    - The switch has `case "palette-commands"` → open palette + paletteInitialView="commands".
    - The CommandPalette mount receives `initialView={paletteInitialView}`.
    - `e.preventDefault()` continues to run before the switch (suppresses the browser Cmd+Shift+P default).
  </behavior>
  <action>
    Add a `paletteInitialView` reactive state ("search" | "commands"). In the
    `matchShortcut → switch(action)` block (~L90): `case "palette"` sets
    open + paletteInitialView="search"; `case "palette-commands"` sets open +
    paletteInitialView="commands". Pass `initialView={paletteInitialView}` to the
    `CommandPalette` mount (the palette consumes it in Plan 06; passing an unknown
    prop is harmless until then). Keep the existing `e.preventDefault()` (L88).
    This task turns the Plan-01 `app-layout-palette-shortcut.test.ts` source-read
    suite GREEN — run it as the verify. (That file lives in web/src/__tests__/;
    it is owned/created by Plan 01, so do NOT recreate it — only make the source
    match its assertions.)
    NOTE on file ownership: this task edits `(app)/+layout.svelte` only; the
    listed `shortcuts.test.ts` in <files> is for the shared run, no edit needed
    beyond Task 1.
  </action>
  <verify>
    <automated>cd web && bun test src/__tests__/app-layout-palette-shortcut.test.ts</automated>
  </verify>
  <done>Both switch arms route to the correct initial view; initialView is passed to the palette mount; preventDefault preserved; the Plan-01 routing source-read suite is GREEN.</done>
</task>

</tasks>

<verification>
- `cd web && bunx vitest run src/lib/__tests__/shortcuts.test.ts` green.
- `cd web && bun test src/__tests__/app-layout-palette-shortcut.test.ts` green (turned green from Plan 01's RED).
- `cd web && bunx svelte-check` clean on `(app)/+layout.svelte`.
</verification>

<success_criteria>
Cmd+Shift+P opens the palette command-first via a new `palette-commands` action;
Cmd+K still opens it search-first; the layout passes the initial view to the
palette; custom overrides for both actions survive. Shortcut + routing suites
green.
</success_criteria>

<output>
After completion, create `.planning/phases/67-command-palette-search/67-04-SUMMARY.md`
</output>
