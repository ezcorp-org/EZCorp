---
phase: 67-command-palette-search
plan: 06
type: execute
wave: 3
depends_on: ["67-01", "67-03", "67-04", "67-05"]
files_modified:
  - web/src/lib/components/CommandPalette.svelte
  - web/src/lib/command-registry.ts
  - web/src/lib/__tests__/command-registry.test.ts
autonomous: true
requirements: [PAL-01, PAL-03, PAL-04, PAL-05, PAL-06, PAL-07]
must_haves:
  truths:
    - "Cmd+K opens the palette search-focused; Cmd+Shift+P opens it command-first (initialView prop honored)"
    - "Typing ≥2 chars renders matching commands AND cross-project message hits together in one scrolling list"
    - "Each message row shows snippet (sanitizeSnippet), role badge, match-type glyph, relative time"
    - "Arrow keys skip non-clickable project/conversation headers; Enter is row-type-aware (command runs action, hit deep-links via ?m=); ez: prefix still wins"
    - "On <lg the palette renders inside BottomSheet with identical section nesting, auto-focused input, and single-Escape close (no double focus-trap)"
    - "The old search-conversations command + its conversation-grained sub-view are removed"
  artifacts:
    - path: "web/src/lib/components/CommandPalette.svelte"
      provides: "unified heterogeneous flatItems palette (commands+hits), cross-project deep-link, BottomSheet at <lg"
      contains: "buildPaletteResults"
    - path: "web/src/lib/command-registry.ts"
      provides: "search-conversations command removed"
  key_links:
    - from: "web/src/lib/components/CommandPalette.svelte"
      to: "web/src/lib/search/palette-results.ts"
      via: "buildPaletteResults(matchingCommands, hits, activeConversationId)"
      pattern: "buildPaletteResults"
    - from: "web/src/lib/components/CommandPalette.svelte"
      to: "ChatThread ?m= deep-link"
      via: "goto(/project/${hit.projectId}/chat/${hit.conversationId}?m=...)"
      pattern: "\\?m="
    - from: "web/src/lib/components/CommandPalette.svelte"
      to: "BottomSheet.svelte"
      via: "wrap modal body in BottomSheet when useBreakpoint('lg').below"
      pattern: "BottomSheet"
---

<objective>
Extend `CommandPalette.svelte`'s existing `searchMode` sub-view into a unified
heterogeneous results model — matching commands AND cross-project message hits in
ONE scrolling, keyboard-navigable list — replacing the old conversation-grained
`search-conversations` flow. Reuse the existing a11y stack (createFocusTrap,
capture-Escape, arrow/Enter, backdrop, focus restore), the Plan-05 grouping
helper, Phase 66's `sanitizeSnippet` + match-type glyph, the `?m=` deep-link, and
wrap the modal in `BottomSheet` at `<lg`. This is the largest extension in the
phase and turns the Plan-01 RED `CommandPalette.component.test.ts` GREEN.

Purpose: satisfies PAL-01/03/04/05/06/07 in the UI layer. Extend — do NOT fork
(CONTEXT lock). Replace the old search command, not duplicate it.
Output: the unified message-grained, cross-project, mobile-aware palette.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-command-palette-search/67-CONTEXT.md
@.planning/phases/67-command-palette-search/67-RESEARCH.md
@.planning/phases/67-command-palette-search/67-VALIDATION.md

# The SUT to extend (a11y stack + searchMode sub-view + flatItems nav + ez: prefix):
@web/src/lib/components/CommandPalette.svelte
# The RED component suite this plan turns GREEN (the acceptance contract):
@web/src/__tests__/CommandPalette.component.test.ts
# Pure helper to consume (Plan 05):
@web/src/lib/search/palette-results.ts
# Reuse verbatim — sanitizer, mode grouping, glyph reference, mobile primitives:
@web/src/lib/search/snippet-sanitize.ts
@web/src/lib/components/ConversationList.svelte
@web/src/lib/components/BottomSheet.svelte
@web/src/lib/use-breakpoint.svelte.ts
# Extended client contract (scope + projectId/projectName from Plan 03):
@web/src/lib/api.ts
# Command factory + the search-conversations command to remove:
@web/src/lib/command-registry.ts
# Deep-link URL shape to mirror (handleSelect):
@web/src/routes/(app)/project/[id]/chat/[convId]/+page.svelte

<interfaces>
<!-- New props the palette must accept (wired by Plan 04's layout): -->
<!--   initialView: "search" | "commands"  (Cmd+K → "search"; Cmd+Shift+P → "commands") -->
<!--   activeConversationId: string | null (drives in-this-conversation grouping) -->
<!-- Consume buildPaletteResults(matchingCommands, hits, activeConversationId). -->
<!-- Deep-link: goto(`/project/${hit.projectId}/chat/${hit.conversationId}?m=${encodeURIComponent(hit.messageId)}`) -->
<!-- matchTypeGlyph: ≈ semantic / ⊕ both / " lexical (ConversationList.svelte L147-151) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the old search-conversations command + sub-view cleanly</name>
  <files>web/src/lib/command-registry.ts, web/src/lib/__tests__/command-registry.test.ts, web/src/lib/components/CommandPalette.svelte</files>
  <action>
    Remove together (Pitfall 6 — avoid dangling refs):
    - The `search-conversations` command def in command-registry.ts (L152-161).
    - In CommandPalette.svelte: the `if (cmd.id === "search-conversations")`
      branch (L125-130), the `selectSearchResult` (L137) +
      `doConversationSearch` (L156) functions, the conversation-search render
      block (~L322-344), and the `SearchResult` usages.
    DO NOT remove the `searchConversations` API function from api.ts — it is
    still used by `api-conversations` server routes + other component tests
    (verified: api-conversations.server.test.ts, command-palette-ask-ez). Only
    the palette's *import* of it is dropped if no longer referenced.
    Update web/src/lib/__tests__/command-registry.test.ts (L126 references the
    `search-conversations` command) to drop that assertion. Run svelte-check to
    confirm no unused-import / dangling-reference errors.
    Leave the rest of the search flow stubbed/compiling — Task 2 replaces it with
    the message-grained flow.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/lib/__tests__/command-registry.test.ts && bunx svelte-check --threshold error 2>&1 | tail -5</automated>
  </verify>
  <done>search-conversations command + its palette sub-view/handlers removed with no dangling references; searchConversations API fn untouched; command-registry.test.ts updated; svelte-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Unified flatItems model + cross-project deep-link + initialView</name>
  <files>web/src/lib/components/CommandPalette.svelte</files>
  <action>
    Accept new props `initialView: "search" | "commands"` and
    `activeConversationId: string | null`. On open, focus the search input when
    initialView==="search"; land in the command list when "commands".
    Replace the modal sub-view behavior: once query length ≥2, call
    `searchMessages(activeProjectId, query, { mode, scope: "all" })` (cross-project)
    and feed the hits + matching commands + activeConversationId to
    `buildPaletteResults` (Plan 05). Render its `sections` tree (project +
    conversation headers are non-clickable, render-only) and set the keyboard-nav
    model `flatItems` to the helper's actionable-rows-only `flatItems`. Widen
    `flatItems` typing to `(Command | MessageSearchHit)[]` and make the existing
    Enter handler row-type-aware via a discriminant (`"messageId" in item`):
    command → `cmd.action()` (existing); hit → `goto(`/project/${hit.projectId}/
    chat/${hit.conversationId}?m=${encodeURIComponent(hit.messageId)}`)` then
    reset + onclose (mirror handleSelect). Keep the `ez:` prefix winning.
    Extend the existing `flatIndex` helper to accept the heterogeneous item with
    a stable discriminant (Pitfall 2 — keep headers OUT of flatItems).
    Each message row renders: `{@html sanitizeSnippet(hit.snippet)}` (Pitfall 5 —
    never raw), a user/assistant role badge, the match-type glyph (copy
    `matchTypeGlyph` from ConversationList.svelte L147-151 — Phase 66 parity),
    and relative time. Empty results → single "No matching messages." Degraded
    response → inline non-blocking notice; NEVER call persistSearchMode on a
    degraded response (Phase 66 Pitfall 4). The empty-query state still shows the
    recent + grouped command list (unchanged).
    Run the Plan-01 component suite iteratively until green.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/__tests__/CommandPalette.component.test.ts</automated>
  </verify>
  <done>Typing ≥2 chars shows commands + cross-project hits together; arrow-nav skips headers; Enter is row-type-aware (deep-link uses hit.projectId); ez: prefix wins; snippets sanitized; glyph + role badge present; initialView honored; empty/degraded handled; component suite green.</done>
</task>

<task type="auto">
  <name>Task 3: BottomSheet mobile fallback at <lg (no double focus-trap)</name>
  <files>web/src/lib/components/CommandPalette.svelte</files>
  <action>
    Extract the palette body (search input + results sections) into a Svelte
    `{#snippet}` so it renders IDENTICALLY in both branches (Pitfall 4 — never
    flatten the project nesting on mobile). `const isMobile =
    useBreakpoint("lg").below;` Render
    `{#if isMobile}<BottomSheet open {onclose} ariaLabel="Search">{@render body()}</BottomSheet>{:else}<div role="dialog" aria-modal>{@render body()}</div>{/if}`.
    Gate the palette's OWN `createFocusTrap` (L247) + document-capture Escape
    (L252) install on `!isMobile` — BottomSheet owns focus-trap + Escape inside
    the sheet (Pitfall 3 — avoid double-trap / double-Escape). Auto-focus the
    search input on open even inside the sheet (an `$effect` after the sheet
    mounts), per CONTEXT. Desktop keeps the centered modal + its own a11y +
    focus-restore-on-close (PAL-06). Verify the Plan-01 component suite stays
    green and run svelte-check.
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/__tests__/CommandPalette.component.test.ts && bunx svelte-check --threshold error 2>&1 | tail -5</automated>
  </verify>
  <done>At <lg the same body renders inside BottomSheet (nesting preserved, input auto-focused, single focus-trap/Escape owned by the sheet); desktop keeps the modal + focus restore; component suite + svelte-check green.</done>
</task>

</tasks>

<verification>
- `cd web && bunx vitest run src/__tests__/CommandPalette.component.test.ts` green
  (the Plan-01 RED suite turned GREEN).
- `cd web && bunx vitest run src/lib/__tests__/command-registry.test.ts` green.
- `cd web && bunx svelte-check --threshold error` clean (no dangling
  search-conversations references, no unused imports).
- Deep-link builds from `hit.projectId` (cross-project), snippets go through
  `sanitizeSnippet`, glyph matches Phase 66, BottomSheet wraps at `<lg` without
  double focus-trap.
</verification>

<success_criteria>
The unified palette renders commands + cross-project message hits together,
keyboard-navigable (headers skipped), row-type-aware Enter with cross-project
`?m=` deep-link, Phase-66-parity snippet/glyph/role, initialView-aware open,
BottomSheet on mobile — the old search-conversations flow removed. Component +
registry suites + svelte-check green.
</success_criteria>

<output>
After completion, create `.planning/phases/67-command-palette-search/67-06-SUMMARY.md`
</output>
