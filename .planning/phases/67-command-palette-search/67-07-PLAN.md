---
phase: 67-command-palette-search
plan: 07
type: execute
wave: 4
depends_on: ["67-02", "67-06"]
files_modified:
  - web/e2e/command-palette-search.spec.ts
  - scripts/coverage-thresholds.json
autonomous: false
requirements: [PAL-01, PAL-05, PAL-06, PAL-07, PAL-02]
must_haves:
  truths:
    - "The full palette e2e spec passes on chromium AND mobile-chromium (Cmd+K, Cmd+Shift+P, cross-project deep-link, BottomSheet)"
    - "Every new Phase-67 source path is pinned at 100% per-file coverage in coverage-thresholds.json and the gate is green"
    - "A human confirms Cmd+Shift+P opens the palette command-first in a real browser without triggering the private-window shortcut"
  artifacts:
    - path: "web/e2e/command-palette-search.spec.ts"
      provides: "GREEN e2e spec on both Playwright projects"
    - path: "scripts/coverage-thresholds.json"
      provides: "100% per-file pins for palette-results.ts + new test-backed source paths"
      contains: "palette-results"
  key_links:
    - from: "scripts/coverage-thresholds.json"
      to: "web/src/lib/search/palette-results.ts"
      via: "100% per-file pin"
      pattern: "palette-results"
---

<objective>
Close Phase 67: turn the Plan-02 RED e2e spec GREEN against the now-built UI on
both `chromium` and `mobile-chromium`, then pin every new source path at 100%
per-file coverage in `scripts/coverage-thresholds.json` so the CI `coverage` job
gates it (project standard — MEMORY: "new features ship with 100% unit +
integration + e2e, CI-gated per-file on new paths"). Finally, a human verifies the
`Cmd+Shift+P` browser-shortcut behavior that automation cannot fully prove.

Purpose: satisfies the e2e layer + the per-file coverage gate that the rest of the
phase was written against; provides the final acceptance for PAL-01/05/06/07/02.
Output: green cross-viewport e2e + green coverage gate + human-verified shortcut.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-command-palette-search/67-CONTEXT.md
@.planning/phases/67-command-palette-search/67-RESEARCH.md
@.planning/phases/67-command-palette-search/67-VALIDATION.md

# The RED e2e spec to turn GREEN:
@web/e2e/command-palette-search.spec.ts
# Coverage gate mechanics (per-file pins + SF re-rooting + node-vitest leg):
@scripts/coverage-thresholds.json
# Precedent for how Phase 66 pinned its three search helpers (66-05 outcome in STATE):
@scripts/test-coverage.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the palette e2e spec GREEN on both projects</name>
  <files>web/e2e/command-palette-search.spec.ts</files>
  <action>
    Run the Plan-02 spec against the built UI. Rebuild + restart the Playwright
    preview server first (Phase 61/62 pattern — preview serves a baked bundle;
    SUT Svelte edits require `PI_SKIP_INIT=1 bun run build` + fresh preview).
    Fix any selector/timing drift in the spec ONLY (the UI is correct as of Plan
    06; prefer test-side adjustments). Confirm:
    - chromium: Cmd+K search-focus, Cmd+Shift+P command-first, mixed sections,
      cross-project click → `/project/<otherId>/chat/<convId>?m=` → scroll +
      pulse + `?m=` stripped.
    - mobile-chromium: BottomSheet render, non-flattened nesting, auto-focused
      input, single-Escape close.
    Run twice for flake-check on each project.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/command-palette-search.spec.ts --project=chromium --project=mobile-chromium</automated>
  </verify>
  <done>The full spec passes on both chromium and mobile-chromium across two consecutive runs (zero flake).</done>
</task>

<task type="auto">
  <name>Task 2: Pin new source paths at 100% per-file coverage</name>
  <files>scripts/coverage-thresholds.json</files>
  <action>
    Following the 66-05 precedent (which pinned the three Phase 66 search helpers),
    add exact 100% per-file pins for the new Phase-67 source paths that carry
    coverage: `web/src/lib/search/palette-results.ts` (pure, vitest/bun via the
    re-rooted SF leg), plus any other new test-backed source files
    (`web/src/routes/api/search/messages/schema.ts` scope additions are covered by
    the server test; `src/db/queries/message-search.ts` additions covered by the
    bun suite). Reconcile against real lcov — run the relevant suites with
    coverage and confirm the pinned files report the expected covered-line counts
    (mirror 66-05's "satisfied by real lcov" verification). Respect the
    `SF:src/→SF:web/src/` re-rooting and the node-vitest leg for vitest-only files
    (66-05 mechanics). Do NOT widen the gate to unrelated files (66-05 Rule-3
    lesson — narrow the shard to the target files to avoid pulling unrelated
    modules in as new violations).
  </action>
  <verify>
    <automated>bash scripts/test-coverage.sh 2>&1 | tail -30</automated>
  </verify>
  <done>palette-results.ts (and any other new Phase-67 source path) pinned at 100% in coverage-thresholds.json and satisfied by real lcov; the gate reports zero NEW violations vs baseline for Phase-67 paths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Cmd+Shift+P browser-shortcut behavior</name>
  <files>(none — manual verification only)</files>
  <what-built>The Cmd+Shift+P command-first palette entry (PAL-02). `e.preventDefault()` runs before the switch (layout) to suppress the browser default, and the chromium e2e asserts the palette opens — but the OS/browser private-window interception is environmental and can't be fully proven headless.</what-built>
  <action>
    PAUSE for the user. Ask them to run the manual verification steps below in a
    real browser (automation cannot prove the OS/browser private-window
    interception). Do not modify any files in this task.
  </action>
  <how-to-verify>
    1. Start the app (`bun run dev` or your usual local URL).
    2. In Chrome AND Firefox, press Cmd+Shift+P (Ctrl+Shift+P on Linux/Win).
    3. Confirm: the command palette opens command-first (search input NOT focused,
       command list visible) and NO private/incognito window appears.
    4. Press Cmd+K — confirm the palette opens with the search input focused.
    5. Type a query, click a result in a DIFFERENT project — confirm it navigates,
       scrolls to the message, and pulses.
  </how-to-verify>
  <verify>Manual — user confirms the five steps above in Chrome + Firefox.</verify>
  <done>User confirms Cmd+Shift+P opens the palette command-first with no private window, Cmd+K opens search-focused, and a cross-project result deep-links + pulses.</done>
  <resume-signal>Type "approved" or describe what happened (e.g. "private window opened in Firefox").</resume-signal>
</task>

</tasks>

<verification>
- `cd web && bunx playwright test e2e/command-palette-search.spec.ts
  --project=chromium --project=mobile-chromium` green, twice, zero flake.
- `bash scripts/test-coverage.sh` reports zero new violations for Phase-67 paths;
  palette-results.ts pinned 100%.
- Human confirms the Cmd+Shift+P browser behavior.
</verification>

<success_criteria>
Cross-viewport e2e green, per-file 100% coverage gate green on all new Phase-67
paths, and the browser-shortcut behavior human-verified — Phase 67 ready for
`/gsd:verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/67-command-palette-search/67-07-SUMMARY.md`
</output>
