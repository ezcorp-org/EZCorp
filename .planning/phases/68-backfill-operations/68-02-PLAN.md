---
phase: 68-backfill-operations
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - src/db/queries/message-embed-outbox.ts
autonomous: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "Calling enqueueEmbedJobIfAbsent twice for the same message leaves exactly one outbox row and never disturbs an existing pending/failed/in_progress row"
    - "getEmbedProgress returns accurate backlog-by-status counts and eligible-vs-embedded message coverage, excluding test=true conversations and system/tool roles"
  artifacts:
    - path: "src/db/queries/message-embed-outbox.ts"
      provides: "enqueueEmbedJobIfAbsent (DO NOTHING sibling of enqueueEmbedJob) + getEmbedProgress shared query helper"
      contains: "enqueueEmbedJobIfAbsent"
  key_links:
    - from: "src/db/queries/message-embed-outbox.ts"
      to: "message_embed_outbox (PK message_id)"
      via: "onConflictDoNothing on messageId target"
      pattern: "onConflictDoNothing"
    - from: "getEmbedProgress"
      to: "messages JOIN conversations + message_chunks"
      via: "eligible-vs-embedded aggregate with test/role exclusion"
      pattern: "c.test IS NULL OR c.test"
---

<objective>
Add the two genuinely-new query primitives this phase needs, co-located in the already-95%-gated `src/db/queries/message-embed-outbox.ts` (RESEARCH recommendation — avoids a new threshold entry): an `ON CONFLICT DO NOTHING` enqueue sibling for gaps-only backfill, and the shared `getEmbedProgress()` helper that is the single source of truth for the CLI, `--status`, and the admin endpoint.

Purpose: Turns the Plan 01 RED tests for `enqueueEmbedJobIfAbsent` and `getEmbedProgress` GREEN. These are the only real new logic in the phase — everything else composes shipped parts.
Output: Two exported functions + their structural-handle types, staying ≥95% per-file coverage.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-backfill-operations/68-RESEARCH.md

@src/db/queries/message-embed-outbox.ts
@src/db/queries/message-search.ts
@src/db/schema.ts

<interfaces>
<!-- Existing in message-embed-outbox.ts — extend, don't rewrite. -->
- `enqueueEmbedJob(tx: EmbedJobTx, messageId, conversationId)` at :54 — DO UPDATE. The new sibling differs ONLY by the conflict clause.
- `EmbedJobTx` at :16 — structural insert handle (DO UPDATE shape). The DO-NOTHING sibling needs a parallel `onConflictDoNothing` structural type.
- `DrainDb` at :109 — `{ execute(sql), delete(table).where(cond) }`. `getEmbedProgress(db: DrainDb)` reuses this handle so the PGlite test db + Bun.sql + drizzle all satisfy it.

Eligibility/test predicates to MIRROR VERBATIM (DRY — do not re-derive):
- `(c.test IS NULL OR c.test = false)` — message-search.ts:139/194/451
- `m.role IN ('user','assistant')` — message-search.ts:195/344/398
- non-whitespace: `length(trim(m.content)) > 0` (mirrors isEmbedEligible, message-chunker.ts:20)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add enqueueEmbedJobIfAbsent (DO NOTHING sibling)</name>
  <files>src/db/queries/message-embed-outbox.ts</files>
  <behavior>
    - First call for a messageId inserts a fresh pending row (attempts=0).
    - Second call for the SAME messageId is a no-op: exactly one row, and the existing row (whatever its status/attempts/nextAttemptAfter) is left byte-for-byte unchanged.
    - A pre-existing 'failed' (attempts=3) row survives an enqueueEmbedJobIfAbsent call unchanged — this is the whole point vs DO UPDATE.
    - Accepts the caller's handle; never calls getDb().
  </behavior>
  <action>
Add `enqueueEmbedJobIfAbsent(tx, messageId, conversationId)` directly below `enqueueEmbedJob` (:66). Mirror it exactly, swapping `.onConflictDoUpdate({...})` for `.onConflictDoNothing({ target: messageEmbedOutbox.messageId })`.

Add a parallel structural-handle type (e.g. `EmbedJobInsertTx`) whose `.values(...)` returns an object exposing `onConflictDoNothing(cfg: { target: typeof messageEmbedOutbox.messageId }) => Promise<unknown>` — mirror the existing `EmbedJobTx` JSDoc-with-PITFALL discipline (never getDb()). The top-level drizzle db AND a tx callback both satisfy it.

Document WHY a sibling (not a flag): gaps-only must NOT disturb already-pending/in_progress/failed/backed-off rows (RESEARCH Pattern 2). `--refresh-stale` (Plan 04) reuses the existing DO-UPDATE `enqueueEmbedJob` for the stale subset only — do NOT change `enqueueEmbedJob`.
  </action>
  <verify>
    <automated>bun test src/__tests__/backfill-embeddings.test.ts -t "enqueueEmbedJobIfAbsent"</automated>
  </verify>
  <done>enqueueEmbedJobIfAbsent exported; the Plan 01 idempotency + enqueueEmbedJobIfAbsent unit cases pass; enqueueEmbedJob unchanged (diff shows only additions).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add getEmbedProgress shared helper</name>
  <files>src/db/queries/message-embed-outbox.ts</files>
  <behavior>
    - Empty DB → all-zero backlog + zero coverage.
    - backlog counts pending / in_progress / failed separately, total = sum.
    - coverage.eligibleMessages = COUNT of user/assistant, non-test-conversation, non-whitespace messages; coverage.embeddedMessages = COUNT DISTINCT messages having ≥1 message_chunks row (a 2-chunk message counts once).
    - test=true conversation messages and system/tool messages excluded from eligibleMessages; NULL test counts as eligible.
  </behavior>
  <action>
Add `getEmbedProgress(db: DrainDb): Promise<EmbedProgress>` and the exported `EmbedProgress` interface from the Plan 01 <interfaces> block (`{ backlog: { pending, inProgress, failed, total }, coverage: { eligibleMessages, embeddedMessages } }`).

Implement with `db.execute(sql\`...\`)` (Claude's discretion per CONTEXT: one aggregate vs two counts — prefer a small number of statements). Backlog: `SELECT status, COUNT(*) FROM message_embed_outbox GROUP BY status` then fold into the typed shape (default missing statuses to 0). Coverage: one query for eligible count over `messages m JOIN conversations c` using the MIRRORED predicates, one for `COUNT(DISTINCT mc.message_id)` of chunked-AND-eligible messages (join message_chunks). Reuse the `result.rows ?? result` row-unwrap idiom already used by claimBatch (:143).

Add unit cases as needed to hold the file ≥95% (cover the missing-status default branch and the empty-DB branch — `bun` ignores c8 fences, every line counts, RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>bun test src/__tests__/embed-progress.test.ts</automated>
  </verify>
  <done>getEmbedProgress + EmbedProgress exported; all five embed-progress.test.ts states pass; `bun test src/__tests__/message-embed-outbox*.test.ts` (existing suite) still green.</done>
</task>

</tasks>

<verification>
- `bun test src/__tests__/embed-progress.test.ts` GREEN.
- `bun test src/__tests__/backfill-embeddings.test.ts -t "enqueueEmbedJobIfAbsent"` GREEN (the rest of that file stays RED until Plan 04).
- Coverage delta: confirm `src/db/queries/message-embed-outbox.ts` stays ≥95% in isolation (run the per-file coverage leg for this file; isolate from the ~68 pre-existing baseline violations).
- `git diff` shows enqueueEmbedJob (DO UPDATE) untouched.
</verification>

<success_criteria>
- OPS-01 idempotent enqueue primitive + OPS-04 progress query both exist, GREEN, ≥95% covered, co-located (no new coverage-threshold entry needed).
- Eligibility/test predicates mirror message-search.ts verbatim (DRY).
</success_criteria>

<output>
After completion, create `.planning/phases/68-backfill-operations/68-02-SUMMARY.md`
</output>
