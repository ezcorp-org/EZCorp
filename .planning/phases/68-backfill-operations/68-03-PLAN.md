---
phase: 68-backfill-operations
plan: 03
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - src/extensions/embed-worker.ts
  - src/__tests__/embed-worker.test.ts
autonomous: true
requirements: [OPS-03]
must_haves:
  truths:
    - "After a drain pass that claims jobs and clears the backlog to zero, the worker runs ANALYZE message_chunks so PGlite's planner has fresh stats"
    - "ANALYZE does NOT fire on an empty tick or a tick that leaves backlog remaining"
    - "background-timers.test.ts stays green unchanged — the EmbedWorker start/stop surface is untouched"
  artifacts:
    - path: "src/extensions/embed-worker.ts"
      provides: "ANALYZE message_chunks hook inside tickOnce() after a backlog-clearing drain"
      contains: "ANALYZE message_chunks"
  key_links:
    - from: "EmbedWorker.tickOnce()"
      to: "ANALYZE message_chunks"
      via: "db.execute(sql) gated on a non-empty drain that cleared backlog"
      pattern: "ANALYZE message_chunks"
---

<objective>
Add the OPS-03 planner-stats maintenance: run `ANALYZE message_chunks` inside `EmbedWorker.tickOnce()` after a drain pass that cleared a meaningful backlog. PGlite has no autovacuum, so without this the HNSW/FTS planner goes stale at scale after a big backfill.

Purpose: Keep the query planner honest post-backfill. CRITICAL constraint (RESEARCH Pitfall 1): the hook MUST live in `tickOnce()`, NOT `start()`/`stop()` — the hand-written `background-timers.test.ts` EmbedWorker stub (:136-146) only models start/stop and asserts `intervalCalls`; touching that surface breaks it and can leak a stray `.pid` lockfile.
Output: A gated ANALYZE call in `tickOnce()` + extended worker tests + an explicit bg-timers regression guard.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-backfill-operations/68-RESEARCH.md

@src/extensions/embed-worker.ts
@src/__tests__/embed-worker.test.ts
@src/__tests__/background-timers.test.ts

<interfaces>
<!-- tickOnce() at embed-worker.ts:304 — current end-of-drain returns
     { claimed: rows.length, embedded, failed, skipped: 0 } at :404.
     The ANALYZE gate goes just BEFORE that return, inside the try block,
     after the per-row drain loop. -->
- `db` is the local `getDb()` handle already in scope at :317.
- `claimBatch(db, batchSize)` returns the claimed rows; `rows.length === 0` early-returns at :337 (the empty-tick path — must NOT run ANALYZE).
- Post-drain backlog probe: `SELECT COUNT(*) FROM message_embed_outbox WHERE status='pending' AND (next_attempt_after IS NULL OR next_attempt_after <= NOW())` — same claimability predicate as claimBatch (:136-137).
- PGlite ANALYZE is live-verified: `await db.execute(sql\`ANALYZE message_chunks\`)` works.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ANALYZE-after-backlog-clear in tickOnce() (OPS-03)</name>
  <files>src/extensions/embed-worker.ts</files>
  <behavior>
    - A tick that claims ≥1 row AND leaves zero claimable-pending rows afterward runs ANALYZE message_chunks exactly once.
    - An empty tick (claimed 0) does NOT run ANALYZE.
    - A tick that drains some but leaves claimable-pending rows remaining does NOT run ANALYZE (it fires on the LATER tick that finally clears the backlog).
    - A degraded-mode tick (embedder not ready) does NOT run ANALYZE.
  </behavior>
  <action>
Inside `tickOnce()`, after the per-row drain loop and BEFORE the `return { claimed: rows.length, ... }` at :404: probe the remaining claimable-pending backlog via `db.execute(sql\`SELECT COUNT(*)::int AS n FROM message_embed_outbox WHERE status = 'pending' AND (next_attempt_after IS NULL OR next_attempt_after <= NOW())\`)`, unwrap with the `result.rows ?? result` idiom (matches claimBatch :143). If the count is 0 (backlog cleared by THIS non-empty drain — we only reach here when rows.length > 0), run `await db.execute(sql\`ANALYZE message_chunks\`)` wrapped in its own try/catch that logs a warn on failure (ANALYZE must never crash a tick or fail the drain — mirror the tick-level catch discipline at :405).

CONTEXT discretion: chose the `backlog == 0 after a non-empty drain` trigger (RESEARCH Open Question 2 recommendation: simple, self-limiting, won't fire every tick on a quiet system). Do NOT add an every-N counter unless the simpler gate can't be tested cleanly.

DO NOT touch `start()`, `stop()`, the constructor, the interval wiring, or the PID-lockfile helpers. The change is entirely local to the `tickOnce()` try block. Keep added branches covered (≥95% gate on this file — the no-ANALYZE-on-remaining-backlog branch and the ANALYZE-failure catch both need a test).
  </action>
  <verify>
    <automated>bun test src/__tests__/embed-worker.test.ts</automated>
  </verify>
  <done>tickOnce runs ANALYZE message_chunks only after a backlog-clearing non-empty drain; new + existing embed-worker.test.ts cases pass; `embed-worker.ts` stays ≥95% in isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend embed-worker.test.ts ANALYZE cases + bg-timers regression guard (OPS-03)</name>
  <files>src/__tests__/embed-worker.test.ts</files>
  <behavior>
    - ANALYZE fires once after a tick that drains the last pending job(s).
    - ANALYZE does NOT fire on: empty tick, partial drain leaving claimable rows, degraded tick.
  </behavior>
  <action>
Extend `src/__tests__/embed-worker.test.ts` (read it first — it already drives `tickOnce()` directly via the PGlite harness, :139-227). Add cases that spy on `db.execute` to detect the `ANALYZE message_chunks` statement. Practical approach: seed N pending outbox rows + their messages so the worker actually embeds them, drive `tickOnce()`, and assert that after the tick that clears the last row an `ANALYZE message_chunks` statement was issued; for the partial-drain case, set batchSize smaller than the pending count and assert NO ANALYZE on the first tick but YES on the tick that clears the remainder; for the empty-tick case assert no ANALYZE. Detect ANALYZE either by spying `db.execute` (wrap/inspect the SQL chunks) or by an `EXPLAIN`-free assertion if a spy is awkward — match whatever the existing test file's spying conventions are (do NOT invent a new mocking style).

This plan must NOT modify `background-timers.test.ts`. Add an explicit regression guard to THIS plan's verification (below) per RESEARCH Pitfall 1 — gsd-verifier only runs the new file and would miss a bg-timers break.
  </action>
  <verify>
    <automated>bun test src/__tests__/embed-worker.test.ts src/__tests__/background-timers.test.ts</automated>
  </verify>
  <done>All four ANALYZE-gate behaviors covered and GREEN; background-timers.test.ts passes UNCHANGED (no edits, no new intervalCalls drift, no stray .pid lockfile).</done>
</task>

</tasks>

<verification>
- `bun test src/__tests__/embed-worker.test.ts src/__tests__/background-timers.test.ts` GREEN.
- `git diff src/extensions/embed-worker.ts` shows changes ONLY inside `tickOnce()` (no start/stop/constructor/interval edits).
- `git diff src/__tests__/background-timers.test.ts` is EMPTY.
- `embed-worker.ts` coverage ≥95% in isolation.
</verification>

<success_criteria>
- OPS-03: ANALYZE message_chunks runs after backlog-clearing drains, never on empty/partial/degraded ticks.
- The bg-timers EmbedWorker stub remains valid (start/stop surface untouched).
</success_criteria>

<output>
After completion, create `.planning/phases/68-backfill-operations/68-03-SUMMARY.md`
</output>
