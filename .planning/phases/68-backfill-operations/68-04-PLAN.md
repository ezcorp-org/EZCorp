---
phase: 68-backfill-operations
plan: 04
type: execute
wave: 3
depends_on: ["68-01", "68-02"]
files_modified:
  - scripts/backfill-embeddings.ts
  - package.json
autonomous: true
requirements: [OPS-01, OPS-02, OPS-04]
must_haves:
  truths:
    - "A bare `bun run scripts/backfill-embeddings.ts` enqueues embed jobs for every eligible (user/assistant, non-test, non-empty) message that has no chunks and no outbox row, across all projects"
    - "Re-running after a kill is safe and adds zero duplicates; --dry-run plans without writing"
    - "Backfill enqueues in paced --batch-size chunks with --sleep-ms between, configurable via EZCORP_BACKFILL_* env (flag > env > default)"
    - "--status prints {backlog, coverage} without enqueuing; worker-down is warned loudly then enqueue proceeds anyway"
  artifacts:
    - path: "scripts/backfill-embeddings.ts"
      provides: "Operator CLI mirroring sweep-perm-expiry.ts: parseArgs/runBackfill/main, gaps-only paced enqueue, --status, --refresh-stale, --project, worker-down detection, JSON summary on stdout, exit codes 0/1/2"
      min_lines: 150
      exports: ["parseArgs", "runBackfill", "main"]
    - path: "package.json"
      provides: "backfill:embeddings script alias alongside verify:*"
      contains: "backfill:embeddings"
  key_links:
    - from: "scripts/backfill-embeddings.ts"
      to: "src/db/queries/message-embed-outbox.ts (enqueueEmbedJobIfAbsent, enqueueEmbedJob, getEmbedProgress)"
      via: "import + per-batch enqueue + status/progress"
      pattern: "enqueueEmbedJobIfAbsent|getEmbedProgress"
    - from: "scripts/backfill-embeddings.ts"
      to: "src/db/connection (initDb/getDb) + .ezcorp/embed-worker.pid"
      via: "DATABASE_URL→PGlite fallback + PID liveness probe"
      pattern: "embed-worker.pid|isProcessAlive"
---

<objective>
Build the operator CLI `scripts/backfill-embeddings.ts` — the headline deliverable: one resumable, idempotent script that indexes an existing install's entire eligible history. Modeled byte-for-byte on `scripts/sweep-perm-expiry.ts` (shebang, initDb/getDb, flag parse, JSON-on-stdout, verbose-on-stderr, exit codes 0/1/2). Enqueue-only — the existing EmbedWorker drains.

Purpose: OPS-01 (gaps-only idempotent enqueue) + OPS-02 (paced/throttled batches) + the CLI half of OPS-04 (`--status` + live progress lines). Composes the Plan 02 primitives (`enqueueEmbedJobIfAbsent`, `getEmbedProgress`) — no new embedding logic.
Output: The script + a `backfill:embeddings` package.json alias; turns the remaining Plan 01 RED cases GREEN.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-backfill-operations/68-CONTEXT.md
@.planning/phases/68-backfill-operations/68-RESEARCH.md

@scripts/sweep-perm-expiry.ts
@src/db/queries/message-embed-outbox.ts
@src/db/queries/message-search.ts
@src/extensions/embed-worker.ts
@src/db/schema.ts

<interfaces>
<!-- Plan 01 pins these exact exports; Plan 02 provides the query primitives. -->
From src/db/queries/message-embed-outbox.ts (Plan 02):
  enqueueEmbedJobIfAbsent(tx, messageId, conversationId)  // DO NOTHING — gaps-only
  enqueueEmbedJob(tx, messageId, conversationId)          // DO UPDATE  — --refresh-stale only
  getEmbedProgress(db): Promise<EmbedProgress>            // --status + live lines

From src/db/connection: initDb(): Promise<void>; getDb(): DrainDb (DATABASE_URL→PGlite fallback)
From src/memory/embeddings.ts: EMBEDDING_MODEL_ID  // stale-model comparison key for --refresh-stale

Worker-down signals (RESEARCH Pitfall 3 — script is a SEPARATE process, can't see in-memory worker):
  - process.env.EZCORP_DISABLE_EMBED_WORKER === "1"
  - .ezcorp/embed-worker.pid lockfile (path const at embed-worker.ts:65) + process.kill(pid, 0) liveness (idiom embed-worker.ts:428-437)

CLI contract to MIRROR (sweep-perm-expiry.ts): parseArgs→ParsedArgs|{error}; printHelp(); main():Promise<number>
calling `await initDb(); const db = getDb();`; JSON summary via process.stdout.write(JSON.stringify(.,null,2));
verbose/live lines via process.stderr.write; top-level `const code = await main(); process.exit(code);`.
Exit: 0 ran (apply or dry-run or status), 1 per-item error, 2 invocation error.

Gaps-only SELECT (RESEARCH Pattern 3 — mirror message-search predicates VERBATIM):
  SELECT m.id, m.conversation_id FROM messages m JOIN conversations c ON c.id = m.conversation_id
  WHERE m.role IN ('user','assistant') AND (c.test IS NULL OR c.test = false)
    AND length(trim(m.content)) > 0
    AND NOT EXISTS (SELECT 1 FROM message_chunks mc WHERE mc.message_id = m.id)
    AND NOT EXISTS (SELECT 1 FROM message_embed_outbox o WHERE o.message_id = m.id)
    -- AND c.project_id = $projectId  when --project given
  ORDER BY m.created_at ASC  [keyset or LIMIT/OFFSET page]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI shell + gaps-only paced enqueue + env knobs (OPS-01, OPS-02)</name>
  <files>scripts/backfill-embeddings.ts</files>
  <behavior>
    - parseArgs handles every flag (--dry-run/-n, --verbose/-v, --status, --refresh-stale, --project <id>, --batch-size <n>, --sleep-ms <n>, --help/-h→{error:"help"}, unknown→{error}).
    - runBackfill(db, opts): selects gaps via the mirrored predicate, enqueues each via enqueueEmbedJobIfAbsent in --batch-size pages with await Bun.sleep(sleepMs) between pages; returns {enqueued, eligibleScanned}; dryRun=true writes nothing but returns the would-enqueue count.
    - EZCORP_BACKFILL_BATCH_SIZE / EZCORP_BACKFILL_SLEEP_MS parse with the defensive idiom (undefined/empty→default, non-finite/≤0→default, clamp floor); flag overrides env overrides default.
    - exit codes 0 (ran/dry-run/status) / 1 (per-item error) / 2 (invocation error).
  </behavior>
  <action>
Create `scripts/backfill-embeddings.ts` with `#!/usr/bin/env bun`. Copy the structure of sweep-perm-expiry.ts. Export `parseArgs`, `runBackfill`, and `main` (so Plan 01 tests import them directly — sweep keeps them module-private, but THIS script exports them for unit testing per VALIDATION Wave 0 note).

`parseArgs(argv)`: build the ParsedArgs shape from the Plan 01 <interfaces> block; `--project`/`--batch-size`/`--sleep-ms` consume the next argv token (validate `--batch-size`/`--sleep-ms` parse to a positive int else {error}); collect unknowns → {error}.

Env resolvers `getBackfillBatchSize()` / `getBackfillSleepMs()`: mirror embed-worker.ts:98-104 exactly (DEFAULT + MIN floors — pick sane traffic-yielding defaults, e.g. batch 100 / sleep 200ms; CONTEXT discretion). The effective value = flag ?? env ?? default.

`runBackfill(db, opts)`: page the gaps-only SELECT (keyset by created_at ASC recommended, OFFSET acceptable given idempotency — RESEARCH Open Q3). For each row call `await enqueueEmbedJobIfAbsent(db, id, conversationId)`. Between pages `await Bun.sleep(opts.sleepMs)` (OPS-02 throttle). Call `opts.onProgress?.({enqueued, eligible, backlog})` periodically. When `opts.refreshStale` is set, ALSO run a stale-model pass: select messages whose chunks carry `embedding_model_id != EMBEDDING_MODEL_ID` and call the DO-UPDATE `enqueueEmbedJob` for those (resets to pending; worker deletes stale chunks before re-inserting — RESEARCH Pitfall 5). `--project` narrows both passes via `c.project_id = $projectId`.

Do NOT import generateEmbedding/chunkByTokens/EmbedWorker — enqueue-only (RESEARCH anti-pattern). Do NOT call getDb() inside enqueue helpers — pass the db handle.
  </action>
  <verify>
    <automated>bun test src/__tests__/backfill-embeddings.test.ts</automated>
  </verify>
  <done>parseArgs + runBackfill + env resolvers exported and GREEN against the full Plan 01 suite; gaps-only/idempotency/dry-run/env-parse/exit-code cases pass; no embedding loop in the script.</done>
</task>

<task type="auto">
  <name>Task 2: main() wiring — JSON summary, --status, worker-down warn, live lines, package.json alias (OPS-04)</name>
  <files>scripts/backfill-embeddings.ts, package.json</files>
  <action>
Complete `main()`: parse args (help→print+0, error→stderr+printHelp+2). `await initDb(); const db = getDb();`.

`--status` branch: `const p = await getEmbedProgress(db); process.stdout.write(JSON.stringify(p, null, 2) + "\n"); return 0;` — enqueues nothing.

Default/apply + --dry-run branch: detect worker-down via `process.env.EZCORP_DISABLE_EMBED_WORKER === "1"` OR an absent/dead PID lockfile — read `.ezcorp/embed-worker.pid` (path const from embed-worker.ts:65; if not exported, inline the same relative path), parse the pid, `process.kill(pid, 0)` liveness (copy the isProcessAlive idiom embed-worker.ts:428-437). If worker-down, `process.stderr.write` a LOUD warning that jobs won't drain until the worker runs, then proceed to enqueue anyway (CONTEXT lock — resumable). Call `runBackfill` with `onProgress` writing periodic "enqueued N / M eligible, backlog X" JSON-lines to STDERR (Pitfall 4 — keep stdout a single JSON doc). On completion write the final summary JSON to stdout: `{ dryRun, enqueued, eligibleScanned, backlog, coverage }` (fetch final getEmbedProgress for backlog/coverage). Wrap per-item enqueue in try/catch collecting errors; `return errors.length === 0 ? 0 : 1`.

Top-level: `const code = await main(); process.exit(code);`.

Add to package.json scripts (alongside verify:* at lines 20-25): `"backfill:embeddings": "bun run scripts/backfill-embeddings.ts"`.
  </action>
  <verify>
    <automated>bun test src/__tests__/backfill-embeddings.test.ts</automated>
  </verify>
  <done>main exported and GREEN; --status returns progress JSON without enqueuing; worker-down path warns-then-enqueues; live lines on stderr / summary JSON on stdout; `bun run backfill:embeddings --help` exits 0 and prints usage; package.json has the alias.</done>
</task>

</tasks>

<verification>
- `bun test src/__tests__/backfill-embeddings.test.ts` fully GREEN.
- `bun run scripts/backfill-embeddings.ts --help` → usage + exit 0; `--bogus` → stderr + exit 2.
- `bun run scripts/backfill-embeddings.ts --status` → single JSON doc on stdout, nothing enqueued.
- stdout is a single parseable JSON doc; all progress/verbose/warn output goes to stderr.
- No import of generateEmbedding/chunkByTokens/EmbedWorker (grep clean).
</verification>

<success_criteria>
- OPS-01: gaps-only, idempotent, resumable whole-install backfill; --dry-run plans only; --refresh-stale handled.
- OPS-02: paced batches with EZCORP_BACKFILL_* knobs (flag>env>default).
- OPS-04 (CLI half): --status + live progress lines via the shared getEmbedProgress.
- CLI shape matches sweep-perm-expiry.ts; backfill:embeddings alias added.
</success_criteria>

<output>
After completion, create `.planning/phases/68-backfill-operations/68-04-SUMMARY.md`
</output>
