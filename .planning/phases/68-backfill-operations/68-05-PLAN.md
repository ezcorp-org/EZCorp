---
phase: 68-backfill-operations
plan: 05
type: execute
wave: 3
depends_on: ["68-01", "68-02"]
files_modified:
  - web/src/routes/api/admin/embed-progress/+server.ts
  - web/src/__tests__/api-admin-embed-progress.server.test.ts
  - web/src/routes/(app)/admin/dashboard/+page.svelte
autonomous: true
requirements: [OPS-04]
must_haves:
  truths:
    - "An admin (scope=admin + role=admin) GET /api/admin/embed-progress returns {backlog, coverage} from the shared getEmbedProgress helper; a non-admin or out-of-scope caller is rejected"
    - "The admin dashboard renders a read-only embedding-progress card showing outbox backlog depth and message_chunks coverage, fetched on mount"
  artifacts:
    - path: "web/src/routes/api/admin/embed-progress/+server.ts"
      provides: "admin-gated GET returning getEmbedProgress() output"
      exports: ["GET"]
    - path: "web/src/routes/(app)/admin/dashboard/+page.svelte"
      provides: "read-only embedding-progress status card fetched from /api/admin/embed-progress on mount"
      contains: "embed-progress"
  key_links:
    - from: "web/src/routes/api/admin/embed-progress/+server.ts"
      to: "src/db/queries/message-embed-outbox.ts (getEmbedProgress)"
      via: "$server alias import"
      pattern: "getEmbedProgress"
    - from: "web/src/routes/(app)/admin/dashboard/+page.svelte"
      to: "/api/admin/embed-progress"
      via: "fetch on mount (mirror fetchSystem)"
      pattern: "/api/admin/embed-progress"
---

<objective>
Ship the OPS-04 admin surface: a thin admin-scoped `GET /api/admin/embed-progress` endpoint returning `{ backlog, coverage }` from the shared `getEmbedProgress()` (Plan 02), plus a small read-only status card on the existing admin dashboard. No live streaming, no toast (that's deferred POLISH-02).

Purpose: The web half of OPS-04 — operators watch the backlog drain from the admin UI. Composes the Plan 02 query via the established admin-endpoint auth pattern; runs parallel with Plan 04 (web-only, zero file overlap).
Output: endpoint + server test + dashboard card. Coverage bar = unit + integration (server vitest + optional component); NO e2e.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-backfill-operations/68-CONTEXT.md
@.planning/phases/68-backfill-operations/68-RESEARCH.md

@web/src/routes/api/admin/system/+server.ts
@web/src/__tests__/api-admin-system.server.test.ts
@web/src/routes/(app)/admin/dashboard/+page.svelte
@src/db/queries/message-embed-outbox.ts

<interfaces>
<!-- Admin auth pattern (system/+server.ts, verified) -->
  const scopeErr = requireScope(locals, "admin"); if (scopeErr) return scopeErr;
  try { requireRole(locals, "admin"); /* work */ return json(...); }
  catch (e) { if (e instanceof Response) return e; throw e; }
  // requireScope from "$lib/server/security/api-keys"; requireRole from "$server/auth/middleware"

<!-- $server alias resolves to ../src (web/svelte.config.js:7-9) -->
  import { getEmbedProgress } from "$server/db/queries/message-embed-outbox";

<!-- getEmbedProgress return shape (Plan 02): -->
  { backlog: { pending, inProgress, failed, total }, coverage: { eligibleMessages, embeddedMessages } }
<!-- NOTE: getEmbedProgress(db) takes a db handle in the backend signature.
     If the endpoint can't easily obtain getDb() in the web runtime, follow
     the SAME handle-acquisition the sibling admin queries use (analytics
     helpers call getDb() internally). Plan 02 may expose a zero-arg overload
     or the endpoint passes the server getDb() — match whatever the existing
     admin analytics queries do for DB access in the web runtime. -->

<!-- dashboard fetch pattern (+page.svelte): fetchSystem() does
  const res = await fetch("/api/admin/system"); if (res.ok) systemData = await res.json();
  wired into refreshAll() Promise.all + onMount. Mirror this for embed-progress. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/admin/embed-progress endpoint + server test (OPS-04)</name>
  <files>web/src/routes/api/admin/embed-progress/+server.ts, web/src/__tests__/api-admin-embed-progress.server.test.ts</files>
  <behavior>
    - scope!=admin → the requireScope rejection Response (not 200).
    - role!=admin → rejected (requireRole throws a Response, caught + returned).
    - admin caller → json({ backlog, coverage }) from getEmbedProgress.
  </behavior>
  <action>
Create `web/src/routes/api/admin/embed-progress/+server.ts` copying `admin/system/+server.ts` verbatim, swapping the body for `const progress = await getEmbedProgress(...); return json(progress);` imported from `$server/db/queries/message-embed-outbox`. Match the existing admin analytics queries' DB-handle acquisition (they call getDb() internally in the web runtime) — if getEmbedProgress requires a handle, obtain it the same way the sibling admin query helpers do; do NOT invent a new connection path.

Create `web/src/__tests__/api-admin-embed-progress.server.test.ts` mirroring `api-admin-system.server.test.ts` (read it first for the exact locals-mock + requireScope/requireRole stubbing + getEmbedProgress mock conventions). Cover the three behaviors above. This runs under web vitest (MUST run from web/ per project memory).

Add `web/src/routes/api/admin/embed-progress/+server.ts` to `scripts/coverage-thresholds.json` if the project gates new admin endpoints (siblings like search/messages/+server.ts are pinned at 100% — match the convention for this new endpoint; check whether the existing admin/system endpoint is pinned and follow suit).
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/__tests__/api-admin-embed-progress.server.test.ts</automated>
  </verify>
  <done>Endpoint gates on scope+role and returns the progress JSON; server test GREEN (3 cases); import resolves via $server alias.</done>
</task>

<task type="auto">
  <name>Task 2: Read-only embedding-progress card on admin dashboard (OPS-04)</name>
  <files>web/src/routes/(app)/admin/dashboard/+page.svelte</files>
  <action>
Add a small read-only status card to the admin dashboard. Add `embedProgress = $state<{ backlog: {...}; coverage: {...} } | null>(null)` and a `fetchEmbedProgress()` mirroring `fetchSystem()` (`const res = await fetch("/api/admin/embed-progress"); if (res.ok) embedProgress = await res.json();`). Wire it into `refreshAll()`'s `Promise.all([...])`. Render a card (match the existing dashboard card styling — `rounded-lg border` etc.) under the appropriate tab (e.g. the "system" tab) showing: backlog depth (pending / in_progress / failed / total) and coverage (`embeddedMessages` / `eligibleMessages`, optionally a percent). Use a `data-testid="embed-progress-card"` hook so a component test (or future check) can target it. Read-only — no buttons, no streaming, no toast (deferred POLISH-02).

Optional (per CONTEXT coverage bar — component test only if the dashboard is otherwise gated): if a `web/src/__tests__/<dashboard>.component.test.ts` convention covers this card, add a minimal render assertion that the card shows the fetched backlog/coverage numbers. Skip if the dashboard page isn't component-coverage-gated (CONTEXT marks the card test "optional per coverage bar").
  </action>
  <verify>
    <automated>cd web && bunx vitest run src/__tests__/api-admin-embed-progress.server.test.ts</automated>
  </verify>
  <done>Dashboard fetches /api/admin/embed-progress on mount and renders a read-only card with backlog depth + coverage; svelte-check clean on the edited page; no new e2e.</done>
</task>

</tasks>

<verification>
- `cd web && bunx vitest run src/__tests__/api-admin-embed-progress.server.test.ts` GREEN (run FROM web/).
- `cd web && bunx svelte-check` shows no NEW errors on the two edited web files (isolate from pre-existing baseline).
- Endpoint uses requireScope + requireRole (grep confirms); no bespoke auth.
- No e2e added (CONTEXT lock).
</verification>

<success_criteria>
- OPS-04 (web half): admin-gated GET returning getEmbedProgress output + read-only dashboard card.
- Reuses the shared getEmbedProgress (DRY — single source of truth across CLI + endpoint).
- Coverage bar = unit + integration only; new endpoint pinned per the admin-endpoint convention.
</success_criteria>

<output>
After completion, create `.planning/phases/68-backfill-operations/68-05-SUMMARY.md`
</output>
