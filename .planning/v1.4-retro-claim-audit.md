---
phase: 60
created: 2026-05-12
status: complete
---

# v1.4 Retro-Claim Audit

**Phase:** 60-audit-claim-docs-polish
**Audited:** 2026-05-12
**HEAD sha:** ebf1ed1f726b059e517b61fab25519a6523d3ba5
**Scope:** 6 retro-claimed v1.4 commits (`cae5c06`, `14b16ca`, `f6c3790`, `7cc5efc`, `b652f2b`, `763d718`).
**Methodology:** adapted from `.planning/v1.4-backend-test-triage.md` (Phase 59 precedent). Per-commit invariant check on (a) failure-path test, (b) doc surface, (c) audit row on block-path — definitions per `.planning/phases/60-audit-claim-docs-polish/60-CONTEXT.md`.

**Audited 6 commits: 6 invariants-met, 0 gaps**

<!-- HTML anchor before each h3 keeps PROJECT.md cross-links renderer-independent. -->

## Verdicts

<a id="cae5c06"></a>
### `cae5c06` — feat(extensions): hard-fail install on `*_API_KEY` env grants

- **Failure-path test:** yes
  - `src/extensions/__tests__/env-key-leak-install-block.test.ts:86` ("single *_API_KEY env name → returns EnvKeyLeakInstallError + 1 audit row")
  - Verify: `bun test ./src/extensions/__tests__/env-key-leak-install-block.test.ts -t 'returns EnvKeyLeakInstallError'`
- **Doc surface:** updated
  - `docs/extensions/AUTHORING.md:163` (§ "The env-key-leak install gate (HARD)"); also L9, L293, L313-315
  - Verify: `grep -n 'env-key-leak\|_API_KEY\|TOKEN\|SECRET' docs/extensions/AUTHORING.md`
- **Audit row on block-path:** yes
  - Constant: `src/extensions/audit-actions.ts:332` (`EXT_AUDIT_ACTIONS.ENV_KEY_LEAK_INSTALL_BLOCKED`)
  - Emission: `src/extensions/clamp-permissions.ts:354-383` (`checkEnvKeyLeakInstallGate`)
  - Assertion: `src/extensions/__tests__/env-key-leak-install-block.test.ts:86,113` (1 row per leaked name)
- **Verdict:** invariants-met
- **Gap:** none

<a id="14b16ca"></a>
### `14b16ca` — feat(extensions): user-configurable memory compaction interval

- **Failure-path test:** yes
  - `src/__tests__/memory-extractor-settings-migration.test.ts:202` ("legacy=4 (unsupported) → clamps to nearest supported value (3)"); also L213, L223, L267-279
  - Verify: `bun test ./src/__tests__/memory-extractor-settings-migration.test.ts -t 'clamps to nearest'`
- **Doc surface:** n/a — internal user-setting; in-product UI is the surface (no per-extension docs page enumerates the key); was renamed to snake_case in `b652f2b` two commits later before reaching any user-facing surface
- **Audit row on block-path:** n/a — no gated capability (setting change applies at next host restart; sweep migration is one-shot sentinel-gated with no deny path)
- **Verdict:** invariants-met
- **Gap:** none

<a id="f6c3790"></a>
### `f6c3790` — feat(ez-actions): generic `!EZ:<extName>:<tool>` dispatch

- **Failure-path test:** yes
  - `src/runtime/ez-actions/__tests__/resolve-bundled.test.ts:110` ("user-installed extension name → null"); also L115, L139, L143, L148, L152, L156, L161, L166
  - `web/src/__tests__/api-ez-actions-generic.server.test.ts:331` ("user-installed (non-bundled) extension name → 404"); also L339, L394 (id-enumeration 404)
  - Verify: `bun test ./src/runtime/ez-actions/__tests__/resolve-bundled.test.ts -t 'non-bundled rejection'`
- **Doc surface:** n/a — surface is internal dispatch syntax; no user-facing docs page exists for the `!EZ:` family (legacy `!EZ:distill` documented inline in extension `package.json` descriptions, but the generic-dispatch grammar is closer to internal-dispatch than user-facing UI per CONTEXT.md's invariant definition)
- **Audit row on block-path:** n/a — route returns 404 on bundled-trust gate failure (pre-PDP); no audit hook on route 404 path. Tool-level PermissionEngine gate inside `executeToolCall` DOES emit audit rows on capability-denied, but that's downstream of the bundled-trust gate.
- **Verdict:** invariants-met
- **Gap:** none

<a id="7cc5efc"></a>
### `7cc5efc` — fix(extensions): close v1.4 install-gate integration gap

- **Failure-path test:** yes
  - `src/extensions/__tests__/env-key-leak-install-block.test.ts` integration block (full install flow end-to-end with synthetic user-install manifest declaring `OPENAI_API_KEY`, asserts `EnvKeyLeakInstallError` propagates + no DB row persists; `actor === "system"` covered)
  - Verify: `bun test ./src/extensions/__tests__/env-key-leak-install-block.test.ts` (run the post-7cc5efc integration block — title visible via test runner; pick the canonical describe title)
- **Doc surface:** n/a — internal wiring correction; the user-facing contract from `cae5c06` (`docs/extensions/AUTHORING.md` § 4) was already documented correctly. This commit just made the contract reachable on the user-install path.
- **Audit row on block-path:** yes (inherited)
  - Inherits `cae5c06`'s `EXT_AUDIT_ACTIONS.ENV_KEY_LEAK_INSTALL_BLOCKED` emission at `src/extensions/clamp-permissions.ts:354-383`; this commit made the gate reach the emission path for user installs.
  - The integration test added in this commit asserts the audit row IS emitted on the now-reachable user-install path.
- **Verdict:** invariants-met
- **Gap:** none

<a id="b652f2b"></a>
### `b652f2b` — fix(extensions): rename `compactionIntervalHours` to snake_case

- **Failure-path test:** yes — explicit regression guard
  - `extensions/memory-extractor/manifest-load.test.ts:60` ("memory-extractor manifest does NOT declare camelCase `compactionIntervalHours`"); also L38, L49, L71
  - Verify: `bun test ./extensions/memory-extractor/manifest-load.test.ts -t 'does NOT declare camelCase'`
- **Doc surface:** n/a — internal manifest-key rename; user-facing UI references the setting by label ("Compaction interval (hours)"), not by key
- **Audit row on block-path:** n/a — rename, no gated capability or block code path
- **Verdict:** invariants-met
- **Gap:** none

<a id="763d718"></a>
### `763d718` — chore(worker): pin wrangler to ^4.90.0 (closes dependabot CVE-2023-3348)

- **Failure-path test:** n/a — dependency-pin commit; `worker/` is currently vestigial with no CI / deploy / test surface; no test exists or would be meaningful for a `package.json` version-range pin
- **Doc surface:** n/a — internal dependency hygiene closing Dependabot alert (CVE-2023-3348, directory traversal in `wrangler pages dev`); no user-facing doc surface mentions wrangler (verified: `grep -rn wrangler docs/ README.md CHANGELOG.md` returns no hits)
- **Audit row on block-path:** n/a — build-time dependency constraint, no runtime block-path
- **Verdict:** invariants-met
- **Gap:** none

---

*Audit complete: all 6 retro-claimed commits meet the invariants per CONTEXT.md's pragmatic definitions ("internal renames and CI patches are legitimately `n/a` here"). Zero `CLAIM-GAP-NN` entries surfaced — `tasks/v1.4-retro-claim-gaps.md` will ship empty (or absent) per the zero-gap outcome in CONTEXT.md.*
