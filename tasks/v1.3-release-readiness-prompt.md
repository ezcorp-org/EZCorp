# v1.3 → release-readiness pass (original prompt)

> Saved 2026-05-09. Source-of-truth for resuming this work in a fresh session.

## Context

EZCorp v1.3 Security & Permissions shipped, audited, archived, and pushed
on 2026-05-09. The milestone audit verified completion criteria (commits
exist, files present, targeted tests green) but did NOT verify
release-readiness. Before exposing v1.3 to real users, work through the 7
items below.

## Repo state

- **Working dir:** `/home/dev/work/EZCorp/ez-corp-ai` (single worktree on main, clean)
- **Audit report:** `.planning/milestones/v1.3-MILESTONE-AUDIT.md` (full breakdown)
- **Roadmap:** `.planning/ROADMAP.md` + `.planning/milestones/v1.3-ROADMAP.md`
- **Project state:** `.planning/PROJECT.md` (Validated reqs through v1.3)
- **Original perm-system design doc:** `.planning/milestones/v1.3-permission-system-original-plan.md` (read for capability/PDP context)

## Sacred constraints

- **Bun only.** `bun`, `bunx`, `bun test`. Never `npm`/`yarn`/`pnpm`/`npx`/`node`.
- **Stash count is 11.** Verify at start: `git stash list | wc -l`. Never run `git stash *` of any kind.
- **`tasks/` is gitignored** (some files like `capability-expiry-design.md` are tracked from before the ignore; new files in `tasks/` won't propagate to worktrees).
- **`.planning/` is gitignored** except for `MILESTONES.md`, `STATE.md`, `ROADMAP.md` which are tracked.
- **No `git push --force`.** No `--no-verify`. No `git config` changes without asking.
- **Local git user.email is `arch1tect0x@users.noreply.github.com`** — don't change it (GH007 email-privacy rejection if you do).
- **Parallel sessions may push to main during your work.** Rebase, don't merge. Check `git log origin/main..main` before any push.
- Test runner: `scripts/test.sh` (parallel per-file, prevents mock pollution). NOT `bun test` directly from repo root — that runs serial and hangs.
- Vitest: `cd web && bunx vitest run` (NEVER `--reporter=basic` — tries to load custom reporter and crashes).
- Playwright: `cd web && bunx playwright test`.
- Typecheck: `bash scripts/typecheck.sh`.
- Biome: `bunx biome check <files>`.

## Work to do (in suggested order)

### 1. Run full Playwright e2e
```bash
cd /home/dev/work/EZCorp/ez-corp-ai/web && bunx playwright test 2>&1 | tee /tmp/playwright-v1.3-baseline.log
```
184 spec files exist; pass/fail count unknown. Triage failures into:
- v1.3 introduced (must fix)
- Pre-existing (file in `tasks/v1.4-pre-existing-failures-triage.md`)
- Bun 1.3.11 segfaults (deferred upstream — don't bother)

### 2. Triage backend + vitest failures
```bash
cd /home/dev/work/EZCorp/ez-corp-ai && bash scripts/test.sh 2>&1 | tail -30
cd web && bunx vitest run 2>&1 | tail -15
```
For each failure: real bug, stale assertion, or pre-existing-and-deferred?

### 3. Decide Ez page-context fate
`fill_form` and `navigate_to` are intentionally non-functional pending
the page-context redesign (deferred v1.2 → v1.3 → v1.4). Pick:
- (a) Ship the redesign now.
- (b) Hide the broken affordances.
- (c) Leave visible with "coming soon" tooltip.

### 4. Production deployment dry-run
```bash
docker build -t ezcorp-v1.3-rc .
docker run --rm -p 4173:4173 -v $(mktemp -d):/data ezcorp-v1.3-rc
curl http://localhost:4173/api/ready
```
Walk a smoke flow: signup → install ext → fire tool call → audit drill-down. Verify migration rollback at `src/db/connection.ts:125`.

### 5. Manual UAT against the security backbone
- 4-scope capability-grant modal
- HostMaintenanceDaemon expiry sweep + ExpiredGrantsBanner
- "Re-approve" manifest-ceiling clamp
- In-chat capability event pills
- /audit admin page drill-down
- Manually expire grants → chat re-prompt

### 6. External security review (if shipping to non-trusted users)
Verify in code:
- PDP fail-closed when unwired (C6: `src/extensions/permission-engine.ts`)
- Manifest ceiling enforcement on reapprove
- Cross-extension capability intersection on `ezcorp/invoke` (C1: `tool-executor.ts handlePiInvoke`)
- MCP isolation depth (user/mount ns + prlimit + bearer-token proxy + per-host PDP)

### 7. User-facing documentation
- "What is the four-scope permission modal?"
- "Why was my grant expired?"
- "How to read the audit drill-down"

Output: `web/src/routes/(docs)/...` or markdown under `docs/`.

## DO NOT
- Run `/gsd:new-milestone` until release-readiness is complete
- Modify `.planning/MILESTONES.md` v1.3 entry
- Re-run `/gsd:audit-milestone v1.3`
- Force-push, rewrite history
- Touch `ez-corp-ai-perm-system` worktree (gone)
