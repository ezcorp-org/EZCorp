# v1.3 Release-Readiness Tracker

> Started 2026-05-09. Source-of-truth for the post-audit release-readiness pass.
> Original prompt: [`v1.3-release-readiness-prompt.md`](v1.3-release-readiness-prompt.md)
> Audit baseline: [`.planning/milestones/v1.3-MILESTONE-AUDIT.md`](../.planning/milestones/v1.3-MILESTONE-AUDIT.md)

## Test re-baseline (2026-05-09, post-e45897a)

| Suite | Initial | Final (post-fixes) | Δ |
|---|---|---|---|
| Backend (`scripts/test.sh`) | 7614 pass / 21 fail | **7616 pass / 20 fail** / 541 files | +2 pass, -1 fail |
| Vitest (`web/`) | 1805 pass / 1 fail | **1808 pass / 1 fail** / 1809 total | +3 pass (new HIGH-2 tests) |
| Playwright | _baseline run_ | 868 pass / 399 fail / 14 skip / 5.8 min | A1 fix verified individually |
| Typecheck | clean | **clean** | — |

**Vitest fail (unchanged):** `api-tool-invoke.server.test.ts` — `PermissionEngine not initialized`.

**Backend failing files (12):**
- ✅ Bun 1.3.11 segfault (deferred upstream): `af1-mcp-sandbox-regression`, `mcp-api-routes`, `mcp-e2e`
- ✅ Pre-existing in audit tech-debt: `executor-attachment-resolver-wiring`, `executor-task-tracking-autowire`, `memory-validation`, `mock-cleanup-coverage`, `queries-lessons`, `scope-enforcement`, `spawn-assignment-handler`
- ✅ Pre-existing, MISSED in audit triage (now classified):
  - `agent-configs-handler` — single `rate limit > 60 tight-loop calls → -32029` test; same flake class as `spawn-assignment-handler`. v1.3's commit `8a0ef6f` added unrelated PDP-deny tests; the failing rate-limit test predates v1.3.
  - `agent-input-form` — single `web build > svelte app builds successfully` test; 60-sec timeout on `bun run build` in parallel runner. Test file (`src/__tests__/agent-input-form.test.ts:219-244`) explicitly documents this as a known flake and skips in CI.

**Conclusion:** No v1.3-introduced backend failures. All 12 failing files are pre-existing or upstream-Bun blocked.

## Items

### 1. Playwright e2e triage — ✅ closed (commit `8085435`)
Full report: [`v1.3-playwright-triage.md`](v1.3-playwright-triage.md). 868 pass / 399 fail / 14 skip / 5.8 min. **Zero v1.3-introduced regressions in production code paths.** The 399 fails are pre-existing test debt; v1.3 touched 116 source files (38 routes + 25 components) without updating e2e specs.

**1 v1.3-introduced WCAG bug found and fixed:**
- `ModelSearchPicker.svelte` combobox lacked accessible name when placeholder was suppressed → added `aria-label="Search models"`.
- `/agents/[name]/+page.svelte` Test CTA used `bg-amber-600 + white` (~3.36:1, fails WCAG AA 4.5:1) → bumped to `bg-amber-700` (~4.97:1).
- Verified: `bunx playwright test e2e/accessibility-mobile.spec.ts -g "Agent Detail"` → 1 passed.

v1.4 follow-up: 398 stale-locator/timeouts fold into existing `tasks/v1.4-e2e-coverage-100.md` workstream.

### 2. Backend + vitest triage — ✅ closed (orchestrator)
Re-baseline + classification complete (see "Conclusion" above). All 12 failing backend files are pre-existing or upstream-Bun blocked. The 1 failing vitest test (`api-tool-invoke.server`) is also pre-existing (already in audit). No v1.3-introduced regressions in backend or vitest.

**v1.4 follow-up:** add `agent-configs-handler` and `agent-input-form` to `tasks/v1.4-pre-existing-failures-triage.md` (audit missed them).

### 3. Ez page-context fate — ✅ closed (commit `9058ea9`)
`fill_form` / `navigate_to` are LLM-facing tools, not UI buttons. "Coming soon" treatment landed in two places:
- Tool descriptions prefix `[COMING SOON — currently disabled]` (fill_form) / `[LIMITED — page-context redesign pending]` (navigate_to) so the model avoids them.
- `client-tool-dispatcher.ts` returns a friendly "Filling forms on the current page is coming soon" instead of the technical "no handler registered" error. New `coming-soon` code added to `DispatchResult` union; old `no-handler` code retained for round-trip-test back-compat.
- `ez-client-tool-dispatcher.test.ts` updated to expect new code/message.

### 4. Production deployment dry-run — 🟡 ready for user
Checklist prepared at [`v1.3-uat-docker-checklist.md`](v1.3-uat-docker-checklist.md) §4. Note: prod port is **3000** (not 4173 as in original prompt). User executes; results triaged into release-blocker / v1.4 / already-known.

### 5. Manual UAT — 🟡 ready for user
Checklist prepared at [`v1.3-uat-docker-checklist.md`](v1.3-uat-docker-checklist.md) §5. Six flows: install grant modal, expiry sweep + banner, re-approve clamp, in-chat pills, /audit drill-down, manual full expiry → re-prompt.

### 6. External security review — ✅ closed (commits `f6dadec`, `ff81344`)
Full report: [`v1.3-security-review.md`](v1.3-security-review.md). Both HIGH findings fixed; PASS-with-caveat findings (Claim 1 messageToolbar audit-bypass, Claim 4 MCP gaps) and 5 LOW cross-cutting findings remain documented for v1.4 follow-up.

**HIGH 2 fixed (`f6dadec`)** — `fix(perm): clamp reapprove to manifest + bundled ceiling; persist install-time narrowed choice`:
- New nullable `installed_permissions` jsonb column on `extensions` row (Drizzle migration); legacy rows leave it null.
- Both install paths (`activate/+server.ts` user install + `bundled.ts` bundled install) now persist the narrowed choice the user actually approved.
- `reapprove/+server.ts` rewritten: clamps against `installedPermissions ?? manifest.permissions`, then second-stage clamps against `getCeiling(name)` for bundled extensions. Bundled-ceiling bypass closed; user-narrowed grants now restored on re-approve.
- 3 new vitest cases in `cap-expiry-flow.server.test.ts` cover bundled clamp + narrowed restore + legacy fallback.

**HIGH 3 fixed (`ff81344`)** — `fix(perm): make ezcorp/invoke capability intersection the default (closes C1)`:
- `tool-executor.ts:946-959` inverted: intersection runs by default; `acceptsCallerCaps: true` becomes the OPT-OUT (callee bypasses caller's cap intersection). Comment block cites C1 design + this security review.
- Bundled-extension manifest sweep: zero extensions declare `acceptsCallerCaps: true`, so no manifest patches needed.
- 1 new test case `(j)` in `cross-ext-attribution.test.ts` for previously-untested confused-deputy default. Cases `(a)`/`(b)` flipped to assert intersection IS the default; `(c)`–`(i)` re-asserted under inverted opt-out semantics. 3 tests in `cross-extension.test.ts` adjusted.

Plus 5 LOW cross-cutting findings remain in [`v1.3-security-review.md`](v1.3-security-review.md) — file as v1.4 issues:
- CC1: `loadConversationOverride` DB-blip widens cap envelope to registry grants (fail-OPEN relative to override intent).
- CC2: audit write failures swallowed without stderr fallback.
- CC3: `MAX_CALL_DEPTH = 10` is per-chain, not per-conversation (fan-out attack surface).
- CC4: reapprove writes `PERMISSION_GRANTED` audit action (couples to first-time grants).
- CC5: `internal-host.ts` regex `^localhost` is unanchored at end (false-positive only, no security hole).

**Claim 1 caveat to track:** `events/[event]/+server.ts:313-372` swallows engine wiring errors and bypasses PDP audit on messageToolbar shortcut. NOT a fail-open (legacy boolean still gates the call) but breaks audit visibility. v1.4 follow-up.

### 7. User-facing documentation — ✅ drafted, pending commit
Three new files under `docs/permissions/`:
- `four-scope-modal.md` (4.1k)
- `capability-expiry.md` (4.8k) — pulls TTL values verbatim from `src/extensions/perm-expiry-config.ts` for accuracy
- `audit-drilldown.md` (6.4k)

No incidental edits to other docs. No README/index added (would be a separate decision). Convention: top-level `docs/` markdown grouped by subject (matches `docs/extensions/` precedent). Uncommitted on main worktree.

## Local commits ahead of `origin/main` (as of 2026-05-09 closeout)

```
ff81344 fix(perm): make ezcorp/invoke capability intersection the default (closes C1)
f6dadec fix(perm): clamp reapprove to manifest + bundled ceiling; persist install-time narrowed choice
9058ea9 feat(ez): mark fill_form/navigate_to as coming-soon pending page-context
8085435 fix(a11y): close mobile WCAG violations on /agents/[name]
23627f5 docs(permissions): user-facing docs for v1.3 trust boundary
```

**Not pushed.** User authorization required before `git push`.

## Final state

- Items 1, 2, 3, 6, 7: ✅ closed.
- Items 4, 5: 🟡 ready for user — paste-ready checklists at [`v1.3-uat-docker-checklist.md`](v1.3-uat-docker-checklist.md). Once the user runs them and reports `❌` items, those triage into release-blocker / v1.4-issue / already-known.
- Stash invariant: **11 entries preserved** throughout this work (verified at preflight, after each agent run, and at closeout).

## v1.4 backlog generated by this pass

- Pre-existing test triage additions: `agent-configs-handler.test.ts` rate-limit flake; `agent-input-form.test.ts` SvelteKit-build flake (author-documented).
- Playwright stale-locator debt: 103 spec files / 398 tests need locator + waitForResponse refresh against v1.3's UI/route reshape. Folds into existing `tasks/v1.4-e2e-coverage-100.md`.
- Security follow-ups (from review CC1–CC5 + Claim 1 caveat): conv-override DB-blip envelope widening; audit write fallback; per-conversation MAX_CALL_DEPTH; reapprove dedicated audit action; internal-host regex anchoring; messageToolbar PDP audit-bypass; MCP isolation hardening (DNS rebinding, env-leak, /tmp overlay, seccomp, netns).
- Doc accuracy nit: `four-scope-modal.md` describes "Allow this time" as one-call; engine code shows `session` scope is persistent-until-restart. Wording cleanup.

## Stash invariant
Verified preserved at 11 entries throughout this work.
